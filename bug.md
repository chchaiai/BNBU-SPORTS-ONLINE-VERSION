BUG-01 系统维护预计恢复时间显示不友好

涉及端： 学生端、教师端

现象：

系统进入维护模式后，预计恢复时间直接显示类似：

2026-09-11T14:14:00.000Z

普通用户难以理解，同时存在 UTC / 本地时间混淆风险。

期望：

统一转换为用户可理解的本地时间格式，例如：

2026年9月11日22点14分

需要确保时区转换正确，不得直接展示 ISO / UTC 原始字段。

BUG-02 切回正常模式后客户端仍停留在维护状态

涉及端： 学生端、教师端、管理端系统模式

现象：

管理员执行：

维护模式 → 正常模式

之后：

学生端仍显示系统维护；
教师端仍处于维护状态；
系统模式历史记录中：
正常模式 → 维护模式：维护公告显示「已发布」；
维护模式 → 正常模式：维护公告显示「未发布」。

实际系统状态与管理端操作结果不一致。

期望：

切换至正常模式成功后：

后端真实系统模式恢复正常；
学生端、教师端能够重新正常进入系统；
客户端能够获取最新系统模式；
历史记录与真实操作结果保持一致；
正常模式恢复不应错误依赖维护公告发布状态。

重点排查：

系统模式持久化、事务、缓存、客户端状态刷新、接口响应以及维护公告逻辑之间是否存在错误耦合。

BUG-03 课程完成进度条始终为空或灰色

涉及端： 管理端课程目录看板

现象：

课程列表每门课程下方的「完成全部打卡学生占比」无法正常显示，进度条始终为空或灰色。

即使课程已经存在学生和打卡记录，也没有显示有效进度。

期望：

根据真实业务数据正确计算和显示完成率，例如：

已完成课程目标学生数 ÷ 应完成目标学生数

同时正确处理：

0 人；
暂无完成学生；
无目标人数；
数据尚未产生；

等边界状态。

BUG-04 批量创建教师账号始终失败

涉及端： 管理端 → 用户与账号 → 批量建立教师账号

现象：

填写合法教师信息并进行校验/创建后，教师记录持续显示：

「操作未完成，请重试。」

导致合法教师账号无法正常创建。

期望：

合法教师数据能够：

校验通过 → 创建教师账号 → 正确写入数据库 → 页面反馈成功。

如果确实创建失败，应返回具体且用户可理解的失败原因，而不是统一模糊提示。

重点排查：

CSV 解析、字段映射、DTO、创建教师接口、数据库约束、账号重复判断、首次密码状态以及后端异常处理。

BUG-05 新增分管理员时请求结果无法确认

涉及端： 管理端 → 分管理员设置

现象：

填写合法账号、部门及权限并创建分管理员后，页面提示：

「请求结果暂时无法确认，请重试或刷新核对最新状态。」

用户无法确认实际创建结果。

期望：

创建请求必须得到明确结果：

成功 → 显示创建成功并刷新分管理员列表；
失败 → 显示明确失败原因；
网络结果未知 → 能够安全查询实际结果，避免重复创建。

重点排查：

接口响应、超时处理、幂等机制、数据库实际写入状态、前端请求状态判定及创建后的刷新逻辑。

BUG-06 学生录制视频完成后无法读取

涉及端： 学生端 → 打卡 → 视频拍摄与上传

环境： 腾讯云部署，媒体使用 COS。

现象：

学生通过网页直接录制视频，结束录制后出现：

「无法读取」

同时页面或错误信息中出现类似：

live...

等浏览器/媒体底层字段，之后无法正常继续视频上传或提交。

期望：

完整链路应能够正常完成：

拍摄 → 结束录制 → 读取视频 → 生成媒体文件 → 获取上传授权 → 上传 COS → 预览 → 提交打卡。

任何异常都应该转换为用户可理解的信息，不应直接展示内部媒体对象或技术字段。

重点排查：

MediaRecorder、MediaStream、Blob、临时 Object URL、MIME Type、文件封装、COS Pre-signed URL、Content-Type 与签名 Required Headers 的一致性。

BUG-07 教师无法评论学生打卡记录

涉及端： 教师端 → 学生打卡记录详情/审核

现象：

教师查看学生提交的打卡记录后，无法正常提交评论/评语。

评论操作可能没有成功保存，或者提交后页面没有正确显示结果，导致教师无法完成对学生打卡记录的反馈。

期望：

教师拥有对应课程和记录权限时，应能够：

查看打卡记录 → 输入评论 → 提交 → 后端保存 → 刷新后仍然存在 → 学生端按业务规则查看评论。

如果操作失败，应给出明确且用户可理解的原因。

重点排查：

评论接口是否真实接线、教师权限校验、打卡记录 ID 映射、Request/Response DTO、数据库评论字段或评论表、保存事务以及前端刷新逻辑。


所有 Bug 修复后，必须先在本地真实 Docker 环境逐项完成回归；本地通过后重新部署到腾讯云当前环境，再使用真实 PostgreSQL、COS、后端及网页端逐项进行线上验收。不得仅以单元测试、Mock、代码检查或本地测试通过作为完成依据。