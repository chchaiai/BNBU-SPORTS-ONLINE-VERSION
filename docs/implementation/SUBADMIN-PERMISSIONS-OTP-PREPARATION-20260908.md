# 分管理员权限复测与 OTP 邮件基础

修复原权限测试夹具的工号过长问题后，首次复测在分管理员邮箱登录处返回 401（docker-subadmin-permissions-fix-20260908.txt）。现行业务明确分管理员使用独立登录账号；夹具补齐 v81_account_security.login_account 并使用该账号登录，未放开邮箱登录规则。

docker-subadmin-permissions-final-20260908.txt 退出 0：52迁移/构建/基础账号通过，权限修改、同键重放、并发 201/409、撤销全部权限、已有会话即时撤权和恢复、分管理员/教师不能授权、不能修改总管理员、重复/非法权限与密码字段422、4条不可变版本事件且不含密码，均通过。保留原失败及第一次修复失败证据。

新增 ADMIN_IDENTITY_VERIFICATION 邮件用途，SMTP 和腾讯 SES 均使用管理员身份核验的中英文文案。docker-admin-otp-delivery-20260908.txt 退出0，6项适配器测试通过，包括两种语言的身份用途、原投递参数及有界失败重试。这里使用发送适配器替身，不是实际 OTP 身份核验闭环或腾讯云投递证明。

下一步实现核验挑战与原子建号：验证码只保存摘要，10分钟有效、最多5次错误尝试、60秒重发间隔；绑定组织、操作者、规范化邮箱及用途。创建时重新校验唯一账号、邮箱、至少一项业务权限及初始密码非空且一致，核验凭据一次消费。邮箱变更使用新邮箱 OTP，登录账号保持不可修改。秘密不写普通事件或日志。

这些技术参数由本任务按用户授权决定，后端挑战、核验、创建/编辑及网页接线仍需实现。学生注销与教师删除按最新决定保持暂缓，不能因为该权限复测通过恢复删除。
存档检查：明确暂存14个文件；git diff --cached --check退出2，仅3个原始Docker日志尾随空格，保留输出并记录未通过。
