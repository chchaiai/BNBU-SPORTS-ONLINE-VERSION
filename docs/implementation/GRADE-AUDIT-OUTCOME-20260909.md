# 成绩事件明确结果

2026-09-09。统一审计网页曾发现当前成绩保存事件 outcome 为空。代码核对确认 v81-final-grades 写入没有填写 0051 已提供的 event_outcome，导致新事件与未知历史事件一样显示未记录结果。

本次仅在新成绩草稿、发布及正式更正成功事务中明确写入 SUCCEEDED。使用原有教师身份快照触发器；不更新历史，不推断失败请求为成功，不修改网页或协议。

## Docker 验证

- `grade-audit-docker-20260909.txt`：score.e2e.test.ts 4/4。草稿/发布两条事件均为 SUCCEEDED/TEACHER，同键回放不增加事件；并发成功一条、失败及越权/非法参数不增加成功事件；成绩隐私和旧接口拒绝回归通过。
- `grade-audit-settlement-docker-20260909.txt`：v81-settlement.e2e.test.ts 1/1。实际报告写入故障触发成绩和审计回滚，同键恢复与更正回放、并发更正仅一个成功；最终两条新更正事件均为 SUCCEEDED/TEACHER，报告版本和学生隐私断言通过。
- 使用隔离 Docker PostgreSQL bnbu_sports_test，构建及运行响应校验钩子；日志完整保留。本轮专项共 5 项，不替代完整全量重跑。此前第二十轮 97/97 是修复前的最新全量。
- `grade-audit-browser-rebuild-20260909.txt`：持续浏览器后端重建记录，使本地后续操作使用此次写入逻辑。

其他模块的空结果尚未逐项审计；历史空值继续保留并显示未记录结果。本轮未重新执行所有三端业务或宣称全量审计完备，云端仍未部署。
