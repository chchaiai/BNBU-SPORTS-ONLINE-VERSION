# 教师与管理员密码恢复 HTTP 验证

2026-09-09 13:31。学生业务 §5.1/5.3 明确无传统密码；学生使用邮箱OTP，失去旧邮箱时联系体育部门/管理员核验身份。因此不新增学生找回密码操作。

后端 client-authentication.service.ts 已允许 TEACHER/ADMIN 通过账户恢复挑战重置，旧 E2E 标题“only for a teacher”只是覆盖范围，不是当前权限规则。本轮将用例参数化为两个角色，分别建立两个真实登录会话，完成恢复后验证两份旧access token访问/me均401、数据库无旧ACTIVE会话，新密码可重新登录，原会话REVOKED且tokenVersion递增。

Docker构建、类型检查、client-capabilities完整套件14/14通过，10992.002365 ms；见 admin-recovery-docker-20260909.txt。无本轮失败或业务源码改动。恢复挑战及验证码摘要由测试夹具明确注入，完成/旧会话拒绝/新密码登录走真实HTTP和PostgreSQL；不证明邮件投递或浏览器找回全流程。第38次99项全量基线早于本新增管理员测试，不称为当前全量100项通过。

下一步：教师/管理员网页找回实际邮件与确认闭环仍需验证。当前Portal请求会携带部署配置中的organizationCode，页面不要求用户输入；管理员业务 §5.2 提到 PASSWORD_RESET 挑战，现实现名称为 ACCOUNT_RECOVERY，协议/业务命名一致性仍需明确核对，不能仅按这次测试宣布全流程已完成。学生换绑双邮箱网页已有独立证据，首次绑定与其余实际入口按当前矩阵继续核对。OCR界面调整仍待答复。
