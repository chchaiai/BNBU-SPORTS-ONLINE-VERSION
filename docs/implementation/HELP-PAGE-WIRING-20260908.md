# 帮助管理页面接线（2026-09-08）

正式模式复用仓库已有帮助管理组件：统计、筛选、分页、文章编辑框和发布确认。真实列表来自管理员接口，创建/编辑/发布/下线/重新上线使用固定版本和幂等键保存；成功后重新读取服务器列表。未修改 CSS 或现有管理组件布局，移除旧正式模式只读分支。

提交期间阻止重复请求；响应未确认时保留原请求，锁定输入及关闭/取消，允许原请求重试。明确失败会解除待确认请求；保存成功但列表刷新失败仍保留原请求以便同键恢复。

浏览器使用持续本地 Docker 后端及合成管理员。`help-page-browser-first-20260908.txt` 首次因脚本按钮名“创建文章”与真实“新建帮助文章”不一致超时；修正选择器后 `help-page-browser-retest-20260908.txt` 退出 0，验证创建草稿丢响应→输入锁定→同键重试→发布→下线→重新上线→编辑正文回读。截图 `help-page-wiring-20260908.png` 已检查。前后两次 Portal tsc 均退出 0。

Docker 网页模块列表回归证据：`docker-help-page-list-20260908.txt`，退出 0；涵盖草稿及各发布状态全量分页读取、保存意图回放和帮助生命周期权限回归。

剩余：整页刷新/离开页面后的待确认请求恢复、多人编辑冲突浏览器专项和移动视口尚未验证；帮助内容安全专项及三端全量验收继续。当前实现的待确认请求仅保留在组件内存。未执行云端操作。
