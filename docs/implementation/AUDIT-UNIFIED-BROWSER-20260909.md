# 管理员统一审计网页接线

2026-09-09。审计页原调用 /audit-logs，仅覆盖基础审计；结果、操作人、资源 ID 仅在当前页本地过滤，漏掉其他页。现有列表/详情接入 /admin/audit-events，服务端处理精确请求 ID、操作人 UUID、资源 UUID、操作、结果及日期。日期直接传 YYYY-MM-DD，由后端组织时区确定边界。

来源 FOUNDATION/V81 与原事件 ID 组合为 UI 行键和详情引用，详情请求明确指定来源，避免来源间 ID 冲突。未伪造统一投影没有提供的组织、权限、哈希等字段；类型改为可选。历史 outcome=null 显示“未记录结果”，不作为成功，也不生成虚构的结果筛选项。SUCCEEDED 沿用成功标签逻辑。

保留现有筛选区、表格、分页、抽屉和样式。操作人和资源 ID 使用后端精确 UUID 校验；无效值由原错误组件显示。演示日期筛选适配到日期字段。

## 验证

- audit-unified-browser-20260909.txt：首批 50 条、下一页无重复；从首批之外选择事件，真实服务器按操作人/资源 ID 返回对应事件，详情通过。
- audit-unified-browser-final-20260909.txt：另验证日期和结果条件实际进入请求，FOUNDATION 详情通过。
- audit-unified-v81-browser-20260909.txt：测试假设历史事件必有结果而失败，原始日志保留。
- audit-unified-v81-browser-retest-20260909.txt：V81 来源、日期、资源/操作人查询及 null 结果显示通过，退出 0。该历史记录不适合验证非空结果筛选，非空情况由上一 FOUNDATION 分支覆盖。
- Portal TypeScript 退出 0，audit-unified-typecheck-final-20260909.txt。截图 audit-unified-v81-20260909.png；已检查原抽屉布局。测试使用运行中的 Docker 后端和合成历史事件，没有创建正式业务数据，仅产生正常只读审计查询事件。

当前完整 Docker 基线仍为第二十轮 97/97。日志 ZIP 仍使用旧导出接口，是接下来的独立接线缺口；本轮不能宣称日志下载或全部审计功能完成。筛选条件跨刷新保存、并发切换查询竞态、移动视口和更多权限组合尚未由本轮证明。
