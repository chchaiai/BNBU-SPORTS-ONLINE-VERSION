# 邀请撤销 Docker E2E（2026-09-08）

在统一进班 E2E 的实际邀请签发、凭据签发、学生进班成功链路后增加邀请撤销。其他教师404，管理员403；责任教师撤销成功，同 key 重放相同结果，新 key 再次撤销409。旧邀请预览及再次申请进班凭据返回 COURSE_INVITE_REVOKED，无新有效邀请产生。已有 ACTIVE 选课仍为1条，选课状态事件仍为1条，撤销事件仅1条且不含原邀请令牌。

首轮4/5通过，严格协议检查发现撤销越权404未声明；已补充协议并重新生成产物，静态parity通过。复测5/5通过。日志 docker-invite-revocation-e2e-first-20260908.txt、docker-invite-revocation-e2e-retest-20260908.txt。真实 Nest HTTP、专用 Docker PostgreSQL 与严格响应协议校验；学生身份为合成输入，进班后仍待邮箱绑定。本轮不等同于已发未使用凭据在撤销后尝试进班的完整竞态验收。未改业务实现或 UI。整体目标继续。
