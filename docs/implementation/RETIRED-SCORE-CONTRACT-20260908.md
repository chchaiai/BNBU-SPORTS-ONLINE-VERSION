# 旧成绩协议与停用清单同步（2026-09-08）

依据已归档业务审计及14个入口的28次有身份拒绝证据，移除这14个旧成绩操作的2xx响应声明，保留deprecated和停用原因。运行清单把它们归入implementedDefaultDeny，停用数量由17变为31，并指向现行控制器守卫与验证证据；未把未验证的新接口自动加入implemented。

检查器用清单明确的expectedDisabledOperationCount校验停用总数，同时拒绝重复停用条目和OpenAPI不存在的条目。既有缺少响应、非法响应、停用路径意外成功等失败要求保留。新增单元回归覆盖数量错误、重复和未知三类配置问题。

验证日志docker-retired-score-contract-first-20260908.txt；249操作静态parity通过，Docker检查器单元5/5、成绩HTTP套件4/4通过。总清单仍126条，与249操作不一致，这是剩余真实清单缺口，仍会失败。学生注销暂缓和旧管理员课程写入口尚需独立同步，不能仅按缺少成功证据分类。整体目标继续，未改UI。
