# 人工审核补证与学生进度 E2E（2026-09-08）

在 exercise-record.e2e.test.ts 增加现行 V81 人工审核完整场景，专用 Docker PostgreSQL 中运行并启用严格 OpenAPI 响应校验。运动记录套件 9/9 通过，进程退出 0。

真实 HTTP 验证：学生创建并提交记录，待审进度为零；责任教师退回 24 小时补证，学生待办和倒计时可读取；学生补交同一已验证材料并幂等重放，形成第二材料版本；再次退回补证返回 422；学生自行审核返回 403；教师通过并重复请求，计入 60 分钟，学生总有效进度为 3,600 秒；工作流保留两个材料版本且标记补证已使用；课程目标读取为 72,000 秒；教师不能读取学生专用进度入口；数据库恰好一次 VALID 事件。

边界：时长一小时的已完成会话和已验证相机材料由现有数据库夹具提供，未在这个场景中真实计时或上传图像。账号使用测试身份和真实教师密码登录，模板/课程规则/人工模式配置走 HTTP。本场景验证审核与进度，不代替相机浏览器、材料存储、维护期间补证暂停或超时任务的专项验收。

首轮缺少 BNBU_RUNTIME_CONFORMANCE_REPORT 环境变量，严格校验启动拒绝；第二轮新测试误将待办 data.items 当作 data 数组，8/9；修正调用和断言后第三轮 9/9。没有修改产品实现或 UI 布局。全部原始日志保留为 docker-manual-progress-e2e-first/second/third-20260908.txt。

完整全接口门禁最新仍为第六轮 79/79 测试通过、88 项覆盖失败；本次专项不能推算整个门禁已通过。仍需其余业务成功证据、旧清单校准、三端最新浏览器回归和云端依赖交接。
