# 网页缺陷修复与验收 · 2026-09-10

状态：2026-09-10 19:08，本轮29项已完成修复或业务裁定；本地Docker门禁通过后已部署，线上Smoke与媒体、日志下载回归通过。下文阶段记录保留当时状态，以本节最终结论为准。

## 最终交接

- 源码存档：`b83b302c1f02f9463ee0ab79150742571ebb18b2`。最终证据与部署工具另以本地提交保存，不推送远端。
- 本地最终检查：Backend单元260/260、Docker集成59/59、端到端102/102、安全53/53；Portal127/127；Student单元10/10、Smoke86/86。契约schema、权限及254项操作登记一致性通过。
- 真实30分钟打卡：实际1812秒，暂停/恢复/结束200；真实照片10047B、原生MP4 687233B上传、扫描、提交成功。教师网页原图加载与视频播放通过，审核后学生刷新显示1800秒／0.5小时。记录 `01a08af0-eff1-72c3-950d-8e73efd7b9f6`。
- 男生1000米免测、三张证明原图、45分钟课程设置与入班、成员移出/恢复、教师删除与幂等重试、维护切换、通知与窄屏对话框均完成本地真实浏览器回归。
- 截图中整段学生打卡技术说明已删除；三个门槛按钮及换算分入口已删除，显示教师实际设置的最短时长。线上 `js/screens/checkin.js` 与本地已验收发布文件逐字节一致。

### 服务器部署与线上验证

- 香港服务器 `43.129.193.7`，current：`/opt/bnbu-sports-production/releases/b83b302c-bugfix2`；旧版保留于 `releases/f0f628ae-scanner1`。
- Backend镜像：`sha256:684f803c1ee7fb97ce8d03e1515a0e0dd610e923f3a07c92a1d50fdade26eee5`；Portal镜像：`sha256:4494fb90c4a786f8c292f846778184b3281ad8b354bd1a10660f2b9bb107a5cb`。两容器最终healthy，Nginx语法通过。
- 数据库备份：`/opt/bnbu-sports-production/backups/before-bugfix-20260910T110359Z.dump`，2035624字节，`pg_restore --list`通过，私有0600。无新增数据库迁移，未执行降级或数据恢复。
- 用户授权的专用策略 `BNBUSportsHKRuntimeArchives`／`285947597` 已于19:02:02关联 `BNBUSportsHKCVMRole`／`4611686018448989343`，仅现有香港桶 `runtime-archives/*` 的私有上传、读取、分片及清理；策略内容见 `tools/production/runtime-archive-policy.json`。
- 首次部署的即时公网检查返回404，自动恢复旧容器、current及Nginx。此前未逐项输出URL，不能从该日志确定失败URL；旧版runtime-config路由确为404。部署工具补充逐URL日志及Nginx重载后的有限等待，第二次同一业务镜像与静态包发布全部200。保留首次失败、自动回滚及成功复验日志。
- Nginx实际发布配置由服务器现行文件增加 `/runtime-config.js` 精确路由生成；没有启用仓库中的OCR代理增量。发布根目录0755，日志目录0750。其他站点配置保留。
- 公网学生/教师ready、runtime-config、学生页面、教师/管理共用入口均200；真实浏览器呈现学生隐私入口、教师/管理员登录页。学生入口、app、checkin、records、i18n发布文件与验收包SHA-256/字节核对通过。
- 线上隔离测试组织：发布课程规则→开始/暂停/恢复/结束→照片与真实分片MP4上传→扫描→提交→教师读取全部mediaId与原件字节一致通过；未签名原件访问403。线上短会话仅1秒，零计入符合规则；正计入30分钟在本地完成。
- 线上管理端日志ZIP实际worker生成、同键重放、匿名401、授权下载、PK标识/长度/SHA-256通过：任务 `adf013f6-00a6-420a-a08c-b7e6d04d1001`，4880B、6条本组织HTTP记录。测试结束已停用新建隔离组织账号并提升tokenVersion，测试历史保留。

### 剩余边界与后续处理

- 未取得华为Mate80等实体手机；摄像头切换、持续录像、403恢复已在合成摄像头浏览器和真实HTTP/存储中验证，实体机兼容性仍需设备补验。浏览器不支持原生MP4时会提示更换受支持浏览器。
- 线上回归重点为真实数据库、COS、扫描、三角色媒体权限和日志ZIP；通知布局、教师删除/成员移除、维护切换及长时计入的完整交互在本地同版完成，未在真实用户账号上重复操作。
- 正式Contract发布标识仍为既有候选边界：`3.0.0-v81-local-draft` 与正式检查预期 `2.0.12-contract` 不一致。适用schema和操作一致性通过，不宣称完成正式Contract版本发布。
- OCR_PROVIDER保持DISABLED、OCR_WORKER_ENABLED=false；未证明真实AI/OCR、真实手机或本轮生产邮件送达。日志ZIP仅包含可用的本组织HTTP诊断，不补造历史日志。
- 需要回滚时，将current恢复到上述旧release，按旧release的compose启动旧固定镜像，再恢复 `/opt/bnbu-sports-production/backups/nginx-before-bugfix-20260910-retry.conf`、执行 `nginx -t` 后重载，并复查ready。数据库无迁移，正常应用回滚不恢复数据库备份；避免覆盖发布后业务数据。已实际验证首次自动回滚。
- 上线故障按用户诊断编号查结构化requestId、operationId、模板路由、错误码；本地索引保留首次失败与复验，不含密钥、Token、私有签名URL或真实用户原件。

输入：仓库根目录 `bug.xlsx`，工作表1，第3—24行，共22项。起始代码 `995c6c046299ee73e4184c2a2a1b2b8f0b289f1f`；起始工作区仅该表未跟踪。业务依据为 `docs/business/` 四份 V8.1 文档。

## 分诊与验收矩阵

| 编号 | 验收范围 | 当前结论 |
| --- | --- | --- |
| FE-001 | 课程日期、打卡截止与收尾安排 | 修正反向限制；真实创建、发布、入班及数据库日期回读通过 |
| FE-002 | 学生扫码入班 | 增加本地 jsQR 解码；真实二维码编码/备用解码与邀请入班通过 |
| FE-003 | 入班测试/技术文本 | 已清理，正式入口浏览器验证通过 |
| FE-004 | 入班与邮箱验证权限 | 用户裁定按 V8.1 先入班再验证；未激活权限拒绝及邮件验证通过 |
| FE-005 | 拍照/录像前后摄像头 | 切换摄像头释放旧流；合成设备浏览器通过 |
| FE-006 | 免测申请权限 | 申请草稿、真实上传、提交闭环通过 |
| FE-007 | 拍摄后无材料 | 修复临时上传凭据刷新及重试；照片/MP4 真实上传通过 |
| FE-008 | 教师审核失败 | 草稿不再列作待审核；已提交申请批准闭环通过 |
| FE-009 | 打卡提交权限 | 真实照片/MP4提交、1812秒会话及审核后0.5小时回读通过 |
| FE-010 | 录像时导航遮挡 | 拍摄模式隐藏底部导航；手机尺寸浏览器通过 |
| FE-011 | 1000米免测申请 | 男生真实入班验证、1000米证明上传提交、教师原图审核、学生已批准回读通过 |
| FE-012 | 录像6秒后黑屏 | 系统状态未变时保留摄像头 DOM；持续12秒录像通过 |
| FE-013 | 教师二维码 | 现有二维码编码与学生备用解码互通；真实邀请入班通过 |
| FE-014 | 课程配置报错 | 日期/收尾规则修正，字段错误可操作；45分钟课程发布回读通过 |
| FE-015 | 免测材料提交 | 三张 PNG/WebP 上传、补证、审批、学生回读通过 |
| FE-016 | 教师免测审核409 | 识别草稿状态误入审核列表；已提交申请审核通过 |
| FE-017 | 30分钟后暂停 | 服务端版本同步；最终1812秒MP4会话暂停/恢复通过 |
| FE-018 | 30分钟后结束 | 服务端最新状态确认；最终1812秒MP4会话结束通过 |
| FE-019 | 结束后提交 | 照片及MP4分片解析修复，完整提交及审核后0.5小时回读通过 |
| FE-020 | 新增管理员对话框 | 头尾固定、内容滚动；640px高视口按钮可达通过 |
| FE-021 | 手机间歇403 | 临时角色凭据刷新、上传403保留Blob重新授权；回归通过，真机待补 |
| FE-022 | 正常/维护切换 | 真实管理页面切换、丢失响应重试、刷新与恢复NORMAL通过 |
| FE-023 | 审计运行日志ZIP503 | 有界日志及本地ZIP下载通过；授权COS策略已绑定，线上4880B ZIP下载与摘要通过 |
| FE-024 | 教师/管理通知UI | 公共Bell图标；双角色通知与窄屏检查通过 |
| FE-025 | 素材均显示0.0MB | 真实字节数按B/KB/MB共用格式化；真实拍摄大小对比通过 |
| FE-026 | 教师看到全部原始照片/视频 | 三张申请原图及打卡原图、MP4实际播放通过，逐项mediaId与数量核对 |
| FE-027 | 删除学生/教师 | 教师移出本人成员并可恢复；管理员删除教师含责任/结算门禁、历史保留、丢失响应重试通过 |
| FE-028 | 学生门槛和换算分按钮 | 删除三个门槛按钮及换算分入口；实际45分钟课程只读显示通过 |
| FE-029 | 学生打卡技术字段 | 截图所示整段技术说明已删除，保留教师最短时长；浏览器通过 |

## 首次检查证据

- FE-024（用户追加）：教师端与管理端通知入口显示异常。截图中的虚线圆来自共用按钮内的字符 `◌`，已改为现有 Lucide 图标库的 Bell；待双角色、主题与窄屏视觉回归。
- 用户确认：FE-004 按 V8.1 先入班再验证邮箱；FE-023 在本地通过后授权现有角色增加 `runtime-archives/*` 专用前缀最小权限。

- SSH只读确认：香港服务器 `43.129.193.7`，两个生产容器 healthy，current 为 `/opt/bnbu-sports-production/releases/f0f628ae-scanner1`。部署对象以最新生产交接为准，早期基础设施待部署记录已过时。
- 线上最近6小时日志仅提取 operationId/statusCode/errorCode 的计数，未输出请求体、身份资料、Token或签名URL。initiateMediaUpload 201共10次，createExemptionApplication 201共4次，createExerciseRecordDraft 201共3次；没有后续confirmMediaUpload。
- FE-016诊断编号 `01a08a41-b16b-77e1-8fca-3e62ee67245b`：reviewExemptionApplication，409，EXEMPTION_APPLICATION_TRANSITION_NOT_ALLOWED。
- Docker最初未运行，默认系统安装路径不存在；按实际用户安装路径启动 Docker Desktop 后 Server 29.7.2 可用，复用既有独立本地 PostgreSQL/MinIO/Mailpit/browser 容器。

## 发布门禁

### 追加验收范围

- FE-025：素材大小显示。原逻辑统一用 MB 保留一位小数，非空小文件会被显示为 0.0 MB；共用格式化改为按真实字节数显示 B/KB/MB，打卡列表、预览与申请材料统一使用。
- FE-026：教师必须能够核对学生提交的全部照片和视频。审核区域接入受权限控制的真实原件预览，逐项比对 mediaId、数量、图像加载与视频播放；不得用演示素材证明线上真实材料可用。
- FE-026 根因补充：真实模式的申请列表缩略按钮仍调用 `openMaterialPreview` 演示预览。该入口已接入真实申请详情；详情逐张请求当前权限下的原件，失败可重新加载。真实三图浏览器校验通过；打卡视频回归仍待完成。
- FE-027：学生移除与教师账号删除。用户已明确学生按现行规则由教师移除本人班级成员并保留历史；教师删除须满足现行业务的教学/结算完成条件。现有教师删除按钮及服务仅实现 demo，需要补全受保护的真实接口，不能直接启用 demo 删除。
- FE-028（用户追加）：移除学生打卡界面的三个门槛按钮，改为只读显示教师配置的最短运动时长；移除“记录与进度”的换算分/等级/排名按钮。无配置时明确提示刷新，不假设门槛。学生 smoke 86 项通过；浏览器外观补验待完成。
- FE-029（用户追加）：删除学生打卡准备区截图所示的整段“计入由服务端……提交仍走现有会话接口……”技术说明，保留教师最短运动时长数据。

### 17:45 阶段复验

- Portal 全量构建与测试 127/127；Backend 单元测试 255/255。
- Docker 集成 59/59；最新端到端 102/102；安全 53/53。端到端动态契约验证覆盖 220/220 已启用接口成功响应、254/254 错误/权限响应及 34 个明确关闭的接口。
- 首轮全量端到端因新增接口后测试中的需登录接口总数仍为238而失败；更新为239后重跑全部端到端和安全测试通过，保留首轮日志。
- 新增教师删除接口 `/admin/teachers/{id}/delete`：管理员权限、版本、工号二次输入、进行中教学拒绝、删除登录状态、历史主体保留、旧Token失效和请求重放通过。复用0046—0049历史主体迁移，无新增数据库迁移。
- 新建课程→网页发布45分钟/每周4次/两类480+720分钟→邀请→新学生入班→真实Mailpit邮件验证→教师名单回读通过。数据库核对收尾与常规截止相隔7天。
- 两类媒体上传的过期签名403重试测试通过：重新初始化授权，保留原Blob，使用新初始化幂等键。
- OpenAPI/schema/权限登记/处理器parity均通过（254 operations）；OpenAPI lint有72项既有警告。完整 formal release 检查仍受现有 `3.0.0-v81-local-draft` 与预期 `2.0.12-contract` 版本不一致限制，未变更正式协议发布标识；该边界与已部署版本交接一致。
- 真实30分钟计时验证进行中，预计北京时间17:57完成；维护切换、更新后的教师删除UI、打卡视频播放和最终镜像验证尚待完成，因此不具备部署条件。

### 18:30 阶段复验与视频根因

- 后端260/260单元、Docker59/59集成、102/102端到端、53/53安全通过；媒体流回归增加真实浏览器MP4夹具及四类异常拒绝。
- 首轮真实30分钟会话实际1813秒，暂停/恢复与结束通过。视频上传首次失败：浏览器优先输出WebM且没有可供既有探针读取的Duration；修正WebM容器的试验后，V8.1提交层仍按规定只接收MP4。因此弃用WebM方案，拍摄优先选择原生MP4；不支持时显示可操作提示。
- 原生MP4再次暴露独立问题：MediaRecorder生成分片MP4，mvhd时长为0、真实时间在tfdt/trun。后端现在按各轨道实际采样时间计算时长，并保留摘要、字节数、音视频轨、15秒上限、位置元数据和扫描校验；元数据内存及采样数量均有上限。
- 分片MP4完整拍摄、上传、确认、绑定、AVAILABLE、打卡提交通过。教师网页逐项加载原照片和原视频，视频实际播放超过1秒、画面尺寸有效，与后端两项mediaId完全一致。
- 30分钟MP4正计入流程已重新开始，预计18:50:40完成。计时期间不重启本地后端或切换维护状态。
- 教师删除真实UI响应丢失后使用同一幂等键重试、刷新确认账号消失通过；移除学生真实UI及恢复同一历史成员关系通过。移除后教师不再读取当前学生资料，恢复后可读，学生账号未删除。
- 最新学生45分钟门槛只读、无换算分入口、无截图技术段落浏览器验证通过；12秒录像跨状态轮询及二维码解码复验通过。
- 正式协议版本边界、真机覆盖边界和线上验证仍单独记录；当前未部署。

### 错误体系与证据存档

- 学生端复用统一错误投影，管理/教师端复用 `ErrorPanel`；按身份失效、权限、状态冲突、输入问题、网络/依赖错误给出下一步操作和诊断编号。对象上传403归为上传授权问题，保留原素材并重新申请上传地址，避免误导用户为课程权限不足。
- 课程日期、目标与收尾错误只投影允许字段及业务原因，用户不接收SQL、异常栈、签名地址、密钥或任意后端文本。
- 后端错误输出 `HTTP_REQUEST_REJECTED` 与安全的 `UNHANDLED_HTTP_ERROR` 上下文，包含requestId、operationId、模板路由、状态/错误码、允许的字段名称与数据库分类。请求鉴权早期绑定操作编号，便于关联权限拒绝。
- 客户端诊断仅保存允许的错误码、路由模板、操作和UUID关联编号；未知路径和任意文本不入诊断记录。现有请求编号贯穿接口、错误面板和结构化HTTP日志。
- 运行日志目录保存有界HTTP诊断，8MiB轮换、当前文件加5份历史，目录0750/文件0640。写日志失败不使已提交业务写入变成失败响应；ZIP仍按组织、请求人、会话和短期能力授权，校验长度及SHA-256，明确只覆盖可用的本组织HTTP日志。
- [脱敏证据索引](evidence/bugfix-20260910/index.json)列出日志/截图的长度及SHA-256。初次失败与修复后的成功分别保存。当前30分钟计时日志仍会在完成后更新；索引本身不表示上线通过。
- 同一上线Smoke脚本已在本地Docker预演：创建隔离测试课程、发布规则、暂停/恢复/结束、真实照片与MP4上传、扫描与提交、教师全部原件字节比对、匿名403通过；日志ZIP实际worker、同键重放、授权下载和摘要通过。测试结束停用新建隔离组织的账号，保留测试历史。

### 本地已取得的阶段证据（尚未构成发布通过）

- Docker 接口集成 59/59；端到端 101/101；安全测试 53/53。后续错误字段、材料预览等修改需补跑相应检查。
- 真实本地教师邀请 → 学生入班 → Mailpit 邮箱验证 → 刷新会话 → 教师名单回读通过。
- 新建独立测试学生与课程：PNG/WebP 三张实际上传、第四张选择受限、关闭课程后续补证、教师接口批准、学生三张图片加载通过。
- 上传回归第一次失败原因：本次启动的预览服务漏配 MINIO_PORT，代理连接默认9000；设置19000后同一测试通过。保留首次失败截图，未将其归为产品修复。
- ZIP 真实任务、响应丢失重试、浏览器下载及大小/SHA-256校验通过。
- 教师/管理员通知铃铛与通知对话框、640px高视口下新增分管理员按钮完整可达、教师实际加载三张学生证明原图通过。
- 合成摄像头切换释放旧流、录像12秒跨系统轮询、二维码编码/备用解码互通通过；真实手机仍待验证。

全部适用缺陷有修复/业务裁定和证据，完成本地Docker真实HTTP、数据库、浏览器、异常回归后才能部署。发布使用保留旧版本的新release和固定镜像，执行备份与迁移审查；破坏性数据库/Contract或新增安全风险另行确认。正式发布与本地通过状态分别记录。
