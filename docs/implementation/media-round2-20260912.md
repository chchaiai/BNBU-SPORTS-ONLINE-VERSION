# 学生材料与拍摄第二轮修复（2026-09-12）

## 范围与根因

基线为 demand.md、现行业务文档和用户本轮七项反馈。课程详情移除本人名单和刷新按钮；资料输入框加底色与边框；拍照采用全屏相机布局。

实际失败照片经服务器只读检查：3,855,230 字节，声明长度和 SHA-256 均匹配，JPEG 的 EOI 后有 1,208 字节手机附加数据，严格像素解码正常（3072×4096）。旧校验错误要求 EOI 必须位于文件末尾。修复允许此类合法尾部，同时保留签名、尺寸、哈希、严格像素解码、位置元数据检查及病毒扫描。没有下载或存档用户原材料。

视频统一处理为 H.264 baseline level 3.1、30 fps、最长边 1280、AAC-LC；原始视频仍保留以便重试。缩略图从挂载且实际播放的静音视频读取；移除预览打开后的重复 load，避免中断 play。不能保证损坏文件或所有厂商私有编码可解码。

材料根据文件头确定 MIME，兼容手机空 MIME / 通用 MIME；可解码的额外图片格式转 JPEG。图片和 PDF 均支持提交前与提交后预览，PDF 使用同源 PDF.js 6.3.289，支持翻页并保留打开原文件入口。不可在当前设备解码的 HEIC/HEIF 等仍需导出 JPEG/PNG/PDF。

未提交申请可切换类型；先上传成功再创建服务端草稿。本机按账号保存表单和材料字节，兼容 WebKit 的文件持久化；已提交申请的补材料仍遵循原申请类型。没有更改审批和历史计入规则，无新 Migration 或 Contract 字段。

## 本地验证

- 后端单元测试 275/275，含手机 JPEG 尾部、篡改哈希和截断文件拒绝。
- 学生检查 86/86。
- Docker PostgreSQL / MinIO / Mailpit 真实业务回归六项通过：邮箱绑定、教师补卡范围、自定义规则、历史审核、照片上传计入、PDF 申请及教师审核。
- Chromium：资料样式、图片/PDF 预览、草稿恢复、切换类型、真实上传提交、上传后签名 PDF 预览、课程冗余移除、全屏拍照、含声音视频录制处理与播放通过。
- Firefox / WebKit：资料样式、图片/PDF 预览、草稿恢复、切换类型、真实上传提交、上传后 PDF 预览通过。
- Android 模拟器实际摄像头录制：720×1280，30 fps，处理后播放时间推进、无解码错误、应用缩略图生成通过。模拟器麦克风不可用，本项 audio=false；网页 Chromium 含音轨流程另行通过。

证据在 [evidence/media-round2-20260912](evidence/media-round2-20260912)。截图和测试均来自隔离的合成账号。

## 保留的失败及修复记录

首次 WebKit 文件草稿恢复失败，改为 ArrayBuffer 存储后通过；首次播放被重复 load 中断，移除后通过。测试脚本异步谓词提前结束及遗漏健康/录音提示确认已修正，最终以实际提交结束和播放器时间推进为准。

## 部署及交接

主修复源码 `b7d9eb29c7531fe80216a4d35f262e4c320736e1`，PDF 模块部署兼容补丁 `04f2ad3499a834f26deca5cad84ca512db1938d1`。当前发布目录 `/opt/bnbu-sports-production/releases/media-round2-pdf-20260912`，前一发布目录 `demand-followup-20260912` 完整保留。

后端镜像 `sha256:067665eed72712b5d55bdadc60c89023f59595b58115910c7a063c8464692944`；212 个静态文件线上 SHA-256 全部匹配。后端及管理端容器健康，学生/教师公开页面加载通过。70 个 Migration 无待执行项；复用原管理端及 Migrator 镜像。备份 `/opt/bnbu-sports-production/backups/before-bugfix-20260912T093448Z.dump`，3,538,137 字节，私有权限、备份目录检查通过。

原失败照片在生产新校验器中只读复验通过（不改申请或上传状态）；新合成 PDF 和带尾部 JPEG 经 COS 上传、ClamAV、签名读取及匿名 403 验证通过。生产验证没有创建或修改学生业务记录。既有失败上传请重新选择材料提交。

部署检查、云端材料检查和公开页面截图均已归档。回退需保留当前规则快照兼容语义及数据库备份，不能自动回退到早期不理解规则快照的后端。

iOS 真机、Mac/Xcode 原生编译、真实相册授权验收仍待具备设备环境。浏览器网页不具备静默写入系统相册的权限；此前已实现原生能力，本轮继续保留。

参考：Sharp constructor failOn、HTMLMediaElement preload、HTML video 官方文档。现有安全大小和时长限制不因兼容修复放宽。

第三方 PDF.js 上游许可证存在原有行尾空格，原文保留；自有改动 `git diff --check` 通过。未纳入原有 25 份文档删除和 0061 Migration 工作区变动。

## 线上 PDF 模块补验

首次线上 PDF 预览发现服务器以 application/octet-stream 返回 .mjs，浏览器拒绝执行。将同源 PDF.js 模块及 worker 改为 .js 文件名，并显式配置 worker 路径；第三方文件内容未改动。新增发布目录保留上一版本，3 个补丁文件 SHA-256 与 JavaScript 响应类型验证通过。公开站点实际 PDF canvas 渲染和 worker 加载通过，无页面脚本异常，截图 cloud-pdf-preview.png。此项是部署后发现并修正的问题，未以本地通过代替线上验收。
