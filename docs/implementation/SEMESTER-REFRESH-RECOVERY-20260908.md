# 刷新后恢复学期未确认请求

2026-09-08。学期写入之前，将原内容、确认版本和幂等键保存在当前标签页 sessionStorage，按登录用户 ID 隔离。恢复不自动写入：页面恢复原编辑/切换弹窗，保持未确认锁定，用户点击原提交按钮时重试同一请求。写入和列表回读成功后删除记录；明确业务拒绝也清除记录，允许修改。

记录不包含令牌或密码，api-client 仅增加读取非秘密用户 ID 的方法。未登录不能持久化或恢复。使用 sessionStorage 保留同一标签页刷新/页面重入，关闭标签页后的恢复不在此实现范围；不同账号读取不同键。

semester-refresh-browser-first-20260908.txt：实际 Edge 及持续 Docker 后端测试退出 0。创建 2035-2036 合成学期成功响应被丢弃，表单锁定；刷新整页、重新进入学期管理后恢复原名称和锁定提示，再次保存使用同一键，只有两次写请求；成功后待确认记录清空。编辑及阻塞切换回归继续通过。semester-refresh-typecheck-first-20260908.txt 为 Portal tsc 退出 0 的空输出。

本包验证创建恢复，切换恢复代码已接入但尚未完成成功切换浏览器专项；多账号切换隔离、损坏存储和浏览器存储不可用还未单独注入测试。布局与 CSS 未改变。没有重新运行全量 Docker 协议套件。完整三端目标仍未完成。
