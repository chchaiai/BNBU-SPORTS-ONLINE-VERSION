# 分类目标再次编辑修复及部署

2026-09-14，用户已确认：责任教师可再次分配分类目标，总量等于全局目标，新目标应用于本课程所有学生的进度和成绩，保留原运动记录及计时规则。
代码提交：101d57b9e724524e7ee0436dbf2a20d0ae47a67a。

## 根因及修改
前端发布后禁用输入框，保存又强制使用原分类目标；后端拒绝全局目标版本未变化时调整分类目标。现已同步移除三处限制。保存按非负整数分钟和总量校验，继续验证责任教师、当前学期、未关闭/归档/结算及乐观版本。复用事务内全班 recomputeCredits 重算投影；记录事实及规则快照不修改。审计增加旧分类目标及 ALL_COURSE_ENROLLMENTS 生效范围，页面补充影响说明。四份业务文档已同步。

## 接口和数据库
沿用 POST /api/v1/class-sections/{id}/v81-rules，输入结构无变化；发布后允许修改 courseTarget/generalTarget。沿用现有规则版本、计入投影和事件表，无新增数据库迁移。关闭、归档和结算保护不放开。

## 验证
后端 typecheck、Linux 生产编译通过；管理端完整 typecheck、生产构建通过。6 项计分领域测试通过。新增真实 PostgreSQL + HTTP E2E 通过，覆盖连续两次重分配、重复提交、版本冲突、非责任教师和管理员越权、总量不符、全局版本过期、关闭课程拒绝。测试将既有自主记录的计入量先变为 0，再变为 30 分钟，确认记录本体和计时快照逐字段相同。
教师页面 ESLint 13 项（8 错误、5 警告），与修改前 HEAD 的规则/消息列表完全一致；未宣称 lint 全绿。构建保留既有大 chunk 等提示。

## 部署与交接
生产版本：/opt/bnbu-sports-production/releases/course-targets-20260914。
回滚版本：/opt/bnbu-sports-production/releases/class-display-final-20260914。
服务器回滚命令：sudo python3 /home/ubuntu/bnbu-course-targets-20260914/deploy.py --rollback。
仅覆盖后端 v81.service 编译模块和管理端构建。部署前镜像/入口基线校验、候选后端导入、隔离门户健康检查通过。上线后前后端容器及公开管理端、学生端、API ready、下载站检查正常。线上课程设置 JS 哈希与构建一致。
证据：evidence/course-targets-20260914/。

未修改生产课程设置进行业务验收；真实写入及重算验证使用隔离测试数据库。生产全班大规模重算耗时未做容量验证，回滚未实际演练。回滚程序只恢复代码，不撤销已授权教师在新版中保存的目标/学时投影。已保留发布前版本。所有代码和交接记录仅本地存档，未推送。
