# 第十轮全量 Docker E2E（2026-09-08）

执行基线08f9ce20，专用bnbu-v81-full-integration、PostgreSQL18.4、57迁移、真实Nest HTTP与严格协议hook。92/92测试、25套件全部通过，0失败/跳过/取消，测试约123秒不含构建。

249个操作全部有错误或访问控制响应；215个启用分类操作同时具成功和错误证据，31个停用操作有拒绝证据，运行时协议符合共246个。1579条有效响应、0非法响应。新增归档、结算后成绩/体测更正和既有新成绩回归均在同一全量执行中通过。

覆盖门禁仍失败、退出1，共4项：createCourse、updateCourse、requestCurrentUserAccountDeletionChallenge缺成功响应，及登记126条与249不一致。前3项需依据管理员只读课程与用户暂缓注销决定补完有身份拒绝证据和停用分类；updateCourse专项不能用教学班修改权限断言替代。123条新操作登记待完善，已定位控制器但不能用通用路径伪造精确证据。

日志docker-backend-e2e-full-tenth-20260908.txt、runtime-conformance-tenth-20260908.json/.md。重复门禁输出分别来自测试层与--write，同一轮而非两轮。第九轮90/90与28项缺口保留为历史。

这是HTTP协议层覆盖，不能证明所有边界、并发与业务规则完备。源材料/日志/身份夹具、内存存储、模拟OCR等范围见专项记录；真实三端浏览器、邮件和对象存储配置、云端架构交接、业务验收矩阵及本地最终交接继续。未操作云端或修改UI。
