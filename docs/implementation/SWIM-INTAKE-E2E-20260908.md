# 游泳材料受理 Docker E2E（2026-09-08）

真实 Nest HTTP、专用 Docker PostgreSQL 与严格响应协议校验，运动记录套件复测 11/11 通过。

学生创建 SWIMMING 草稿，首次状态查询返回 404；教师、管理员受理写入 403；重复媒体和缺少泳后照片分别 422。正常泳前、泳后两份相机材料受理为 ON_TIME，锁定两个材料条目，传输期限为受理后 30 分钟。同 key 重放相同结果，新 key 再次受理 409。状态读取确认尚无上传确认凭据时 completedAt=null、readyForReview=false，即使媒体夹具为 AVAILABLE；剩余时间有效且未超时。

首轮因测试局部登录函数作用域错误而失败（原有 10 项通过）；修复测试后复测全通过。协议补充状态查询既有 404，生成文件同步，249 操作/处理器静态 parity 通过。未修改业务实现及 UI。

日志：docker-swim-intake-e2e-first-20260908.txt、docker-swim-intake-e2e-retest-20260908.txt。会话、相机媒体和学生身份为明确测试夹具；此轮不证明真实上传完成、延迟受理、暂停延时、跨学生归属隔离或游泳审核计时完整验收。整体目标继续，最新全量仍第八轮 87/87、40 项缺口，后续新增成功覆盖尚待全量汇总。
