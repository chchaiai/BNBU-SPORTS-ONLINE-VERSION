# 成绩纠错并发及原事实边界

2026-09-08。产品代码未改，扩展现有 Docker 探测。docker-grade-correction-boundaries-first-20260908.txt（归档状态）和 docker-grade-correction-current-20260908.txt（当前学期已结算）均退出 0。

验证非责任教师/其他组织教师404、管理员/学生403；未有成绩的成员409且没有成绩行；有一版成绩但无结算报告的课程409，失败后仍只有原成绩，无成绩审计。两个不同请求同时纠正同一版本，返回201/409，成绩最新版本只增加一次，更正版报告也仅增加一份。之前的空原因/备注拒绝、报告故障回滚同键恢复、Schema及学生成绩隔离检查继续通过。

原结算报告和可选归档状态使用标注的隔离夹具；无报告课程的原成绩以测试 SQL 准备。测试未对云端或持续浏览器环境写入。两种学期状态中专项纠错均可工作，普通成绩写入仍拒绝。

本包不证明页面纠错、真实学校成绩验收或其他类型事实纠错；三端完整目标仍未完成。没有重复执行无变化的契约检查，测试源码/手写文档检查空白，原始 Docker 日志保留。
