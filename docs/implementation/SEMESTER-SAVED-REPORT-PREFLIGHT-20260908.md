# 学期切换预检查读取正式报告

2026-09-08 14:47。管理员现有 switch-check 接口移除正式结算来源恒定 UNAVAILABLE 的占位实现，读取每个旧学期课程的已保存报告和实际待办。

FORMAL_COURSE_SETTLEMENT 返回尚无报告的课程数量，存在缺项为 BLOCKED；新增 COURSE_UNFINISHED_WORK 汇总尚有待办或未知事实的课程数量。未知事实保留 UNAVAILABLE。整体 ready 和汇总覆盖整个旧学期，课程详情分页不会缩小检查范围。未改变页面布局或增加学期切换写操作。

## 验证

- docker-semester-saved-report-first-20260908.txt：退出 0。使用真实正式结算 HTTP 生成一份报告后，预检查未结算数量等于课程总数减一；即使游标之后为空页，其他课程仍阻塞。正式结算并发、回放、权限、历史读取及导出原有检查继续通过。
- docker-semester-preflight-regression-20260908.txt：退出 0。学期创建/编辑、历史、版本冲突、日期限制、分页、跨组织、教师拒绝、分管理员权限及只读无变更回归通过。
- docker-semester-readiness-unit-first-20260908.txt：首次运行失败，镜像未包含刚新增的测试文件；保留原始证据。
- docker-semester-readiness-unit-retest-20260908.txt：重建镜像后 3 项单测通过，覆盖全部课程清理完成、分页外阻塞/不可用，以及零课程。全部课程就绪的正向分支目前为服务单测证据，不宣称已验证真实学期切换。
- semester-readiness-parity-20260908.txt：245 操作与 handler 对应，生成产物一致。

未新增操作或权限条目，仅更新 Draft 描述及生成摘要。旧契约检查 391 条存量诊断仍待处理，本包没有重新运行该检查。源码和手写文档空白检查通过；原始 Docker 输出保留工具空白。

## 交接

下一步实现幂等、版本核对、组织锁及事务内重查的正式学期切换，并验证归档历史保留、唯一当前学期和并发。预检查当前逐课程读取完整检查，全学期在同一 RepeatableRead 事务内完成，30 秒超时会失败而不会返回部分就绪；大规模学校的性能尚未验证。管理员 COURSE_VIEW 范围的独立结算摘要、其他事实纠错及三端页面接线仍需完成。持续浏览器后端未重建，本包验证使用隔离 Docker。整体目标保持未完成。
