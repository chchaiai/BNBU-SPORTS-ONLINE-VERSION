# 原始体测接线核对及审计结果

2026-09-09。当前 teacher-data.ts 仅有原始体测历史读取，teacher-workspace.tsx 的真实成绩保存分支处理最终 INT 成绩；原始体测提交没有网页入口。后端 testedOn 为必填 YYYY-MM-DD 事实，不能代填录入当天。已向用户提出在原弹窗增加用时和日期的最小必要调整，尚未收到答复，本轮不改相关 UI。

独立后端修复：v81-physical-results 新成功确认及更正事件写入 event_outcome=SUCCEEDED，使用现有教师角色快照。原先未填该列；历史空值不回写。成绩结果已在上一专项处理，本轮只修改原始体测写入。

`physical-audit-docker-20260909.txt`：隔离 Docker PostgreSQL、真实 Nest HTTP 及运行协议校验，两个体测场景及结算套件合计 3/3，exit 0。800/1000 米场景在幂等重放、并发及拒绝测试后均只有两条 SUCCEEDED/TEACHER 事件，原始事实、学生隐私、通知去重断言继续通过；现有结算更正、事务回滚和归档流程回归通过。结算套件没有额外新增原始体测更正结果字段断言，不能把普通确认断言扩大为全部更正边界证明。

`physical-audit-browser-rebuild-20260909.txt` 保存本地持续浏览器后端重建记录。产品网页及布局未修改。最新完整后端回归仍为修复前第二十一轮 97/97，本轮不作新的完整全量结论；其他审计模块与网页录入继续。

原始 Docker 输出中的工具尾随空格按原样存档，源码和 Markdown 单独检查。
