# 2026-09-18 深度评审修复与生产发布

用户要求：依据 `BNBU_SPORTS_REVIEW_c87b0bd.md` 与同名 ZIP 修复后部署。ZIP 内评审正文与单独 MD 一致。评审中的建议作为待核对问题，不作为额外操作授权。

最终状态：应用修复已部署到香港生产，2026-09-18 16:11:56（北京时间）复核通过。R10 的 GitHub 工作流已补入本地仓库，远端执行及分支保护仍待后续推送和配置，不宣称 GitHub 门禁已生效。

## 修复对应关系

| 评审项 | 实际处理 | 验证与边界 |
|---|---|---|
| R01 学时计算与历史写放大 | 保留精确择优语义，加入可行下界及安全上界剪枝；满足条件时使用精确优先顺序搜索。超过 12 条候选使用独立计算线程，最多同时 2 个，预算 1500ms，每个线程限制堆内存；未变化的投影不写入。计算繁忙或超时回滚事务及幂等预约，允许相同提交键重试。 | 1500 组种子对照原算法、既有穷举测试、10/30/60/120 条限额容器测试通过。计算仍在事务生命周期内，预算约束锁持有时间；非常复杂输入可能返回可重试 503，不保证所有输入在预算内算完。 |
| R02 学校共用行锁 | 登录、开始运动、提交、自动结束改用组织共享锁；开始运动对班级取共享锁，保留学生入班关系锁及活动会话唯一约束。 | 本地真实 PostgreSQL/HTTP 验证共享组织和班级锁兼容、重复开始被拒绝。管理员维护仍可通过互斥锁协调。未进行生产并发压测。 |
| R03 视频临时空间 | staging 与生产配置专用磁盘目录；启动及每次转换检查可写和至少 512MiB 可用；容量失败可诊断、可重试；输出限制 200MiB，保留原件。 | 生产候选容器中实际上传并转换 144,604,014 字节合成视频，输出 27,161,112 字节；独立 64MiB tmpfs 容量检查按预期拒绝。生产挂载已核对。 |
| R04 未提交运动恢复 | 增加受资源权限约束、带分页的服务端恢复列表，覆盖已完成未建草稿与 DRAFT；学生端恢复原会话结束时间、材料和草稿。显式放弃持久化到服务端。 | 真实本地 HTTP 与移动浏览器覆盖无本机副本恢复、材料复用、已提交排除和放弃后不再出现。尚未拍摄上传的本机文件无法由服务器恢复。 |
| R05 当前审核筛选 | 按每条记录最新审核版本过滤，并在一致快照中完成筛选和分页查询。 | 本地真实 HTTP 验证历史状态不错误命中当前筛选。 |
| R06 成功后附带失败 | 提交后的重复成绩调用移除，沿用事务内 V8.1 学时更新；本机材料清理及刷新失败不会把已接受提交显示为提交失败。 | 后端测试、学生 smoke、浏览器回归通过；追加两项单测验证临时失败不固定保存为幂等失败。 |
| R07 工作区加载与竞态 | 成绩、通知、体测模块允许独立错误提示；记录和通知按 50 条加载及继续分页；刷新通过请求序号和认证代次保护，异步恢复也检查时效。 | 浏览器注入通知 503 和新旧请求交错，验证主工作区可用及旧响应不覆盖新状态。进度接口不支持时仍完整读取记录以维持旧端统计正确性。 |
| R08 后台任务隔离 | 自动结束逐项捕获、日志及短期冷却，使用 SKIP LOCKED；媒体轮询错误记录日志，完成/失败写入校验尝试号、实例标识和租约，规范化结果按尝试独立命名；关闭时等待当前处理。 | 毒任务隔离、旧实例和过期租约测试通过；复核期间生产 worker 错误标记为 0。 |
| R09 AI 规则权威 | 四份业务权威文档与索引明确现行 DEFAULT_VALID_ADVISORY_20260918：普通合规打卡默认有效，AI 学生 20% × 记录 20% 仅建议，历史和强制人工分支保留教师审核。旧冲突描述标为历史规则。 | 依据仓库已有 2026-09-18 决定与当前生产策略，未重新改变审核决策或批量改写历史。 |
| R10 发布门禁与来源 | 增加根目录 GitHub workflow，包含构建、类型、单测、代表性 PostgreSQL 集成/安全测试和两端检查；发布包记录源码 HEAD、变更文件 SHA-256、运行文件和资源摘要。 | 本次本地与候选检查实际通过；GitHub workflow 尚未推送运行，分支保护未配置。本次发布来源为固定 HEAD 加摘要记录的工作区修改，不能称为远端已验收提交。 |

另修复验证中发现的历史补录可选最大时长对 null 的校验不一致，以及媒体 E2E 测试替身缺少复制方法。学生端既有零目标确认改动与发布前线上版本核对一致并保留。

## 已执行验证

- 后端全量单元测试：335/335；随后补丁增加两项针对性测试，2/2 通过。没有将两次运行表述为一次 337 项全量运行。
- 后端类型检查、构建、接口契约核对通过；最后补丁重新类型检查和构建通过。
- 学生端 smoke：87/87；Web 检查通过。
- 本地 PostgreSQL + HTTP：38 项通过，含权限隔离、当前审核筛选、恢复、投影稳定与锁兼容。
- 本地移动浏览器：恢复会话和已上传凭证、真实服务端结束时间、服务端放弃、模块故障隔离和竞态保护通过，无浏览器错误。
- 最终生产：16 个后端运行文件、4 个学生端资源摘要核对；两域健康接口通过，未认证恢复访问返回 401。Backend/Portal 均 healthy，重启计数 0；自最终启动至复核时错误日志与 worker 失败标记均 0。

候选环境为 Node 24.18.0、1 CPU、768MiB，使用合成数据隔离测试：

| 记录数 | 计算含线程启动耗时 ms | 0ms 定时器延迟 ms |
|---:|---:|---:|
| 10 | 24.04 | 23.90 |
| 30 | 46.61 | 2.44 |
| 60 | 41.40 | 0.91 |
| 120 | 59.04 | 0.50 |

这些是固定构造输入的单轮测量，不是生产学生提交时延、容量上限或真实账号全流程验收。本次未对生产发送真实学生打卡、审核或批量历史重算请求。

## 发布与回退

- 最终 release：`/opt/bnbu-sports-production/releases/deep-review-retry-20260918`
- 最终 backend image：`sha256:887bb62de6b696963fc64d7d9c2f333731c2acbe7f6ef75d2c9f75ee66619d6f`
- 源码 HEAD：`c154343aad8e084f2ed04ab8a342623413562ea3`
- 最终源码清单 SHA-256：`643b617b1e5936d2f2c71faac78be2f0d54e8e4c23c83c8da829993acecf93f0`
- 主修复包 SHA-256：`17ca362606b1d896bb0c5ba809aee80ad4232cf97b5989ce6300368e420c213e`
- 最终重试补丁包 SHA-256：`0e4b351a8407a5cfbd25ccb49c50e1078a168000791955411b761788220e817f`
- 发布前备份：`/opt/bnbu-sports-production/backups/before-bugfix-20260918T081050Z.dump`，41,480,630 字节，备份目录校验通过。
- 补丁前 release：`deep-review-20260918`；整轮修复前 release：`zero-target-20260918`。两者及镜像保留。回退应先核对当前指向，再按对应 release 的 compose 启动后端并原子切换 current，重新检查健康与资源摘要。
- 本次无数据库迁移、历史批量改写、Git 提交或推送。Portal 镜像维持核对通过的既有镜像。

补丁首次候选构建因 Docker 将裸 image ID 当作远端镜像名称而失败，未触达发布切换。改为本地固定镜像打标签后构建并验证成功；失败包保留供追溯。

## 证据文件

位于 `evidence/deep-review-20260918/`：`backend-unit.txt`、`submit-retry-test.txt`、`student-smoke.txt`、`web-test.txt`、`http.json`、`browser.json`、`candidate.json`、`deployment.json`、`deployment-final.json`、`runtime-verification-final.json`、移动浏览器截图，以及固定发布清单。测试账户及令牌仅在本地忽略目录 `.local`，不属于发布证据。

部署脚本和封包脚本位于 `tools/production/`；HTTP/浏览器回归脚本位于 `tools/local-integration/`。后续复用应重新验证基线和路径，不能直接对已存在 release 重跑本次一次性脚本。
