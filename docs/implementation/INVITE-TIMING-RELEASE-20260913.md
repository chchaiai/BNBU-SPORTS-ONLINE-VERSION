# 邀请期限修复待发布记录

状态：本地验证通过，尚未部署。现网仍为 a46fbe41。

## 修复行为

- 默认邀请有效 30 分钟，教师可提交 5–120 的整数分钟，由服务端计算期限。旧 expiresAt 字段继续接受范围内的绝对时间；两种期限字段不能同时填写。
- 已在自然到期前登记的入班流程，使用固定“邀请到期 + 10 分钟”截止时间。重复请求不延长截止时间；自然到期后新幂等键不能登记新流程。
- 原幂等键可在宽限内取回同一凭证，解决登记响应丢失；学生端重试消费同一凭证和同一消费键。撤销、关闭入班、课程关闭或学期归档仍立即阻止未完成入班。
- 旧 COURSE_INVITE_TTL_SECONDS、JOIN_CAPABILITY_TTL_SECONDS 环境字段保留兼容配置校验，但当前邀请业务期限以 V8.1 固定规则为准；不再用旧七天配置创建新邀请。
- 教师页面保持现有布局，改用 expiresInMinutes；学生网页与 Android 共用网页模块的重试逻辑得到修复。

## 验证

详见 evidence/ocr-triplatform-20260913/invite-local-validation.json：后端单元 281/281、契约 35/35、真实 PostgreSQL HTTP 12/12、学生模块 16/16。后端/教师端类型检查、教师端构建和修改范围 ESLint 通过。

首次 HTTP 样本遗漏 closedAt/closedBy/closeReason，第二次遗漏关闭时 isEnrollmentOpen=false；均由数据库约束拒绝，完整日志保留。补齐合法样本后关闭边界通过。学生旧测试仍拒绝 PDF，与业务文档后续修订冲突，已更新为允许申请 PDF、继续禁止运动打卡 PDF。

线上只读审计未发现仍生效且时长超过 120 分钟的旧邀请；未改写生产邀请或凭证事实。临时发现问题的邀请已撤销。

## 发布与回滚

候选发布需同时更新 backend、portal 镜像和 student/js/api.js。无需数据库迁移、COS 权限、长期密钥或 Nginx 修改。先记录 current 仍指向 /opt/bnbu-sports-production/releases/ocr-full-20260913，并保留该发布及镜像作为回滚点。部署后验证默认期限、5/120 边界、越界拒绝、学生/教师页面和真实自然到期宽限。

发生失败时恢复原发布链接、原 backend/portal 镜像和学生静态文件，核对健康状态及三站入口。新建凭证仍保留事实；旧代码恢复后会恢复更严格的邀请到期拒绝，不改写凭证或入班记录。

用户已明确批准 B，补丁现已基于最新生产版本发布。最新发布链与真实自然到期验证见 OCR-APPROVED-RELEASE-20260913.md；上面的旧候选回滚基线仅保留为历史记录。

## 限制

- 客户端重试上下文仅在页面内存中；刷新或关闭页面会丢失。此行为不会刷新服务端宽限，但尚不构成跨重启恢复验收。
- 已发出的旧凭证继续使用原存储期限，不自动延长。旧缓存教师页面应重新加载后使用相对分钟参数。
- 本地时限边界使用独立可重置数据库中的回溯样本；尚未证明生产自然等待、浏览器和原生端完整链路。
