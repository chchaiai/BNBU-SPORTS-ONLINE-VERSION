# 结算后体测更正统一 E2E（2026-09-08）

在真实结算、报告及学期切换后、最终成绩更正之前接入体测更正探针，原始用时271秒改为272秒并追加报告。空原因、性别项目不符422，管理员403、其他教师404；无原体测或无报告409，均无体测/通知/审计副作用。

注入新版报告写入失败，500后原始体测历史、通知数量及审计不变；移除触发器，同键重试成功。重放不重复，旧版本409，新通知仅1条，报告v1不变、v2记录新用时。并发273/274秒更正仅201/409，新报告v3与唯一成功结果一致，历史保存全部版本，通知总计增加2条。普通体测写入仍409。

通过实际SMTP发送验证码到Mailpit，再走学生验证码验证HTTP登录；学生体测和结算结果显示最新版原始用时，不含finalGrade/correctionReason/physicalVersion/actorId，跨学生404，学生更正403。读取邮箱使用node:http访问测试服务，全部业务HTTP继续严格OpenAPI hook校验；排除挑战前旧邮件，避免复测误取历史验证码。

首轮未声明越权404；补充correctV81PhysicalResult的404和故障500并生成协议，parity249通过。复测一错误地把Mailpit管理API当成业务协议校验；复用已存在的独立邮箱读取方式后复测二1/1通过，同时包括结算/切换和最终成绩更正回归。保留docker-physical-correction-e2e-first-20260908.txt、docker-physical-correction-e2e-retest-20260908.txt、docker-physical-correction-e2e-retest2-20260908.txt。

探针只允许明确隔离数据库，合成学生和历史规则/源名单仍为夹具；不是生产邮箱或腾讯云验收。未改UI或业务实现。整体目标继续。
