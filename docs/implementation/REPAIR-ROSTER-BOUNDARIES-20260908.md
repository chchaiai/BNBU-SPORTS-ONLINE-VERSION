# 名单行数与关闭受理修复

依据教师业务第 170 行：单批最多 500 个人员行、表头不计、重复或错误行计入；课程关闭后新增来源不受理，已合法受理对象仍可后续处理。

修复服务端 CSV 与 Portal CSV/XLSX 解析器的旧 10000 行限制为 500。仅调整解析常量，未改页面结构、样式或布局。新名单受理在事务内锁定课程行、核验责任教师及关闭状态，关闭则 409；已受理阶段恢复及成功幂等重放保持原路径。

Docker 复测：

- docker-roster-csv-row-limit-fix-20260908.txt：500 行返回 201；501 行返回 422，当前名单仍是原 500 行，退出 0。
- docker-portal-roster-row-limit-fix-20260908.txt：CSV/XLSX 两项 500/501 边界测试均通过，退出 0。
- docker-roster-closed-intake-fix-20260908.txt：关闭后新上传 409，导入数量保持 1，退出 0。
- docker-roster-closed-replay-fix-20260908.txt：补充关闭后原成功请求同键重放返回 201 且数据一致；新上传仍 409、导入数量不变，退出 0。
- parity-roster-repair-20260908.txt：232 操作/处理器检查通过，无静态例外。

上述后端专项构建及 52 项迁移检查通过，使用合成课程和真实 HTTP/MinIO。关闭状态由合成资料直接设置；尚未单独验证关闭与上传并发、阶段恢复、100MB 大文件、浏览器点按或完整名单对账。事务行锁用于防止新受理与关闭交错，不能以代码存在替代并发运行证据。

旧失败日志保留，上述对应问题由新复测覆盖；重复身份处理、Portal 完整上传依赖及其余问题仍另行处理。持续浏览器后端将按当前构建刷新，数据卷和合成账号保留。

持续后端已刷新，本机3199就绪200，见browser-roster-ready-20260908.json。

存档检查：明确暂存16个文件；git diff --cached --check退出2，仅5个原始Docker日志尾随空格，保留输出并记录未通过。
