# 结算后普通写入限制

日期：2026-09-08 13:39。完整三端目标仍进行中。

新增 requireUnsettledCourse：在已确认权限、持有组织写锁的事务内查询真实结算报告。已结算返回 CONFLICT_STATE_TRANSITION / SETTLED_FACT_CORRECTION_REQUIRED；未结算不改变原流程。既有幂等结果回放仍先由幂等服务处理。

本次接入运动开始、申请创建/编辑/提交/审核、体测追加、最终成绩追加、认可撤销、补练创建、课程规则保存、游泳首次受理、电子名单确认、打卡审核。当前审核纠错入口也会拒绝已结算课程，待追加报告联动实现后再受控放行；未结算课程纠错保持原行为。

## 验证

- docker-settlement-write-guards-first-20260908.txt：Docker 构建、57 项迁移、未结算原业务 HTTP 回归通过，退出 0。
- docker-settlement-write-guards-specialized-20260908.txt：内部结算事务专项及新增防护测试通过，退出 0。真实 PostgreSQL 事务内生成合成报告后，调用真实体测/成绩写服务验证拒绝原因，数据库确认体测仍为原版本、成绩没有新增；其他课程守卫通过。全部临时事实回滚。

成绩专项的幂等外层使用事务适配器，未模拟 HTTP 丢响应或证明结算后整条 HTTP 链；两类边界不能混同。首次运行未出现产品或脚本失败。原始日志保留工具空白，代码和手写文档空白检查通过。

## 尚未覆盖

入班/成员变更、邀请、OCR/名单来源切换及其他写入路径还需继续审计和补齐。旧事实纠错尚未同事务追加更正版报告。正式结算服务仍未开放 HTTP，现有结算检查 ready 仍 false。后续需协议、幂等端点、报告读写/导出、三端接线和学期切换完整验证。

没有 UI 布局、协议操作、数据库迁移或云环境变更。持续浏览器后端未在本包重建，本包运行证据来自隔离的本地 Docker 测试服务。
