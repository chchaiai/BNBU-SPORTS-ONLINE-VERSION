# 活动认证认可与撤销 E2E（2026-09-08）

运动记录统一套件新增校队认证链路。专用Docker PostgreSQL、真实Nest HTTP及严格协议校验，首轮套件10/10通过。

学生以已验证材料创建EXERCISE_CHECK_IN/SCHOOL_TEAM申请并提交，教师APPROVE分配课程120分钟、通用0分钟；学生课程recognizedSeconds=7200，认证列表显示APPROVED，认可历史保留7200秒。学生自行撤销403；教师附理由撤销成功、原key重放相同、新key旧版本409；学生认可进度回到0，历史依次为撤销inactive和原认可active，数据库认可投影active=false。原人工运动/补证/审核更正场景同时通过。

日志：docker-certification-e2e-first-20260908.txt。身份、已完成运动及认证可用文件为明确数据库夹具；申请、提交、审核、进度、列表、认可历史和撤销均走HTTP。认证材料允许文件选择，不改变用户对运动打卡只允许相机的决定。未改UI。

本轮不代表真实材料上传、其他教师/学生跨主体完整隔离、认可分钟重新分配、分类上限或结算后认证更正均已验收；这些与云服务及三端浏览器继续审计。全量门禁最新仍第八轮87/87、40项缺口。
