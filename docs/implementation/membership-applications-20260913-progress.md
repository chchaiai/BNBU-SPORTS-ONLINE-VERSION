# 成员范围与免测申请修复（2026-09-13，历史进度）

以下为确认前的历史记录。用户后续已确认，修复及线上验收已完成，当前结论见 [最终交接](membership-applications-20260913-final.md)。

已实现并本地验证：
- 教师打卡列表后端只返回本人未删除课程且仍有 ACTIVE 成员关系的记录；课程仍开放时单独移出学生，列表不再返回该生记录，数据库历史仍保留。真实 Docker HTTP/数据库证据 `.local/membership-active-course-retest.log`。
- 申请提交重新读取真实选课状态，不把当前学期展示标记作为在读判定；旧班级草稿不再复用于新班级，已有材料校验所属选课。上传重试签名加入选课 ID，变更目标时更新幂等请求。
- 新拍、本地恢复及远端草稿图片加载真实缩略图，复用现有材料大图预览。
- 真实浏览器验证校队申请提交、缩略图和大图通过：`.local/membership-application-browser-retest.log`。测试使用真实 ACTIVE 成员，并模拟旧学期展示标记；未放宽服务端有效成员校验。
- 学生 86 项 smoke 及后端类型检查通过。首测等待关闭预览失败是测试用了未实现的 Escape 操作，改为界面“关闭”后通过。后台初测仍在运行旧容器源码，复制修复源文件并重新编译启动后真实回归通过。

待确认：移出班级后的“清除申请记录”是否采用：师生业务页面不再展示原班申请，终止未完成申请，撤销当前免测/免打卡资格，但保留审计和已结算成绩。现行 docs/business/20-teacher-flow.md 第 7.2 节要求历史保留，此项涉及已批准资格、历史成绩与附件，未擅自物理删除。

确认后继续完成申请生命周期、存量兼容和回归，再统一部署腾讯云并线上验收。原有 bug.md、0061 行尾及旧报告删除未纳入本轮存档。
