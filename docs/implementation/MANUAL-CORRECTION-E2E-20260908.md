# 未结算记录更正与学生进度回退（2026-09-08）

依据 docs/business/20-teacher-flow.md 第 9 节：教师判断有效性，不能改运动日期、时长和类别；历史只追加，重复请求不重复计入。继续扩展人工补证测试，在通过计入 60 分钟之后，通过正式 corrections 接口更正为 INVALID。

Docker 严格 HTTP E2E 运动记录套件 9/9 通过，首轮退出 0。验证空白更正理由 422、学生自行更正 403，两次拒绝后进度仍为 3,600 秒；教师以固定公开原因及内部更正理由成功更正，原请求幂等重放，另一个旧版本请求 409；学生进度回到零，记录计入时长为零但实际时长仍为 3,600 秒；学生记录详情不暴露内部更正理由。数据库保留初始待审、教师通过、更正无效三个 review_records，FACT_CORRECTED 事件恰好一次。

日志：docker-manual-correction-e2e-first-20260908.txt。原人工补证与材料版本验证仍一并通过。此场景是未结算课程的旧事实更正；不能代替正式结算后更正版报告、并发更正、其他受日/周限制记录重新计入的专项。时长和可用相机材料仍是明确标注的数据库夹具，未改变 UI 或产品实现。

全量门禁最新仍第六轮 88 项缺口；继续补齐其他业务证据与三端浏览器检查。
