# 帮助管理刷新恢复（2026-09-08）

帮助写入前按当前登录用户 ID 将原请求体、版本、幂等键和原编辑/确认框状态存入 sessionStorage；刷新或组件重新挂载后读取并恢复，不自动提交。重试仍使用原键和原内容，成功且列表回读完成后删除记录。存储失败时阻止发送；没有存储有效记录不能直接执行重试。存储内容不含令牌或密码。

未确认期间锁定输入、取消及关闭；编辑框中与原请求目标状态不同的按钮禁用，避免用户点击“发布”却重试原“草稿”操作。页面布局与 CSS 未变。

验证均连接持续本地 Docker 后端：

- `help-refresh-browser-first-20260908.txt`：创建草稿丢响应→刷新→恢复原内容及禁用状态→同键成功→存储清理→发布/下线/重新上线/编辑通过，退出 0。
- `help-transition-refresh-browser-20260908.txt`：包含上述创建恢复，并增加发布确认丢响应→刷新→恢复确认框→同键重试→清理，通过，退出 0。
- `help-refresh-typecheck-final-20260908.txt`：Portal tsc 退出 0。
- `help-transition-refresh-20260908.png` 已检查；另保留创建恢复截图 `help-refresh-recovery-20260908.png`。

剩余：同标签页多账号、损坏或不可用的 sessionStorage 专项、多人编辑冲突浏览器和移动视口仍需验收。关闭标签页不承诺保留 sessionStorage；内容安全和三端全量验收继续。未运行云端操作。
