# 当前用户决定（2026-09-09）

- 已授权并完成教师课程设置弹窗四项参数最小调整：两类目标、运动门槛、每周次数；见 COURSE-PARAMETERS-WEB-20260909.md。

- 已批准并完成总管理员三个必要表单：统一模板发布、按课程人工审核、OCR配置/启停。复用现有页面内容区与样式；验证见 ADMIN-REVIEW-SERVICES-WEB-20260909.md。
- 学生游泳网页的泳前/泳后标记和延迟说明、学生本人结算网页显示明确暂缓。保留后端能力；不再将这两项列为本轮网页交付阻断。
- 下列早期排查保留历史，其他已获准界面调整及验证以当前验收矩阵为准。

# 现有界面与接口接入缺口

用户要求保持现有UI/UX布局。本文件记录只读核对到的输入缺口，不代表允许改布局，也不将服务方法存在视为页面可操作。

| 业务 | 现有位置与事实 | 当前可交付的接口能力 | 尚需解决 |
|---|---|---|---|
| 游泳前后材料 | student/js/screens/checkin.js中的draft只有type、文件名、大小、duration、blob；没有BEFORE/AFTER输入 | swim-intake接受明确阶段并冻结批次 | 不得按文件顺序或本机时间猜阶段；页面阶段来源待明确 |
| 游泳离线说明 | 原checkin提交只有运动说明，未见独立延迟说明输入 | intake.delayReason与submit.swimDelayReason | 不得把运动说明自动当作离线原因 |
| 补证包满额替换 | 提交事件目前保留全部原包，再追加现有本地草稿 | 后端每版独立校验6图1视频，泳包保留原前后证据 | 原包已满时需要明确当前送审材料选择；不能自动删历史证据 |
| 教师INT最终成绩 | teacher-workspace.tsx的saveGrade真实分支调用旧recalculateStudentScore；现有form围绕耐力跑分钟/秒 | final-grades追加整数、发布内部版本、查询历史 | 不能把运动秒数或换算分推定为教师手填最终成绩；现有输入与新业务不匹配 |
| 总管理员人工模式 | Portal未找到人工审核范围/理由开关输入 | manual-mode按课程读写、SUPER权限、版本和事件 | 尚无现有页面入口可直接绑定 |

部分页面仍显示旧“1.2.0只展示、不写入”提示，而本轮已连接对应事件。没有修改HTML/JSX模板；这些提示与当前行为的差异需在最终交接中明确处理，不能称页面文案与功能已全部一致。

### 体测导入入口核查及修订历史（2026-09-07）
核对 teacher-workspace.tsx 与 roster-reconciliation.tsx：当前只有官方名单导入，体测只有单条用时编辑且没有测试日期；不存在可直接复用的体测成绩单导入入口。本轮按接口/后端范围继续，未新增界面或改变名单导入语义。原日期问题仍保留。
新增 GET /physical-imports/{importId}/revisions?rowNumber=...：责任教师范围、逐行版本降序、limit/beforeVersion 分页、原始内容和创建时间；不返回学生跨课程信息。177 操作 parity 通过，正在 docker-physical-import-history-20260907.txt 运行 HTTP 分页、原文历史和越权验证。
