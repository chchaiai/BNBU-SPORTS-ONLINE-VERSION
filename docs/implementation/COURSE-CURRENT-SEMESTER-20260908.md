# 当前学期建课约束

2026-09-08。依据 docs/business/20-teacher-flow.md 的当前学期建课要求，createClassSection 现在拒绝 UPCOMING 学期，保留 ARCHIVED 拒绝，新课程固定为 ACTIVE。建课在现有 Serializable 幂等事务中先取得组织锁，与学期切换按相同顺序互斥；已有幂等成功结果仍按原机制回放。

docker-course-current-semester-first-20260908.txt 退出 0：目标未切换时建课 409 且没有课程新增，切换后建课 201/ACTIVE，同键回放相同结果且数据库只有一条，归档学期建课 409。原学期权限/预检查、审计故障回滚、重试、并发和历史保留检查同时通过。该测试仍使用空旧学期，不代表有课程的完整结算切换。

course-current-semester-parity-20260908.txt 确认 246 操作对应 handler、生成产物一致；Draft 仅补充现行约束描述。未重新运行旧契约检查，原有 391 条问题继续保留。

course-upcoming-browser-inventory-20260908.txt 为本地持续浏览器数据库只读盘点：UPCOMING 课程数量为零，没有执行历史课程转换。真实云端既有数据未核查；如存在此类旧数据，需明确来源后处理，不能自动视为已发布课程。

没有改变前端 UI/UX。下一步继续含课程结算后切换、新学期规则发布/入班/运动及现有页面接线。持续浏览器后端尚未重建，完整三端目标未完成。原始工具日志保留；源码及手写文档单独检查空白。
