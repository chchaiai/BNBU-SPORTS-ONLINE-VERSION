# 教师批量建号与首次改密 E2E（2026-09-08）

新增 v81-teacher-imports.e2e.test.ts，在专用 Docker PostgreSQL 与真实 Nest HTTP 中运行严格协议验证，首轮1/1通过。自定义邮箱域名按用户明确决定处理；教师删除仍暂缓，本专项不执行删除。

验证：两种自定义域名CSV预览成功且不建号、不含密码；重叠批次也先取得合法预览；教师无权预览/确认；篡改CSV与预览凭证不匹配422、弱统一初始密码422；管理员确认创建两个账号，原请求重放一致、新key重复确认409；重叠批次409且NEW003没有部分创建；列表能查到新教师、教师无权读管理列表；密码持久化为Argon2id散列，响应和审计不包含密码；实际临时密码登录后必须先改密，业务接口403；本人改密后重新登录并读班级接口200，旧临时密码401。导入事件恰好一条，count为2。

日志：docker-teacher-imports-e2e-first-20260908.txt。基础管理员和组织为合成测试夹具，新教师创建、登录和改密走HTTP；不代替SMTP邮件或浏览器交互验收，未改UI布局。

完整全接口门禁最新仍第七轮84/84、70项覆盖失败。名单、OCR、认证、结算后更正、日志归档及旧清单、三端浏览器继续。
