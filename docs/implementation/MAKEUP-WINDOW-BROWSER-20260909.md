# 补练授权网页闭环

2026-09-09。用户已批准指定学生起止窗口的最小调整。在学生姓名打开的原详情快捷区增加“授权补练”，复用原业务弹窗，真实模式显示北京时间起止输入和本人授权记录。保留原页面布局，不再走演示补录分钟逻辑。授权严格提交 enrollmentId、当前已发布规则版本和带时区时间；后端裁定窗口与课程状态。支持原授权记录回读、填写原因撤销，服务端分页完整读取后只展示选定学生的授权。

创建/撤销意图按用户与入班关系保存在 sessionStorage；未知结果重放原键和原请求，刷新后仍可恢复。加载和提交时禁用确认按钮。保留历史，不增加运动学时；学生必须实际运动并提交材料。

实际浏览器连接现有 Docker 后端及 PostgreSQL，makeup-window-browser-retest3-20260909.txt 通过：非法历史日期 422 与可见错误、合法将来补练窗口创建、服务端 201 后丢弃响应、刷新后同键返回同授权 ID、填写原因撤销该 ID、再次刷新读取撤销状态。makeup-window-typecheck-final-20260909.txt 类型检查退出 0。当前合成课程补练期在 2027-01-23 至 2027-01-30，本次是将来授权，不声称已在该将来窗口实际运动；后端已有运动授权链测试独立保留。

失败保留：初次脚本将 fetch Response.status 当函数调用，修正后重跑；第一次 15 秒名单加载等待不足，延长至 45 秒。retest2 已创建合成授权但刷新时按钮在加载期间仍可点，操作被忽略；修复 busy 状态同步后 retest3 通过。此前失败尝试生成的将来授权保留，没有撤销身份不明或其他用途的授权；retest3 只撤销自己精确 ID。日志中未发布课程 progress-target 404 为既有未配置目标状态。

尚待：跨身份竞争、浏览器存储损坏/满额及既有未确认授权的人工清理决定；本地只含合成账号，不影响生产。当前未改后端，没有重跑或替代第 23 次后端全量结论。原始体测、学生本人名单展示与两端通知继续实施。
