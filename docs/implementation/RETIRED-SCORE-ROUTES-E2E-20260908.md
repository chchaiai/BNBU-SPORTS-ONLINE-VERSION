# 旧成绩入口拒绝矩阵（2026-09-08）

接续29a76eaa对当前业务停用旧成绩流程的实现。逐个对14个旧规则、成绩、调整端点发送已登录教师与管理员请求，共28次403拒绝断言；请求后规则、学生成绩和调整表均为零。使用有效格式但不存在的资源ID验证入口在访问领域数据前拒绝，不将此项当作历史数据保留的专项验收。

专用Docker PostgreSQL、真实Nest HTTP和严格协议hook，score套件4/4通过，包含新内部最终成绩版本、发布、重放、并发及学生隐私回归。日志docker-retired-score-routes-e2e-first-20260908.txt。Draft协议为14个入口加deprecated及现行业务停用原因，生成产物同步，249操作静态parity通过。

后续仍需清理旧成功响应声明与运行覆盖清单，不能仅靠deprecated字段判定门禁完成。未改UI或删除业务数据。最新全量仍第九轮90/90、28项覆盖缺口。
