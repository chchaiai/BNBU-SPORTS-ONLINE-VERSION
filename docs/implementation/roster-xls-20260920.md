# 教师名单 XLS 支持与上传修复（2026-09-20）

已发布香港生产 `roster-xls-20260920`，上一版本 `checkin-media-20260919` 保留用于应用回退。

## 修复内容

- 教师端选择器、格式提示、真实上传适配器、multipart 签名校验、隔离解析线程、名单确认链路支持原始 BIFF/OLE `.xls`；原件与摘要保留。
- 修复普通文件名含句点（例如 `课程.2026.09.xlsx`）时，前端可预览而上传端拒绝的问题；新增 0087 迁移同步数据库约束，保留历史记录。
- 文件级错误不再要求用户修正并不存在的标记字段，给出文件格式/损坏/密码保护提示及诊断编号。
- 保持单文件 100 MiB、500 人员行、签名/MIME 校验、私有存储、工作线程内存及超时限制。

截图诊断编号 `74fff50b-bd8a-40a1-9b4e-62376efcca56` 的生产记录确认 createRosterImport 返回 422 / ROSTER_FILE_INVALID。旧日志未保存 category，且未获得原始名单，故不能确认截图这一次请求的具体触发项；已复现并修复句点文件名的同类失败。

## 验证

- 后端单元测试 339/339。
- 前端名单专项 12/12：CSV/XLSX/XLS 原字节上传，确认响应丢失时复用来源与幂等键，行数边界。
- 本地真实 HTTP + PostgreSQL 4/4：三格式带句点中文文件名上传、VALIDATED、正式教师确认、原字节/中文姓名/前导零读回，以及原有权限、关闭课程、对齐回归。此层使用内存私有对象存储。
- 前后端类型检查、构建、本次修改文件 lint 通过；全量 Portal lint 仍有既有 49 errors / 36 warnings，不计为通过。
- 数据库 87 项迁移静态检查、13 项迁移安全测试通过；本地实际执行 0087 成功。
- 基于生产镜像增量候选验证三格式 multipart + 隔离解析，通过。候选检查未写入生产名单。

## 生产发布

- 仅覆盖名单相关后端模块和教师端构建产物，沿用生产镜像其余内容；生产迁移镜像只新增 0087。生产基线尚无本地 0084，未将其夹带发布。
- 数据库备份通过 pg_restore 目录验证；0087 执行成功。
- 首次上线因检查脚本误请求内部构建清单而 404，自动回退，旧镜像健康已核实。修正检查为公开 assets 后重发成功。
- 最终 backend / portal healthy，6 个后端文件摘要与所有公开教师端 assets 校验通过；独立公网 HTTPS readiness 返回 UP。
- 生产未新建合成名单，未进行真实教师账户浏览器导入；教师原始文件尚未复测。
- 0087 为扩展性约束，应用回退保留该迁移，不删除已导入数据。

证据目录：`evidence/roster-xls-20260920/`。发布脚本：`tools/production/deploy-roster-xls-20260920.py`。
