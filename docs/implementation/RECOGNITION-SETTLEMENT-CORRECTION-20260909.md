# 结算与归档后的认可分钟更正

2026-09-09。本轮依据教师业务文档 §10.3、§12.3，扩展现有 `adjustV81Recognition`。课程未结算时继续追加普通调整；已结算时，认可更新、审核历史、通知、审计、更正事件及新报告在同一串行化事务内提交。报告关联原申请、更正版本、原因、操作者事件及上一份报告。

已归档课程也可更正，但原批准事件必须早于首次结算。无首次报告的归档课程拒绝调整。原申请必须仍为 APPROVED、有有效认可、符合原教师范围和版本要求；各类上限与合计 1200 分钟限制继续生效。撤销后恢复、结算后撤销尚未由本轮开放。数据库使用已有追加历史表，无新增迁移；候选协议说明已同步，仍为 253 操作、59 迁移。

## 本地验证

- 第 31 次隔离 Docker：97/97，25 suites，0 fail/skip，121884.079775 ms。
- 运行协议：219/219 启用成功，253/253 错误与权限响应，34 默认拒绝。
- 扩展结算 E2E：合成学生通过真实 HTTP 提交和批准认证（30/15 分钟），课程结算及学期归档后，责任教师更正为 15/20 分钟。学生请求拒绝 403、他人教师拒绝 404、过期版本拒绝 409、同键返回完全相同结果；只追加一份报告，旧报告完整内容及 SHA 保持一致，学生读取最新报告版本。
- 后端构建通过。本地全量 `typecheck` 未通过：78 条测试文件诊断，包括缺少 `.mjs` 声明、可能 undefined 和测试输入类型问题。完整输出保留；未将它写为通过，也未在本轮逐项处理。输出没有 `src/` 错误。
- Docker 预览后端已重建。前端文件本轮未修改；本轮未进行结算后认可更正的浏览器点击验证，因此 HTTP 验证不代表这一网页流程已验收。

证据：`docker-backend-e2e-thirtyfirst-20260909.txt`、`runtime-conformance-thirtyfirst-20260909.json/.md`、`recognition-settlement-typecheck-20260909.txt`、`recognition-settlement-browser-build-20260909.txt`。测试使用合成身份及材料元数据，不代表真实学校材料或云端验收。开发脚本首次因 Windows Python 默认 GBK 读取 UTF-8 失败，尚未写入文件；随后改用明确 UTF-8 与补丁完成。

## 继续交接

下一步补充本业务浏览器操作、报告下载核对、故意报告失败时的认可全事务回滚，以及结算后撤销路径。继续处理申请补材料、名单/OCR、权限写入组合及全量类型检查问题。全范围三端目标保持进行中，外部 COS/CVM/PostgreSQL、正式邮件和模型验证依赖沿用总交接记录。
