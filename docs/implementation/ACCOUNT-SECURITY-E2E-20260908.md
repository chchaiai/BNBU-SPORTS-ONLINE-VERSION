# 账号安全统一 E2E（2026-09-08）

在系统管理 E2E 套件中新增两个账号安全场景，使用真实 HTTP 与 Docker PostgreSQL，严格运行时协议校验：

- 教师、管理员被设置为首次改密状态后，安全状态可读但 /me 被拒绝；确认密码不一致 422，错误旧密码 401。正确改密后当前会话仍有效，其他会话立即 401；旧密码不能登录，新密码可登录。同一幂等键重放返回同一结果，数据库只有一条改密事件。
- 学生不能读安全状态或调用密码修改接口；维护期间教师不能读安全状态或修改密码，管理员仍可读取并成功改密。

首轮系统管理三项中 2/3 通过；失败是维护时账号安全接口返回正确的 503，但协议未声明。补充 /auth/account-security 与 /auth/own-password 的 503 响应，未修改账号安全实现或 UI。

证据：docker-account-security-e2e-first-20260908.txt、docker-account-security-e2e-retest-20260908.txt。协议生成及 249 操作/249 handler 静态对应检查通过。完整门禁最新仍为第四轮 69/69 用例通过、126 项覆盖缺口，不能把专项通过当成三端全部验收。

复测结果：系统管理全部 3/3 通过，含新增两个账号安全场景，退出码 0。
