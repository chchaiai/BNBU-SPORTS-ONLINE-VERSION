# 2026-09-18 打卡与审核修复部署

## 交付
- 普通提交继续默认有效；AI 改为学生 20% × 记录 20% 的稳定随机抽查，只保存建议，不改变状态、审核记录或学时。旧策略待执行任务退出队列。
- 教师“历史补卡”新增单次最多分钟数（1–1440，至少达到课程最低时长）。新历史会话冻结此配置，规则快照沿用该上限，修复缺省 3600 秒导致的截断。历史补录仍须教师审核。
- 手机补录表单增加审核说明、上限说明、清晰字段与触控输入。
- 教师新增“审核下一个同学”，末位禁用。
- 管理员新增学生打卡查询，支持姓名、学号、项目和日期、游标分页，查看本组织正式提交的凭证。沿用 COURSE_VIEW 权限，拒绝未提交材料及无对应权限的分管理员。
- 当前组织业务事件显示时间、真实姓名、角色、学号、邮箱、动作、操作反馈、后续处理。以组织、requestId 与业务对象关联审计；缺失可靠归属时显示未记录，不猜测操作人。

## 验证
- TypeScript 后端/Portal、后端构建、Portal 构建、OpenAPI 生成检查和 git diff --check 通过。
- 6 项 AI 单元/数据库集成测试通过：随机子集、普通默认有效、异常建议不扣学时、重试、租约、预算及人工审核优先。
- 本地真实 PostgreSQL + HTTP 26 项检查通过。教师配置 180 分钟，120 分钟补录审核前 0 分钟、审核后 120 分钟；181 分钟拒绝。检查了管理员检索、实际签名凭证读取、草稿凭证拒绝、分管理员权限、非任课教师隔离及事件归属。
- Chromium 浏览器完成管理员列表、事件表、教师上限、下一学生实际切换和 390×844 手机补录弹窗验证；截图附后。

## 生产
- 发布：/opt/bnbu-sports-production/releases/review-updates-final-20260918
- 回退应用：/opt/bnbu-sports-production/releases/demand-six-20260917
- 数据库备份：/opt/bnbu-sports-production/backups/before-bugfix-20260918T041040Z.dump（40,467,848 字节）
- 仅新增 0085_history_duration_limit。线上原登记截至 0083；发布包没有带入本地未部署且不相关的 0084。
- 后端、Portal、学生入口与 readiness 检查通过；15 个后端文件、2 个学生资源和 Portal 静态资源哈希核对通过，详见 deployment.json / public-verification.json。
- 已追加审计并恢复 106 条仅被旧 AI 异常规则转为待审核的普通记录，重新计算学时，复查符合恢复条件的剩余记录为 0。跳过补录、人工审核与结算记录。旧审核事实保留。
- 业务流程浏览器/HTTP 验收在本地隔离测试组织进行；线上验证包含资源、健康及数据库恢复读回，不冒充真实师生浏览器验收。

## 存档范围
本次实现、契约、业务补充说明、测试与发布证据；保留任务开始前 demand.md 改动和 tools/production/__pycache__/。构建包、基线与失败诊断仅留本地且不加入 Git。
