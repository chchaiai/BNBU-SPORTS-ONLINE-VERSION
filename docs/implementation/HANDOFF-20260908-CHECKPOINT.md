# 三端后端联调阶段交接

这是本地开发检查点，整体业务验收未完成，不能作为上线或全部通过的依据。本文件所在的 Git 提交保存当前实现与原始失败证据；后续工作继续在当前任务推进。

- 分支：`codex/bnbu-sports-web-new-20260907`。
- 检查点之前的 HEAD：`fb511a9ccbad65c2f76c74a2de1e04883083b287`。
- 当前本地 Draft：229 个操作；数据库已应用 49 项迁移。
- 业务依据：`docs/business` 四份 V8.1 文档，以及用户确认的相机来源、人工审核优先、不要求学校工作日日历、教师邮箱不限制域名。
- 用户要求测试发现的问题只记录，不修复；本检查点保留失败代码、夹具和日志。

## 验证状态

逐项范围见 `ACCEPTANCE.md`；时间顺序与原始日志索引见 `VALIDATION.md`；问题见 `ISSUES.md`。较早整套测试通过不能证明后续扩展已全量通过。

最近已验证：运行诊断 ZIP 的真实后台生成、MinIO 私有下载、独立解码、审计和请求关联、权限撤销、合成凭据过期、取消后的派生文件清理；学生注销挑战的真实 SMTP 邮件、版本校验、重放和限流；历史主体迁移及合成学生物理删除后的成员/审计 HTTP 读取。

当前全量协议检查失败：`deletion-challenge-parity-20260908.txt`，分管理员权限数组 `ENUM_MISMATCH`。`docker-subadmin-permissions-20260908.txt` 在合成资料字段超长处失败，尚未执行新增权限接口请求。

最新 Docker 失败：`docker-session-subjects-20260908.txt`。第 49 项迁移及构建/启动通过，但清理偏好时被既有 `user preferences cannot be deleted` 触发器拒绝。`session-subject-rollback-20260908.json` 确认失败事务已回滚。未修改规则或夹具绕过失败。

其他保留失败包括 CSV 501 行仍被接受、关闭后新增名单来源、Portal 容器缺少 XLSX 依赖、原解析对象不变性断言、部分 OCR/补练夹具错误、旧完整人工流程夹具修改已发布安排、学期切换检查缺少管理员权限映射等。详细证据以 `ISSUES.md` 为准，不能因后续构建通过而关闭。

## 尚需完成

1. 学生注销确认、删除回执、账号侧凭据/挑战/会话清理，以及全部历史查询的适配。当前只有注销挑战接口；确认接口没有实现。
2. 业务答复：待审核、待补证、技术处理中材料、待处理认证/免测是否禁止注销。另有此前已询问的结算未注册/未录体测分支、管理员身份核验来源、年级来源、现有页面复用等问题仍待答复。
3. 正式结算、不可变报告及受控更正、学期原子切换归档、账号删除治理，以及验收清单中的其他管理动作和三端接线。
4. Prisma 当前资料 relation 与历史主体的全面适配。已迁移数据库外键不等于所有历史查询都能正常读取。
5. 三端实际浏览器验收；静态布局检查不是全部 UI/UX 验收。`ui-preservation-20260908.json` 显示已跟踪源码的元素/样式属性/HTML片段一致，但教师建号页存在按钮条件、事件与动态提示文案差异。
6. 全量非 HTTP 运行日志、基础设施历史健康、生产采集/保留策略、上传中断孤立文件处理，以及其他尚未验证的异常/并发分支。

## 本地运行与云端依赖

使用 `tools/local-integration/compose.v81-test.yml`，项目名 `bnbu-v81-local-validation`。本地 PostgreSQL 业务测试库为 `v81_runtime_test`，对象存储为 MinIO，邮件为 Mailpit。测试容器已停止，数据卷保留；不执行 `down -v`。真实本地配置位于忽略路径 `.local/v81-sql.env`，不得写入提交或输出内容。

需要重新测试时使用相应专项环境标志，命令及日志见 `VALIDATION.md`。新运行会产生合成业务事实，不能把它们当作真实学校数据。此前某些夹具仍按旧模板/迁移数假设编写，未自动修改为通过。

腾讯云方案继续使用 CVM、私有 COS 和 PostgreSQL。未访问生产数据、部署云端或验证真实腾讯 OCR/SES；云端 IAM、网络/TLS、存储保留与邮件配置属于剩余交接依赖，不能由本地 MinIO/Mailpit 通过替代。

## 存档范围

仅本任务的前端服务接线及测试、后端、契约副本与本地 Draft、测试工具、文档和证据，以及本地忽略/构建上下文配置。暂存按明确文件清单进行，未纳入 `.local`、实际 `.env`、依赖目录或密钥目录。业务权威文档未修改。最终提交范围以 `git show --name-status <本检查点提交>` 为准。

这次存档为本地阶段提交，不推送、不合并、不发布；当前任务继续，最终交付仍须覆盖原始完整目标。

2026-09-08 阶段本地存档前检查：明确清单暂存1341文件，约36.3MB，无范围外路径、无删除。当前业务权威docs/business无差异。凭据模式扫描35处数据库URL候选，均为示例/测试、仅供schema生成的127.0.0.1:1占位地址或运行时插值；无匹配私钥、腾讯/AWS访问密钥或JWT。实际.env、.local、依赖和密钥目录不在清单。该扫描是有限模式检查，不等于完整安全审计。
暂存SQL字节逐项校验：49个migration.sql与暂存manifest.json的SHA256全部一致；Git换行转换未改变迁移校验内容。git diff --cached --check退出2，14842行报告涉及408个文件：83个Prisma生成文件、323个证据/文档文件，另.gitignore和v81-feedback.ts。为保留原始输出和已记录失败，本次不批量改写生成代码或测试日志，明确记录空白检查未通过。阶段提交保存当前状态，不据此宣称质量门禁/整体验收通过。
