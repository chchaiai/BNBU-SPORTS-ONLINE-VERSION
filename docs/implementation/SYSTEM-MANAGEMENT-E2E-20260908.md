# 系统维护管理 HTTP E2E（2026-09-08）

新增 v81-system-management.e2e.test.ts，自动纳入完整 E2E。Docker 专项 1 个完整业务场景通过，退出 0；实际 Nest HTTP、PostgreSQL 18.4、57 个迁移、严格 OpenAPI 响应校验。

验证管理员从 NORMAL 切至 MAINTENANCE，同一幂等键重放不重复写历史；公告双语字段符合输入；普通教师和学生不能切换模式或读管理历史；缺少公告标题的维护请求 422；教师建课在维护期间 503，数据库课程数量不变；过期版本恢复 409，正确版本恢复 NORMAL；最终历史两条、维护中断一条且已结束，每个本组织活跃用户只收到维护和恢复各一条通知。

两个合成组织初始均 NORMAL。主组织切至维护后，另一个组织仍正常时公开公告 503；测试显式将隔离组织策略同步到维护后验证公告，并在恢复时同步回正常。该夹具操作不代表多组织维护联动已实现，也不宣称不同学校公告的选取需求已经验收。

修复 Draft 协议：公开公告补 503；历史返回从错误的 object 改为最多 100 条事件数组，约束 id、actor_id、version、facts、occurred_at；教师建课补维护时 503。后端既有模式逻辑没有修改，界面布局没有修改。

证据按执行顺序保留：
- docker-system-management-e2e-first-20260908.txt：公告 503 未声明。
- docker-system-management-e2e-retest-20260908.txt：建课维护 503 未声明。
- docker-system-management-e2e-final-20260908.txt：完整场景通过。

协议生成检查和 249 操作/249 handler 对应检查通过。全量 E2E 未重跑；最近完整结果仍是第四轮 69/69 用例通过、覆盖门禁 126 项失败。本次补入系统管理三个 V81 操作成功响应，不据专项结果推算全量门禁通过。
