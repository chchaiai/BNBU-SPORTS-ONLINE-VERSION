# 分管理员当前验收 2026-09-09

本地 Edge、持续 Docker 后端、PostgreSQL 与 Mailpit，使用全新合成分管理员账号。总管理员通过现有页面完成：新增资料和审计权限、邮箱 OTP、错误 OTP 后重试、创建与刷新、停用/启用与刷新、修改姓名和部门、撤销全部权限、变更邮箱重新 OTP、删除确认与刷新后消失。subadmin-current-browser-20260909.txt 退出 0。删除对象仅为本轮新建的合成分管理员，未操作教师或学生删除。

将已有 probeSubadminPermissions 接入 v81-subadmin-identity.e2e.test.ts，使权限即时撤销成为常规 E2E。专用 Docker PostgreSQL 严格协议测试覆盖：实际初始密码登录与改密后登录；总管理员变更权限及同键回放；当前已登录会话失去审计权限后 403、恢复后 200；分管理员自行赋权和教师修改被拒绝；旧版本冲突；SUPER、重复权限及夹带密码字段拒绝；并发修改为一个 201、一个 409；撤销全部权限后当前会话立即拒绝；不可变权限事件的数量/版本正确，账号资料和状态不受权限变更影响。

首次 docker-subadmin-current-20260909.txt 退出 1：保护总管理员目标的正确 404 未在权限修改接口声明。仅补齐 /admin/subadmins/{id}/permissions 的 NotFound 及生成结果，未改变业务权限或数据库实现。docker-subadmin-current-retest-20260909.txt 为 2/2 通过，同时回归真实 SMTP OTP、一次性消费、资料/状态管理、删除后的历史回放。subadmin-current-contract-check-20260909.txt 基础协议通过，251 操作维持不变；数据库仍为 58 迁移。

本轮不修改 UI 或布局。已有完整后端全量基线仍为第十六次 94/94，本轮新增一个 E2E 用例，仅运行所影响文件 2/2；不能将其称为已跑 95 项全量。

剩余：所有八项权限的完整组合、各业务写动作和移动视口尚未由这次专项覆盖；首次改密网页缺口仍存在，复用密码弹窗的调整保持待用户答复。云 SMTP/真实业务资料及其他三端范围继续待验收。