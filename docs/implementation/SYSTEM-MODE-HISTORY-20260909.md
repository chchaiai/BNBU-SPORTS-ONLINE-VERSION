# 维护历史网页与分页 2026-09-09

业务依据 docs/business/30-admin-flow.md 13.4：每次成功切换追加只读历史，展示原因、前后模式、操作人、时间和公告发布状态。

原真实页面没有接入历史，后端最多 100 条且无后续读取方式。GET /system-mode/history 现在接受可选正整数 beforeVersion，返回本校严格小于该版本的记录，按版本倒序，每页仍最多 100。原无参数调用兼容。页面自动继续读取，并拒绝非递减或无效版本，复用既有历史卡片与行布局；未新增编辑、删除或翻页控件。成功切换后重新读取当前模式及历史。操作人展示已保存的账号 ID，不推测历史姓名，也不额外扩大账号资料读取权限。

Docker 严格协议专项最终 7/7：105 条明确标注的合成分页夹具分成 100+5、空末页、无重复、版本顺序、非法参数 422；另一学校的 999 版本记录不混入。既有维护切换、公开学校范围、角色限制及改密等同时回归。日志 docker-mode-history-20260909.txt、docker-mode-history-isolation-20260909.txt 均退出 0。基本协议和 Portal TypeScript 检查通过，251 操作与 58 迁移不变。

持续本地 Docker 后端更新健康。Edge 真实管理员登录后，历史全部字段与实际接口对比，刷新后 ID 顺序一致，历史区域无编辑/删除控件。先读取 8 条，再经现有 UI 成功切换/恢复后读取 10 条；mode-history-browser-20260909.txt、mode-history-after-switch-browser-20260909.txt 退出 0。最终截图 mode-history-current-20260909.png。切换回归原选择器同时匹配控制按钮与历史 li，首次退出 1，保留 mode-history-switch-regression-20260909.txt；限定现有控制区域后 mode-history-switch-retest-20260909.txt 通过，无业务实现失败。

本次浏览器历史来自本地合成业务操作；105 条边界与外校隔离只在专用 Docker 测试数据库，未伪造持续浏览器历史。浏览器尚未验证 100 条以上的页面实际载入，代码自动分页与后端边界证据应分别看待。未确认切换跨刷新恢复、教师学生维护公告/补证暂停展示、生产学校范围与真实云配置仍待验收。最新完整全量为第十八次 96/96；新增历史用例仅运行所影响文件 7/7，未声称已跑 97 项全量。三端完整目标继续。
