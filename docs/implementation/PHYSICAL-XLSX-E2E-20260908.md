# XLSX 体测导入与请求大小配置（2026-09-08）

在体测导入 E2E 增加真实 XLSX 二进制：两个工作表中只选“体测”表，女生800米4:20解析为260秒；创建及重放，源文件Base64逐字节与SHA256一致，确认后学生读取正式结果。不存在的工作表与无效Base64均422，数据库只留下一个合法批次。CSV原有回滚场景一并通过，Docker 2/2，退出0。

首轮合法文件422，定位为通用测试夹具 REQUEST_BODY_LIMIT_BYTES=2048 的请求体限制，未进入导入业务。已仅为文件导入套件配置2097152字节；通用小请求边界测试保留。进一步发现 backend/.env.example、.env.test.example、config/staging.env.example 和本地初始化脚本的1048576字节不够容纳接口允许的1MB文件经Base64编码后的JSON，统一为2097152，与既有V81浏览器及启动探针一致。业务文件大小上限未扩大，未改实际秘密配置或云环境。

首轮与复测日志为 docker-physical-xlsx-e2e-first-20260908.txt、docker-physical-xlsx-e2e-retest-20260908.txt。环境模板差异与空白检查通过。本轮尚未对最大文件尺寸做边界压测。

对象存储是内存测试适配器，XLSX解析走真实隔离worker；不代表COS/MinIO持久化验收，未修改UI布局。全量门禁最新仍第六轮88项缺口，剩余业务及三端浏览器、云端依赖继续。
