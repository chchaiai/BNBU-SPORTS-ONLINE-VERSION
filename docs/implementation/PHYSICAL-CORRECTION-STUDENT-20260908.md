# 体测纠错学生读回与无报告回滚（2026-09-08）

测试范围：正式 HTTP 结算、学期切换归档、教师体测纠错后，学生通过本地 SMTP 邮箱验证码登录，读取本人最新体测和结算结果；检查学生越权读他人及写纠错被拒绝。无结算报告的对照课程准备一条原始结果，再请求纠错并检查原记录完整、没有新增通知或审计。

首次失败：`docker-physical-correction-student-first-20260908.txt`。对照课程已被场景准备移至另一学期，测试新成员仍使用旧学期，外键拒绝；改为读取对照课程的实际学期。

第二次失败：`docker-physical-correction-student-retest-20260908.txt`。学生错误使用教师/管理员密码入口，返回 401；改为现有学生邮箱验证码入口并从 Mailpit 读取测试邮件。未修改产品认证规则。

最终执行日志：`docker-physical-correction-student-otp-retest-20260908.txt`，命令退出 0。学生本人最新体测与第三版报告中的秒数一致，学生读他人 404、写纠错 403；无报告纠错 409 且原结果、通知、审计检查通过。原有并发、报告故障回滚、回放、历史保留以及正式结算切换回归全部通过。

所有数据仅位于隔离 Docker 测试库；无报告课程的原始体测由 SQL 准备，纠错和学生登录读回使用 HTTP。本轮未改产品或界面。剩余免测专项、持续浏览器环境更新、三端页面接线及全量验收继续；云端依赖尚未验收。
