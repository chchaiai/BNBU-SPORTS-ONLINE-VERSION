# 管理员维护模式访问修复（2026-09-14）

用户决定：维护模式下，总管理员与分管理员正常使用各自已授权的全部功能。教师和学生业务仍暂停。

根因：全局模式 Guard 仅例外放行管理员身份查询；多个管理服务事务仍要求 NORMAL；前端每次维护状态刷新都跳转系统页；模板、日志归档、学生删除数据库函数也要求 NORMAL。

修改：统一行政模式可用性判断；保留每项权限、首次改密、版本、幂等与审计约束；取消管理端强制跳转；日志归档工作器处理维护期间的授权任务。数据库迁移 0078 仅替换三个函数中的模式条件，无表结构或业务数据变更。学生补证和教师 SLA 暂停行为保持现行规则。

本地验证：19/19 真实 HTTP/PostgreSQL 测试、33/33 前端相关测试、1/1 模式单测通过；前后端类型检查、构建、78 项迁移安全扫描通过。ESLint 153 项既有问题与 HEAD 按文件和规则计数一致，无新增。生产不切换维护模式做测试；真实业务测试使用独立临时 PostgreSQL 和合成账户。

回退：应用与页面回切部署前版本；0078 为兼容迁移，旧应用仍执行原有维护限制。需要回退数据库条件时应先核对三个函数定义，备份后执行专用逆向迁移，不能恢复整库覆盖新增业务数据。

并行工作：0077 学时投影迁移及相关未提交文件属于其他任务；此次生产迁移在已部署的 0077 基础上追加 0078。

## 生产结果

已部署 `admin-maintenance-20260914`，源代码提交 `ef4ccbffc8eb60a6898080145b7bd0221918685b`，上一版本 `student-bulk-deletion-20260914`。

数据库备份：`/opt/bnbu-sports-production/backups/before-bugfix-20260914T133408Z.dump`（13,469,416 字节），备份目录验证通过。0078 已执行并只读核对迁移校验和与三个数据库函数。30 个后端构建文件和 6 个线上前端资源哈希与测试产物一致。教师端、学生端、后端 ready 和入口站点均 HTTP 200。

接口 URL、请求/响应结构未改变；改变的是管理员在 MAINTENANCE 状态的可用性。前端维护刷新不再关闭管理员弹窗或改变当前页面。数据库无表结构变更。

证据目录：`docs/implementation/evidence/admin-maintenance-20260914/`。独立测试服务已停止。无生产维护切换或真实学生删除测试；设备上所有页面逐项操作未验证，生产业务验收边界为健康、实际资源、迁移和函数读回。维护业务链路已在隔离数据库验证，含 64 项分管理员权限组合、管理员写入、学生删除、模板发布与日志私有下载。

静态检查基线：153 项错误在修改前后逐文件、逐规则数量一致，详见 maintenance-eslint-baseline.json；不声称全仓库 lint 通过。应用回退命令为服务端部署脚本 `--rollback`，数据库 0078 保留兼容状态；完整备份仅供灾难恢复，禁止为功能回退覆盖新增业务事实。
