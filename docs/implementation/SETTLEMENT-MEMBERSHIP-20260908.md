# 结算后成员、邀请与名单来源保护

日期：2026-09-08 13:47。完整三端目标仍进行中。

本轮将真实结算报告检查接入手动入班、成员移除/恢复、二维码入班、邀请生成/轮换、电子名单首次导入、名单来源切换、OCR 受理/草稿修改/名单确认。成员和邀请写入新增组织锁，使其与结算命令按组织先行串行协调；校验权限后才检查结算状态。

电子名单已受理阶段的恢复继续按原阶段处理。合法结算的预检查会阻止 RECEIVED/VALIDATING 导入，不能通过结算截断原有受理链。原有幂等回放保持在业务执行前处理。

## 本地 Docker 证据

- docker-settlement-membership-guards-first-20260908.txt：构建、57 项迁移和原业务 HTTP 回归通过。
- docker-settlement-membership-specialized-20260908.txt：真实服务与数据库仓储在 Serializable 回滚事务内验证已结算课程的手动入班、移除/恢复、生成邀请、切换来源拒绝；成员状态/版本和邀请数量不变，结算内部命令等专项通过。幂等外层为事务适配器，不代表完整 HTTP 结算链。
- docker-settlement-source-guards-final-20260908.txt：补电子名单和 OCR 受理/草稿守卫后，构建与原业务回归通过。
- docker-settlement-ocr-regression-20260908.txt：首次 OCR 回归失败，旧断言把未注册人数计入 ROSTER_PENDING_DIFFERENCE。
- docker-settlement-ocr-regression-retest-20260908.txt：按当前检查结构分别验证名单差异 CLEAR/0、注册未完成 UNAVAILABLE/1、ready=false 后通过。OCR 受理、读取、草稿、确认、并发/重试、原件摘要、权限和管理员摘要通过；识别提供方使用合成响应，不能作为腾讯云 OCR 实际识别验收。

首次失败保留；其他四个进程退出 0，失败进程退出 1 后修正测试复跑，无悬挂测试。原始日志保留工具空白，代码/手写文档单独检查空白。

## 交接与剩余工作

继续审计名单核对及解决差异、OCR 任务重试、体测批次等剩余写入路径。旧事实纠错仍需与报告更正版同事务联动。正式结算 HTTP 尚未开放，ready 保持 false；政策未定的注册/体测缺项仍需业务答复。之后完成协议、幂等端点、报告读/导出、原页面接线和学期切换全链验证。

没有 UI/UX 布局、协议操作、迁移或云环境变更。持续浏览器后端本轮未重建，测试来自本地隔离 Docker 服务。
