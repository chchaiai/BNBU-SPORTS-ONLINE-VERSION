# 生产变更待确认方案

状态：用户已明确批准 A、B，本轮已部署。当前发布为 invite-ui-final-20260913；配置、验证、回滚点与剩余事项见 OCR-APPROVED-RELEASE-20260913.md。以下保留获批方案。

## A：OCR 大文件入口

只针对教师域名 /api/v1/class-sections/{UUID}/ocr-roster-batches 和 ocr-physical-batches 两个路径：请求上限101 MiB（容纳100 MiB文件及multipart开销）、关闭代理请求缓冲、读取超时65秒。后端鉴权、文件类型和100 MiB文件校验继续执行，其他入口限制保持现值。执行前读取并备份实际站点配置，生成窄范围差异，nginx -t通过才reload；失败恢复原文件并再次校验。保留现有日志策略。

风险：两个上传入口可接收更大的请求，占用更多网络和连接资源。验证使用有界合成文件，检查超过2 MiB合法上传及超限拒绝，不做并发压测。无需新云权限、长期密钥或新付费服务；现有流量按现有计费政策。

## B：邀请期限与固定宽限

部署已本地验证的ab02e8f4：默认30分钟、教师可选5至120整数分钟；自然到期前已登记的入班流程，可在邀请到期后固定10分钟内完成，重复请求不延长。撤销邀请、关闭入班或课程等仍立即阻断。

风险：已登记凭证的有效消费时间延长，属于生产安全策略。候选需基于最新后端与门户已发布补丁隔离构建，不能直接覆盖为旧版本。无需数据库迁移。发布前备份镜像、学生静态文件和current指向；失败恢复。批准后执行部署前检查、发布及真实有界自然到期验证。

两个项目可以分别批准或拒绝。Android版本策略和隐私政策不包含在这份授权范围内。
