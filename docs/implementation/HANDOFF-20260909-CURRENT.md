# 三端本地业务交接

状态：按用户当前确认范围完成本地三端业务交付；云端接入与正式协议发布不在本地验收通过声明内。以本文件、CURRENT-ACCEPTANCE-20260909.md 和 BUSINESS-CHAPTER-AUDIT-20260909.md 为当前入口；旧日期报告保留当时状态。

## 已有可复核证据

- 三端原始主流程共 30 项保留在 [逐章节矩阵](BUSINESS-CHAPTER-AUDIT-20260909.md)，其本地证据链接已检查存在。LOCAL_EVIDENCE 仅覆盖所列场景，不等于任意异常全排列通过。
- 最新完整 Docker：第 41 次，101/101、25 套件、零失败/跳过，后端类型检查通过；59 个迁移。见 [日志](docker-fortyfirst-20260909.txt)。
- 候选接口 253 项：219/219 启用成功响应、253/253 错误/访问响应、34 个按策略拒绝的操作覆盖通过。见 [接口矩阵](runtime-conformance-fortyfirst-20260909.md)。
- 总管理员模板发布、按课程人工审核、OCR 配置启停的三个批准表单已完成，见 [表单验收](ADMIN-REVIEW-SERVICES-WEB-20260909.md)。
- 新学生通过已有有效课程邀请入班并首次验证邮箱、无效记录后重新运动、补练授权与撤销后续办、自动期末提醒的网页证据分别见 [注册](NEW-STUDENT-REGISTRATION-WEB-20260909.md)、[重新运动](INVALID-REPEAT-WEB-20260909.md)、[补练](MAKEUP-DEADLINE-FIX-20260909.md)、[提醒](LIVE-REMINDER-WEB-20260909.md)。失败与修复后的复测均保留。

## 最后阻断已解除

教师课程参数最小调整已获授权并完成。[新课程验收](COURSE-PARAMETERS-WEB-20260909.md)证明：从全新课程选择 8/12 小时、45 分钟、每周 4 次，发布锁定、生成邀请、新学生首次入班与邮件验证、教师名单和数据库独立回读通过。原失败历史保留，不再列作待授权。

原始 30 项主流程均有对应本地证据或用户明确暂缓/排除及真实云端依赖说明。没有用合成历史课程的测试替代本次新课程发布；没有把本地 MinIO/Mailpit 或模拟 OCR 作为腾讯云服务验收。

## 用户排除或暂缓

学生注销、教师删除显示“暂时功能无法实现，敬请期待”；学校统一工作日日历及其 SLA 验收排除，保留教师审核轮次事实。学生游泳专用材料标记/延迟说明、本人结算网页显示暂缓，后端保留。先验人工审核，自动 AI 通过仍需真实样本验证。

## 腾讯云架构与依赖

CVM 运行同一 API 工程和后台任务；PostgreSQL 保存事务、版本、幂等及审计事实；COS 保存私有材料，使用角色凭据和短期授权。详见 [架构记录](ARCHITECTURE.md)。本地对象存储为 MinIO、邮件为 Mailpit；不据此宣布 COS 或真实邮件投递通过。

云端接入仍需核对 COS 桶/区域/角色/前缀权限/CORS，CVM 到 PostgreSQL 的网络与可信 CA，生产发信服务，真实 OCR 与 AI 样本验证，备份恢复、运行采集及对象清理策略。本任务未执行云端部署。

候选协议为 `3.0.0-v81-local-draft`；已发布协议的兼容与发布门禁未通过，不得按旧客户端兼容升级发布。差异清单见 [候选审计](local-candidate-compatibility-audit-20260909.json)。工作区整体生成检查还受缺失 Android/旧 frontend 入口影响，未伪造目录或放宽发布门禁。

## 本地运行与存档

学生入口 `http://127.0.0.1:4274/student/`，教师/管理员入口 `http://localhost:3300/`，API `http://127.0.0.1:3199/api/v1`。测试数据为合成数据；账号定位仅存本地忽略目录，不在报告中公开。

Docker 配置位于 `tools/local-integration/compose.v81-test.yml`、`compose.v81-browser.yml` 和 `compose.v81-full-integration.yml`；本地变量文件 `.local/v81-sql.env`。继续工作前检查现有容器、分支和工作区，不重新初始化正在使用的测试数据库。每轮按准确路径本地提交，提交哈希以 Git 日志为准；没有 push、合并或部署。
