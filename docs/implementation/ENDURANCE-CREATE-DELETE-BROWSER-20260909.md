# 换算区间新增删除网页验证

2026-09-09，现有 Portal localhost:3300 连接本地 Docker API 3199、PostgreSQL 合成组织。产品源码未变。

`tools/local-integration/v81-endurance-create-delete-browser-probe.mjs` 使用实际管理员登录及原有按钮，完成：

- 男生大一大二表原 101 行，在末尾新增连续区间，POST 201；刷新网页后能读到唯一合成备注。
- 尝试删除中间第二行，实际 DELETE 命令返回 422，现有错误区域可见；HTTP 回读确认版本仍为新增后的版本、102 行未变化。
- 通过原确认弹窗删除刚新增的末尾行，POST 201；刷新后该行消失。
- HTTP 回读全部 bands 与测试前逐字段相同，表版本只增加 2，保留新增与删除修订历史。finally 仅按本次唯一备注清理自建区间，采用最新版本，不删除其他记录。

`endurance-create-delete-browser-20260909.txt` PASS，退出 0。浏览器后端容器 healthy。本轮仅新增测试和记录，未重跑完整后端套件；第二十轮 97/97 仍为最新全量证据。

此证据覆盖现有新增、删除、间隙拒绝和刷新回读。结合 ENDURANCE-BROWSER-20260909.md 的编辑同键重试证据，可证明这些本地页面操作。跨刷新未确认写入、其他三张表、并发管理员、教师历史结果绑定以及真实云端仍需对应验证，不能视为三端整体验收完成。
