# 分管理员 SMTP OTP 统一 E2E（2026-09-08）

依据用户明确要求，分管理员身份核验采用邮箱 OTP。新增 v81-subadmin-identity.e2e.test.ts，复用现有完整身份专项，在统一 E2E 中启用实际 SMTP 投递和严格 OpenAPI 响应校验。

专用 Docker 测试项目新增 Mailpit v1.30.0，只有内部测试网络，无宿主端口和生产邮箱连接。测试从 Mailpit 读取随机合成邮箱的验证码；Mailpit 是夹具服务，使用独立 HTTP 读取器，业务请求仍全部经过严格协议校验。普通专项默认读取方式保持兼容。

专项复测通过：申请 OTP 与重放、重复申请限流、错误验证码次数与版本、正确验证码核验及重放、核验凭证一次性消费后建号、首次改密与权限限制、停用交接确认/恢复/会话撤销、资料更新及匹配新邮箱 OTP、权限变化、删除分管理员时并发重放与会话/推送/旧回执清理，保留反馈历史和主体记录。该删除测试仅涉及分管理员，不涉及用户要求暂缓的学生注销和教师删除。

首轮失败为 OTP 申请接口限流 429 未声明；已补 TooManyRequests，未降低限流。首轮与专项复测见 docker-subadmin-identity-e2e-first-20260908.txt、docker-subadmin-identity-e2e-retest-20260908.txt。

随后增加管理列表在建号后可见、删除后不可见、教师不能读取的检查，随完整第五轮运行验证。生成检查及 249 操作/249 handler 静态对应检查通过。没有改变 UI 布局；腾讯云正式邮件投递和真实邮箱业务验收不在本地 Mailpit 证据中。

第五轮完整结果：15 套件、75/75 用例通过，新增列表断言通过；950 条响应有效，0 无效。覆盖门禁 107 项失败，退出 1。见 docker-backend-e2e-full-fifth-20260908.txt 和 runtime-conformance-fifth-20260908.json/.md。按仍待校准的清单，127 个启用接口具备成功及错误证据，17 个停用入口拒绝已验证；105 个接口验证未完整，另有两项清单数量不符。全部 249 接口有错误/权限响应证据。整体三端验收尚未完成。
