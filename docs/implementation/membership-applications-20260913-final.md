# 教师成员范围与免测/免打卡申请修复交接

2026-09-13：代码修复、本地真实 Docker 回归、腾讯云发布和合成账号线上验收完成。

## 根因与修复结果

| 问题 | 根因 | 最终行为 |
| --- | --- | --- |
| 教师审核看到已移出成员打卡 | 原列表仅按责任教师筛选课程，没有要求打卡所属成员关系仍为 ACTIVE | 后端按本人课程、课程未删除及 ACTIVE 成员关系筛选。移出后列表不返回该成员记录；学生本人历史保留。恢复有效成员关系后可继续按当前权限查看 |
| 移出后免测/免打卡申请和资格仍有效 | 成员结束未联动申请生命周期、认可分钟和本机草稿 | 0074 在成员状态变更事务中清理申请业务状态，待办终止、当前资格撤销。已清理申请在师生列表和详情不可读取或继续修改；保留原始事实、审核记录、附件及系统审计。恢复入班不复活旧申请 |
| 校队申请误报非在读 | 前端把当前学期展示标记当作在读判定，旧申请草稿及上传重试可能沿用旧选课 ID | 提交前读取最新 ACTIVE 选课；旧草稿不跨选课复用，材料校验所属选课，上传签名包含选课 ID。真正没有有效成员关系时仍禁止新申请 |
| 新拍照片只有通用图标 | 材料行已有点击预览，但未为本地及恢复材料加载封面 | 新拍照片、本地草稿和远端草稿显示真实缩略图；点击复用材料大图预览。已提交证明继续通过私有 COS 短时授权读取 |

本机草稿按学生、选课 ID 和成员版本隔离。提交后的列表以最新服务端结果为准，避免把已清理申请从旧响应重新插回页面。

## 已确认的历史边界

- 本轮用户明确确认：清除师生业务页面展示、终止待审申请、撤销当前免测/免打卡资格，同时保留审计及已结算成绩。
- 迁移处理既有已移出成员；具有结算报告的课程跳过申请事实和成绩修改，其无有效成员关系的申请仍不在师生业务列表显示。
- 清理保留申请原始范围、材料、审核和已发生的时间事实，记录 membership_cleared_at 及系统事件；不会物理删除照片或伪造教师审核意见。
- 未结算课程移出/恢复时重新计算当前计入投影；已结算报告通过既有写入保护保持不变。

## 本地验证

- 后端 277/277 单元测试，学生 86/86 smoke，后端类型检查、74 个迁移安全检查、OpenAPI 263 操作生成校验及 Backend/Migrator 生产镜像构建通过。
- 真实 PostgreSQL/HTTP：课程仍开放时移出学生，教师列表不再返回其打卡，历史记录仍在；已批准的两类申请、待审核及草稿均终止，认可分钟停用；恢复不复活旧申请。
- 回滚式数据库测试覆盖 5 个成员、14 份既有结算报告，结算报告及关联申请事实不变。
- 真实浏览器：最新有效选课存在但展示标记过期、持有旧班级草稿时，校队申请成功提交；真实图片封面与点击放大通过。

## 线上验证

独立 MEMBER13 合成组织及账号，使用实际香港 COS、数据库和网页：

- 耐力跑免测和校队免打卡真实提交并批准，社团申请提交待审；照片上传、扫描、封面和大图读取通过。
- 真实等待 60 秒建立打卡记录，教师在有效关系期间可读；移出后教师 API 及打卡审核网页不再显示该记录，学生本人仍可读取历史。
- 成员移出同幂等请求重放通过；师生申请列表清空、原申请详情返回 404；恢复后旧申请仍不出现，认可分钟为零。
- 只读数据库验收确认保留 3 份申请、2 条审核历史、3 个附件关联和 3 条系统清理事件，当前有效认可数量为零。
- 线上截图已检查：教师审核为空、学生申请为空、已批准申请详情及真实图片封面。脱敏证据在 `evidence/membership-20260913/`。

测试失败过程保留在私有 `.local/membership-*.log`：旧容器源码未同步、测试请求早于服务就绪、使用不存在的返回按钮、提交完成前刷新以及多图片定位未限定元素。修正测试环境或操作后取得上述最终通过结果，没有删除失败记录或通过修改真实用户数据绕过验证。

## 部署和交接

- 当前发布：`/opt/bnbu-sports-production/releases/membership-20260913`，源码 `63d2d26300298bd1fa34b49faf9657a72a0bc061`，包含前序 `08269aaf` 修复。
- Backend：`sha256:e74cf865dd03ae60673f86b74956c523ddbc3162d9d6a1b3a4696759e3823fda`。
- Migrator：`sha256:6619b8e8c2461efaca397b8932b68fa8e02807f5496815a21321d2e9ae861379`。Portal 沿用上一版镜像。
- 部署前数据库备份：`/opt/bnbu-sports-production/backups/before-bugfix-20260912T163105Z.dump`，3,769,711 字节，备份目录校验通过。文件名使用 UTC。
- 写入短暂关闭后执行 0074，启动应用并校验健康及两个学生 JS 文件哈希，随后恢复写入。上一发布为 `course-retired-20260912`。
- 新清理状态已生效，后续问题应优先前向修复；不能以恢复备份的方式复活已清理资格。数据库恢复需要独立评估新增业务数据。
- 用户刷新网页即可使用。真实手机系统相机硬件操作不是本轮自动化验收结论；本轮验证了浏览器拍摄文件入口、文件处理、封面、放大和真实提交。
- 本轮本地提交未推送；原有 bug.md、0061 行尾状态及旧报告删除保留原状态。
