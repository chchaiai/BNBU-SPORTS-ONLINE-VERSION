# 学生帮助缓存隔离修复（2026-09-08）

发现原帮助缓存仅以语言为键，不区分账号。改为服务端用户 ID 与语言共同标识，匿名状态不读写，旧无账号缓存不再读取。页面帮助状态同时绑定用户 ID 和登录会话代次；请求结束时校验当前状态对象和会话，防止退出/切换账号或语言后旧请求覆盖新状态。

新增 Docker 测试 `v81-help-cache.test.mjs` 覆盖两用户双语言、忽略旧无账号缓存、匿名拒绝读写及损坏单项不影响另一账号。运行证据 `docker-student-help-cache-20260908.txt`，1 项通过，命令退出 0。

`student-help-cache-browser-20260908.txt`：真实学生邮件登录和本地 Docker 帮助读取、管理员下线/重发后学生在线重入同步均通过，退出 0。未修改界面布局。

剩余：同浏览器两个真实账号切换及旧请求延迟专项、离线缓存提示、英文切换与移动视口继续。本轮缓存单测不能替代真实多账号端到端验收。未执行云端操作。
