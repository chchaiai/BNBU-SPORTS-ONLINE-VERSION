# 分管理员邮箱 OTP 建号验证

用户已授权修复测试失败，并指定分管理员使用邮箱 OTP 核验。学生注销和教师删除按用户决定暂缓，沿用现有界面的不可用提示。

新增迁移 0053 及三个接口：总管理员发送目标邮箱验证码、核验验证码、消费核验凭证创建分管理员。凭证绑定组织、操作人和邮箱，十分钟有效、五次错误锁定、同邮箱六十秒发送间隔，创建时一次性消费。账户首次登录必须改密，授权范围由总管理员选择。

本地 Docker 首次建号返回 500，原始证据保留在 docker-subadmin-identity-20260908.txt。修复 PostgreSQL advisory lock 的 void 查询返回值，改为返回整型；姓名上限同步数据库的 100 字符。没有修改已应用迁移。

复验 docker-subadmin-identity-retest-20260908.txt 退出 0：真实 Mailpit SMTP 收码、错误码尝试次数持久化、核验及创建幂等重放、凭证不可重复消费、建号密码哈希、首次改密与分管理员权限验证通过。测试使用合成账户，不代表腾讯云邮件验收。

契约检查通过：235 个操作及处理器，47 个 query、113 个 body，无静态豁免。数据库成功应用 53 个迁移。

剩余工作：现有分管理员页面接线、邮箱修改核验、验证码锁定和到期等扩展边界验证、三端浏览器操作及其他尚未完成业务。当前结果不代表三端全量验收通过。

## 页面接线进度

正式模式已接入后端分页列表，映射八项权限，加载失败沿用现有错误区域。保留原有组件结构及样式；真实账号对象不保存密码验证器，也不伪造创建时间。新增邮箱 OTP 请求、验证和建号客户端方法，尚未连接编辑弹窗。

本地 `node --import tsx --test tests/subadmin-api.test.mjs` 通过 1 项：分页、重复游标终止、权限拒绝传播、OTP 请求参数及幂等键、不可下放分管理员设置。`tsc --noEmit -p tsconfig.json` 退出 0。这是客户端协议测试，未替代 Docker 后端验证或浏览器验收。

## 停用与重新启用接线

正式模式原有停用/启用按钮现已调用状态接口；复用 AdminConfirm 展示账号及交接确认，页面结构和 CSS 未改变。使用列表版本，并在确认弹窗内保留同一幂等键供网络失败重试；成功后使用服务端返回的状态、版本、更新时间更新账号。

Docker 首次状态测试因固定版本 1 返回 409；首次改密已推进权限版本，测试改为读取当前版本。原始日志 docker-subadmin-status-wiring-20260908.txt 保留。复验 docker-subadmin-status-wiring-retest-20260908.txt 退出 0，包含交接确认必填、停用幂等重放、旧登录令牌 401、旧版本 409、重新启用和新登录成功，并重跑 SMTP OTP 建号链路。页面类型检查和客户端协议测试再次通过。

修复客户端中文 locale 从 zh 到接口 zh-CN 的转换，测试输入使用页面实际 zh 值。状态操作尚未完成浏览器点击验收；OTP 建号弹窗、资料编辑和分管理员删除仍待接入。

## OTP 建号页面流程

正式模式已开放原有新增按钮，原有表单校验后发送邮箱验证码；复用 AdminConfirm 和 AdminField 组件呈现验证码确认步骤，保留原页面和编辑表单布局、样式。核验成功消费凭证建号，使用服务端结果加入列表。密码只在表单内存中，正式模式不生成或保存浏览器密码验证器。

发送、核验、建号分别保留幂等键供结果不明时重试；错误验证码返回的新版本用于下一次核验。关闭验证码步骤返回原表单，可以重新申请验证码；后端发送间隔和过期检查继续生效。尚无浏览器点击证据，不据此宣称真实三端全量可用。

客户端测试增加核验版本和创建权限、凭证、幂等键的请求检查，1 项通过；页面 tsc 退出 0。后端没有修改，沿用上一节 Docker 成功证据。接续重点为浏览器实际收码建号、资料编辑、分管理员删除以及三端其他未完成业务。

## 真实浏览器验证

已刷新 backend-browser 镜像，53 个迁移应用于专用 v81_browser_test，后端 3199 与管理端 3300 均 HTTP 200。管理端使用 BNBU_LOCAL_BACKEND_ORIGIN 指向本地 Docker 后端。内置浏览器工具启动失败（kernel assets 路径不存在），改用本地 playwright-core 与已安装的 Edge 无头浏览器，未更改业务实现。

`node tools/local-integration/v81-subadmin-browser-probe.mjs` 退出 0，证据 browser-subadmin-otp-20260908.txt：真实总管理员表单登录、填写新增账号、选择审计权限、发送并读取 Mailpit 邮件、页面填写 OTP、创建成功、刷新重新查询仍存在、页面停用和重新启用均成功。使用合成身份；没有输出或存档实际密码、OTP、登录令牌。刷新后查询证明账号来自数据库，未使用 localStorage 演示建号。

浏览器依赖装在忽略目录 .local/browser-test，测试默认使用本机 Edge 路径，也可通过 BROWSER_EXECUTABLE 指定。管理端开发服务会话 84103 继续运行，URL http://localhost:3300/；Docker 后端持续运行。该结果覆盖分管理员建号和状态操作，不覆盖资料编辑、分管理员删除或其他两端全部业务。

## 资料更新与邮箱变更后端

新增 POST /admin/subadmins/{id}/profile，总管理员在 NORMAL 模式下更新同组织分管理员的姓名、部门、邮箱和权限。账号、密码、启停状态保持原值。使用权限版本及幂等键，将资料、权限、凭证消费与无邮箱明文的 PROFILE_UPDATED 事件在同一事务提交。邮箱改变时必须提供属于当前组织和操作人的已验证、未过期且邮箱完全匹配的凭证；不变时禁止附带凭证。空权限列表允许收回全部可下放权限。

Docker 日志 docker-subadmin-profile-email-20260908.txt 退出 0：资料修改与重放、旧版本拒绝、教师拒绝、未验证邮箱拒绝、权限立即收回、邮箱凭证不匹配拒绝及成功消费、登录账号和密码哈希保留验证通过，同时重跑建号及停启用链路。当前 236 个操作通过契约检查，53 个迁移，无新增迁移。新资料接口尚未连接页面，持续运行的浏览器后端需再次刷新镜像后才能验收该接口。

浏览器新增错误验证码后更正并成功创建，以及停用、启用后分别刷新查询的验证，browser-subadmin-otp-retry-20260908.txt 通过。该浏览器结果仍基于已有建号与状态接口，不代表资料编辑通过。

## 资料编辑页面验收

资料编辑已接入现有表单。姓名、部门和权限修改直接调用 profile；变更邮箱复用已存在的 OTP 弹窗，核验成功后提交。编辑允许收回全部权限，创建仍要求至少一项权限。登录账号输入保持禁用，编辑请求不携带密码或登录账号。相同更新内容重试复用幂等键，修改内容产生新键。

已刷新持续运行的 Docker 浏览器后端。browser-subadmin-profile-20260908.txt 退出 0：浏览器表单修改姓名/部门、收回全部权限、刷新查询、修改邮箱并读取真实 Mailpit 邮件、OTP 核验保存、再刷新读取新邮箱全部通过，同时重跑建号、错误码更正、停用与启用。页面 tsc 退出 0。测试使用专用本地数据库合成身份，不代表云环境验收。

分管理员建号、资料修改、邮箱核验、权限收回及状态操作已具备上述浏览器证据；分管理员删除仍待完成，其他三端业务范围以全量验收清单继续推进。
