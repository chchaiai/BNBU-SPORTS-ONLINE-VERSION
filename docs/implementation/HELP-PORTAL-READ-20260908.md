# 管理员帮助读取接线（2026-09-08）

现有正式帮助页面仍通过 admin-service 调用旧 `/help-articles` 路径。新增 help-api.ts，页面改用 `/admin/help-articles?status=published&page=...` 管理员接口，循环取得所有分页并按语言选择标题及正文。页面原有 DOM 布局、样式、筛选和只读功能保留，时间显示使用服务端真实 updatedAt，不伪造发布时间。

Docker 命令退出 0：`docker-help-portal-first-20260908.txt`。测试实际加载网页 API 模块，以管理员认证请求真实后端；新增六篇已发布测试文章，逐项核对中英文、内容、更新时间并确认读取第二页。帮助发布/下线/重发布、学生真实邮件登录、学生网页 API、跨组织与分管理员权限回归一并通过。

`help-portal-typecheck-20260908.txt`：Portal tsc 退出 0。本轮未执行浏览器界面测试。运行在隔离 Docker 测试库，没有执行云端操作。

剩余：管理员帮助创建、编辑、发布等原有演示操作仍需接入真实 API，当前真实页面继续只读；不能据此声明帮助管理页面或三端全量完成。当前页面旧说明文案保留，完整管理接线时需同步更新。
