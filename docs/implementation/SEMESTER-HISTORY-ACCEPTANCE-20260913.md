# 有课程历史的生产学期切换验收

状态：待执行。主合成组织的 10 门课程存在后续验收依赖，不用于成功切换样本。

浏览器成功确认已完成：独立组织通过正常密码登录，目标 `01a09925-662c-7042-900c-a9765ea178f3` 在页面点击确认后变为 CURRENT，上一学期 ARCHIVED，列表显示 0 个待开始、3 个历史学期，历史课程/学生 1/1 仍可见。临时管理员已停用。目标由 HTTP 准备，因为浏览器自动化日期 fill 未保留日期，创建表单因此拒绝；此项不算创建表单成功证据。顶部全局当前学期似未同步，需要继续核对。见 `semester-success-browser.json`。

管理员浏览器阻塞分支已验收：主合成组织显示当前学期 10 门课程/6 名学生，归档学期只读；切换下一学期弹窗显示开始日期、未结算和待处理事项三个阻塞，确认按钮禁用，取消后正常关闭。见 `semester-blocked-browser.json`。成功切换已有独立组织 HTTP 证据，浏览器成功确认仍未验证。

归档后的新增操作拒绝已验证：`semester-archive-denials-v2.json` 中学生新建运动、教师新建补练均返回 409 `CONFLICT_STATE_TRANSITION`，运动会话数量未增加、补练数量仍为零，账号最终停用。首次预期学生返回时间窗口错误，但源码核对证实先命中 `requireUnsettledCourse` 的结算保护，保留初次失败证据。该结果证明已结算归档课程不能新增业务，不宣称单独隔离验证了学期日期条件。浏览器切换流程仍待验证。

正式结算和历史保留 HTTP 验收已通过：`semester-history-settlement-v2.json` 包含正式报告确认/重放、已结算单课程学期归档/重放、原报告逐字段不变、导出字节一致、成员行不变、唯一当前学期。目标学期 `01a0991d-914e-733b-a516-ac832509923a`，报告 `01a0991d-90da-7749-bf85-0e369919ab48`。管理员和教师最终停用。历史规则日期为显式样本初始化，业务命令通过生产 HTTPS 执行。初次因在启用合成总管理员前写模板触发保护并回滚，保留 `semester-history-settlement.json` 失败证据；调整同事务顺序后通过，未修改保护规则。浏览器切换流程和归档后新操作拒绝仍待验证。

正式名单及体测已完成：公开 HTTPS 上传 CSV 到真实 COS，源文件回读字节完全一致；名单 `01a0991b-359f-746e-8918-60b6304fb294` 已正式确认，学生 1000m 原始用时 300 秒已保存。四项检查通过，测试结束后责任教师重新停用并递增 tokenVersion。证据 `semester-history-roster.json`。规则、正式结算报告及学期切换仍待执行。

学生基线已准备：独立组织当前学期仅保留 `01a09918-10d1-735e-8790-0176abb8e77a` 一门课程。两门无学生、无运动记录的样本空课程移入该组织原有历史学期。新增 `TEST SEMESTER HISTORY / 9900000099`，账号停用、未建立会话；邮箱验证是显式合成初始化，不能视为真实邮件验收。成员及学生标识见 `semester-history-student.json`。正式名单、体测、成绩、结算及切换仍待执行。

初始化进度：生产已新增主样本组织 `01a09918-10d0-75ae-93a0-a4bf948d2495` 和隔离对照组织 `01a09918-10d1-735e-878f-c4e38e791c8a`。4 个账号均在创建事务内停用，没有建立认证会话；随机密码未输出或存档。具体新增标识见 `semester-history-fixture.json`，后续复用这些对象，不重复创建。第一次因小写组织代码违反格式约束整体回滚，修正为大写后通过。名单、学生、正式结算及切换尚未执行。

已核对可复用逻辑：`backend/test/e2e/v81-settlement.e2e.test.ts` 的单课程结算场景，以及 `tools/local-integration/v81-settlement-http-probe.mjs` 的 `V81_SETTLED_SEMESTER_SWITCH` 分支。不能直接对生产运行测试入口，因为入口包含 `resetFoundationDatabase`；生产脚本必须只创建独立组织，并明确记录其所有新增标识。

具体验收对象：一个独立合成组织、责任教师、管理员、已验证合成学生、旧学期、一门课程、已确认名单、原始体测、内部成绩及正式结算报告。历史截止日期属于显式合成初始化；结算确认、学期切换和回读使用真实服务器时间与公开 HTTPS。

必须取得的证据：

1. 结算前保存报告内容、哈希、导出内容和成员行快照。
2. 目标学期已到开始日期，switch-check 返回全部课程可切换。
3. 正式切换与同键重放一致，旧学期归档且组织仅一个当前学期。
4. 原报告逐字段不变，导出业务行不变，成员和学生历史不变；管理员结算摘要仍可读取。
5. 归档后普通新运动和补练被拒绝，原责任教师仍可读取旧事实；不借验收增加接管权限。
6. 合成会话结束并停用测试访问，留下明确的最终清理清单。

业务待确认：代码将名单内未注册者及缺少体测且无获批免测者列为 UNAVAILABLE。现行文档要求保留未完成原因，但未明确这两类事项能否由教师说明原因后结算；已向用户提问。本独立完整资料样本不依赖该答案。

此文件为经源码核对的执行边界，不是生产成功证据，不替代待执行的 HTTP 或浏览器验收。
