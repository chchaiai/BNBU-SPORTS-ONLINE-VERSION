# 正式结算与学期归档统一 E2E（2026-09-08）

新增 v81-settlement.e2e.test.ts，实际 HTTP、Docker PostgreSQL 18.4、严格运行时 OpenAPI 校验。最终完整场景通过，退出 0。

前置边界：在业务事实写入前将未使用课程移出当前学期，形成一门课程的测试学期；历史已发布规则和名单来源由合成夹具入库，数据库白名单只增加明确的 bnbu_sports_test，且要求临时数据库确认常量。名单确认、两名成员体测事实、教师内部成绩、正式结算与切换均走 HTTP。此场景不验证名单源文件真实上传。

验证缺少体测事实时不可结算，补事实后预览指纹变化，过期预览拒绝；管理员和其他教师不能确认，非法报告注入拒绝；并发确认只有一份报告，同键重放一致。教师列表/详情/导出读取成功，报告与摘要满足 schema；管理员摘要不披露学生行，跨组织拒绝、COURSE_VIEW 撤销即时生效。结算后普通体测写入拒绝。

正式报告使切换预检反映结算状态；维护期间切换 503 且状态不变，恢复后切换并可重放。归档后报告、导出结果文件名、管理员摘要、入班历史保持，当前学期唯一。报告存有教师内部最终成绩 123；学生归档后可读本人结算结果，响应不含分数、操作者、姓名、学号或排名字段，其他学生 404，教师/管理员不能代读学生接口。

首轮 docker-settlement-e2e-first-20260908.txt 失败于管理员跨组织摘要 404 漏声明。补该 404 及维护期间学期切换 503，docker-settlement-e2e-retest-20260908.txt 通过；增加学生隐私断言后 docker-settlement-e2e-final-20260908.txt 仍通过。

生成检查和 249 操作/249 handler 对应检查通过。没有 UI、结算业务算法或数据库约束修改。本场景未逐单元格检查导出文件，不代表浏览器结算和全部历史更正已经验收。全量门禁最新仍第五轮 75/75 用例通过、107 项覆盖缺口，需在后续统一运行汇总。
