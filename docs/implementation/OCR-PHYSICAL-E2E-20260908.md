# OCR 体测识别到正式结果 E2E（2026-09-08）

统一OCR链路继续处理之前实际上传的体测批次，通过HTTP排队、真实worker调用模拟腾讯表格响应、HTTP创建草稿和教师修订后正式确认。草稿没有直接写数据库。Docker完整场景复测1/1通过。

验证女生800米表格识别为一行草稿；未核对确认422，学生确认403，其他教师读写404，正式结果仍NOT_RECORDED；教师对照原图核对形成草稿版本2；用旧版本1确认409；正确确认后pendingCount=0，原key重放一致、新key再次确认409；学生正式结果为800m、270秒、2026-09-08、版本1；数据库来源关联草稿2到结果1，通知恰好一条。之前OCR名单、切换、成员投影和导出场景同时通过。

首轮跨教师404未声明，补getV81OcrPhysicalConfirmation/confirmV81OcrPhysicalRows并加读隔离验证；249操作/249handler静态对应通过。日志：docker-ocr-physical-e2e-first-20260908.txt、docker-ocr-physical-e2e-retest-20260908.txt。未修改UI布局。

云提供方仍为模拟返回，原图存储为内存适配器，worker显式执行，学生成员/性别为合成夹具；本轮不是云OCR质量、实际体测或COS验收。原始图片到业务草稿及正式事实的HTTP/数据库链路已验证，其他业务和三端浏览器继续。全量门禁最新仍第七轮70项缺口。
