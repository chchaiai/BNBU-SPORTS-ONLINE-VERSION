# 结算内部命令与事务验证

日期：2026-09-08 13:34。依据教师流程第 12 节，继续实现正式结算。完整三端目标仍进行中。

## 已实现

新增 V81SettlementReportsService.confirmInTransaction。校验责任教师、组织、系统模式、未归档学期、结算时间与全部已实现待办检查；要求 Serializable 事务，按组织和课程加锁。教师确认的综合名单指纹覆盖除生成时间以外的预览事实，数据变化返回版本冲突。

检查通过后在同一事务追加第一版报告与审计事件，关联来源名单、确认指纹与数据库计算的报告摘要。已有报告拒绝重复初次结算；不覆盖报告。缺注册/体测的未决业务政策仍按预检查 UNAVAILABLE 阻止。

该服务尚未注册到 HTTP 控制器。开放前必须补全结算后的普通写入限制、受控纠错入口、幂等外层和协议。现有 GET 结算检查仍明确 ready=false，不把内部实现或合成测试呈现为已结算业务。

## Docker 验证

- docker-settlement-command-first-20260908.txt：构建、57 项迁移、原名单/权限回归及内部命令首次验证通过。
- docker-settlement-command-boundary-20260908.txt：补充计划时间前一毫秒拒绝后通过。
- docker-settlement-command-serializable-20260908.txt：增加隔离级别强制检查、改用 Serializable 测试事务后通过。

真实 PostgreSQL 验证管理员及其他教师拒绝、体测追加后旧预览指纹拒绝、新预览生成快照、原始体测版本保留、数据库规范 JSON 摘要一致、审计摘要对应、重复初次结算拒绝。事务最终主动回滚并断言报告和临时体测没有持久化。

测试使用注入的未来时钟和合成学生，未在持续浏览器库生成正式报告。首次日志沿用旧的“read-only test”标签，当时内部写入同样在回滚事务中；后两份日志已改为准确的 synthetic transaction test。三次进程均退出 0。原始日志保留工具空白；源代码和手写文档单独进行空白检查。

## 下一步

1. 在运动/入班/申请/名单/体测/成绩/认可等普通写入口统一执行结算状态限制；旧事实纠错必须追加原因及报告版本。
2. 完成结算预览指纹、幂等确认、版本读取和导出协议与端点，再接线原页面合适入口。
3. 学期切换必须检查所有课程真实报告和待办，学生仅回读本人脱敏结果，管理员仅治理摘要。

当前没有新增迁移、协议操作或 UI 布局修改；腾讯云正式环境验证仍是后续依赖。
