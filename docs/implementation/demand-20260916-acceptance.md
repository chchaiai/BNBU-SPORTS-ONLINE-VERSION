# demand.md 逐项验收与部署记录

更新于 2026-09-16。当前发布目录 `/opt/bnbu-sports-production/releases/demand-20260916-final`；旧版本 `student-origin-20260916` 保留。本文取代进度台账中较早的状态，不把历史阶段结果当成当前结论。

## 已确认业务边界

- 新绑定邮箱严格要求小写字母加九位数字，保留历史已验证学校邮箱登录。
- 新打卡由教师打卡日期控制，结束日期不得越过学期结束。已开始运动、既有材料、审核与反馈继续按各自规则处理；结算检查有效待办。
- 首次正式专业即确认；未分流可自主确认一次。USER_ACCOUNTS 管理员按原因、版本及审计纠正，不恢复已用机会。
- 手机和平板可用，电脑阻止进入学生登录；设备判断属于产品限制。
- Outbox 仅提供当前组织的真实查询与诊断，不自动消费或补发历史事件。

## 逐项状态

证据根目录：`evidence/demand-20260916/`。`browser/` 为本机真实服务及 PostgreSQL/MinIO/Mailpit，`online/` 为正式域名、生产服务和独立合成组织。

|需求|实现与影响|本地及 Docker 证据|线上证据与边界|
|---|---|---|---|
|12 错误体系|客户端保留业务原因、下一步、错误码和每次请求编号；上传、导入、课程等路径复用安全错误投影；后端关联日志脱敏|错误投影及日志单测、HTTP 错误用例、完整接口响应校验|`online/http-result.json` 记录拒绝响应及请求编号；`online/log-correlation.json`：5 个请求编号均找到对应后端日志|
|14 邮箱|前后端新绑定、入班和验证消费均严格校验；历史登录使用兼容校验，无批量数据修改|邮箱单元、登录/入班/换绑 E2E，Mailpit 真实收件|非法新绑定 422；未向真实学校邮箱发送验收邮件，历史兼容由自动测试及部署摘要证明|
|16 时间|去掉独立常规截止 UI/API 必填要求；领域、开始运动、旧草稿提交、补练、历史记录、提醒及结算同步；保留旧数据列兼容|日期领域、HTTP 跨午夜及学期结束、完整结算/补练 E2E；迁移 0082|英文设置中旧字段消失；已发布日程更改仍受原状态规则拒绝；线上维护中的既有运动继续提交并由教师审核通过|
|17 上传进度|照片、视频、PDF 显示实际传输字节；区分读取、确认、处理、成功和失败；修复打卡进度状态未渲染|`browser/upload-result.json`、`video-result.json`，真实摄像头模拟录制→MinIO→提交|`online/upload-result.json`：PDF/PNG 两次 COS PUT 200、提交 200；`video-provider.log`：真实 COS、ClamAV、FFmpeg 通过|
|18 未分流|复用资料流程，服务端持久确认事实、并发和权限约束；完整未分流资料增加确认入口|专业/资料单元、HTTP，浏览器未分流→JC、入口关闭|`online/http-result.json`：一次确认、重放、重复修改拒绝、管理员纠正、锁定仍保持|
|19 设备|启动和渲染入口限制电脑；手机/iPad 兼容；登录前显示明确说明|桌面无验证码请求，手机和平板登录入口通过|`online/browser-result.json` 桌面拦截；移动上传已通过。使用浏览器设备模拟，未宣称实体设备验收|
|20 维护恢复|ACTIVE 上传凭证仅可延长；已存在对象不按 PUT 凭证过期清理；恢复原草稿、表单、对象关联并重新获取状态|真实 MinIO 超过 301 秒应用时钟的 E2E、本机完整链路；浏览器实际维护切换后原表单提交、两文件仅两次 PUT|`online/recovery-result.json`：实际等待 309 秒超过凭证期限，原对象确认、关联、提交及教师审核通过（计入 7 分钟）；无需重新上传；正式组织保持 NORMAL|
|21 事件排障|新增组织隔离 API 和真实管理页入口；AUDIT_QUERY 权限；微秒游标、类型/状态/时间过滤；同口径 backlog；无敏感 payload|HTTP 权限、隔离、分页、脱敏、计数及浏览器真实入口|`online/admin-outbox-result.json`、`http-result.json`：入口、筛选、分页、计数、教师拒绝通过；没有消费者的状态不伪造为最终失败|
|22 JC|SCC 增加 JC；SAI 合并目录同步，所有学院可选未分流|双端目录及资料校验；浏览器选择 JC|线上实际确认 JC 成功；无历史专业批量重写|
|23 课程改名|复用责任教师 PATCH 教学班；不改共享 Course 目录；师生及管理员投影同步；保留版本、幂等与审计|完整 teaching-structure E2E、浏览器真实保存与刷新|`online/browser-result.json`：PATCH 200、刷新名称保留；学生线上页面也显示新名称|
|24 英文课程设置|补齐设置子模块、动态提示与翻译资源|语言测试、Portal 构建、本机英文无系统中文、中英往返|`online/course-settings-english.png`、中文截图及 `browser-result.json` 通过|

## 测试汇总

- 后端单元 322/322，Contract 测试 35/35，Docker 数据库集成 60/60。
- 本机与 Docker 安全测试均 53/53；Docker 完整 E2E 144/144。
- 真实响应覆盖：231/231 启用接口成功、264/264 错误/权限、33 个停用接口拒绝。清单计数修正后对同一原始响应记录重新校验通过，没有豁免接口。
- 学生端 69/69，Portal 140/140；前后端类型检查及最终构建通过。
- Windows 本机真实媒体、维护及结算 13/13；FFmpeg/ffprobe 仅为验收进程补齐。
- 浏览器发现并修复三个真实问题：完整未分流资料没有确认入口；Outbox 组件误放预览分支；打卡上传状态更新但没有渲染。修复后分别重新操作通过。最终视频验收还断言成功页清除过期的“正在提交”提示；单文件静态修正另存新 release，两个应用容器未重启。
- 脚本初次失败中的夹具格式、接口状态码和页面异步等待已按实际契约修正。阶段日志保留，最终结果与最初失败区分。

## 部署、数据与回滚

- 发布前读取服务器基线并比较文件，排除纯换行差异。发布叠加 29 个后端编译文件、14 个学生资源及已测试 Portal 构建，SHA-256 清单在 `release-bundle/validation.json`。
- 数据库备份：`/opt/bnbu-sports-production/backups/before-bugfix-20260916T080549Z.dump`，25,341,494 字节，备份目录验证通过。
- 0081 只调整上传会话过期续期守卫；0082 调整补练日期守卫。无删除表、批量重写学生或回放事件。
- 新旧镜像、发布目录均保留。部署脚本在健康/摘要检查失败时切回旧目录和固定镜像；不自动回滚数据库。
- 线上验收只修改 DEMAND16 独立合成组织，保留审计及测试材料。验收结束后 5 个合成账号已停用，合成组织及正式组织均为 NORMAL，见 `online/cleanup-audit.json`。
- 本地存档须包含本轮源码、必要测试和已验证依赖基线；预先存在的无关文档、其他发布包及私密会话不纳入。

## 保留边界及未完成项

- 正式 Contract 发布基线仍为 `2.0.12-contract`，运行契约为既有 `3.0.0-v81-local-draft`。结构、权限、枚举、OpenAPI、处理器一致性及运行响应校验通过；完整 formal compatibility 门槛仍因既有版本差异停止。本轮未改写旧发布快照。
- 无实体手机/平板测试；有真实浏览器设备模拟、真实服务与对象存储测试。
- 完整 `npm run lint` 未通过：原配置对历史 `test/profile-quality-http.mjs` 使用类型规则但未提供类型服务；临时按现有脚本规则诊断后得到 720 个报错，包含既有文件和本轮文件中的返回类型、未使用变量、测试替身 async 等。诊断后恢复原配置，没有关闭全局规则。功能测试、类型检查和构建通过不等于 lint 通过。该静态检查欠项如实保留，不影响上述已实际完成的业务链路。
- 本地精确存档的提交号由最终交接提供。私密会话与本机运行时仅保存在 `.local/`；源工作区仍保留其他任务的未提交文件。
