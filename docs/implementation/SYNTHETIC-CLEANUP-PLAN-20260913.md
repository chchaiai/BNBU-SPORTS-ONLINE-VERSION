# 合成测试清理准备

状态：只读清单已取得，尚未执行最终删除。

生产证据：`evidence/ocr-triplatform-20260913/synthetic-cleanup-inventory-v2.json`。脚本将事务设为 READ ONLY，单条语句超时 3 秒、事务超时 30 秒，仅统计明确的四个合成组织。读取了 111 张带 organization_id 的表，其中 86 张有目标数据；记录 364 条数据库外键定义用于后续依赖审查。

## 保留范围与账号

| 组织 | 用途 | 当前账号状态 | 删除依赖 |
| --- | --- | --- | --- |
| 01a096c2-20a2-706b-8c69-802f67dee12c | 主三端联调 | ACTIVE 7，PENDING_CONTACT_BINDING 2 | 真实邮箱、上传、下载、最终关键链路回归 |
| 01a09918-10d0-75ae-93a0-a4bf948d2495 | 有课程历史的学期切换 | DISABLED 4 | 保存最终历史证据及清理审查 |
| 01a09918-10d1-735e-878f-c4e38e791c8a | 上项隔离对照 | DISABLED 1 | 同上 |
| 01a098da-7a63-72c8-8e68-8209f304250b | 空课程学期切换 | DISABLED 1 | 清理审查 |

本清单不是全部曾用合成组织的穷尽证明。执行前还需将既有 fixture / evidence 中的组织 ID 与本清单核对，显式补齐已证明属于本次测试的对象，不能用名称模糊匹配决定删除。

## 存储对象

45 个不同的数据库引用键：运动媒体 30、OCR 图片 5、电子体测源文件 4、名单源文件 6。OCR 从 source_manifest 提取；体测源文件按现行 sourceKey 实现由组织、批次、摘要、格式确定；其他两类使用数据库键。

2026-09-13T05:19:27Z 已用现有 CVM 角色对这 45 个键逐个 HEAD，全部取得明确结果：44 个 EXISTS，1 个 404 ABSENT；存在对象共 5,867,985 字节。存在项为媒体 29、OCR 5、电子体测 4、名单 6，见 `synthetic-cos-head.json`。本次仅 HEAD，没有下载、上传、删除或扩权。

缺失项 `01a097a4-2975-710e-86f5-2b8e27f152b3` 当前为 PENDING_UPLOAD，uploadedAt/boundAt/availableAt 均为空，运动正式材料和免测关联均为 0；有一条游泳接收材料关联。历史 `cloud-swim-transfer-expiry.json` 明确它是超过 24 小时上传拒绝样本中的 AFTER 图片，并记录上传未完成、需最终清理。此项符合已知未上传样本，不是已 AVAILABLE 媒体丢失证据。当前状态与关联见 `missing-synthetic-media.json`，清理时需一并处理游泳接收子项。

HEAD 只证明清单内对象的当前状态，不能据此断言整个测试对象集合恰有 45 个键。未完成上传的残留、已删除业务记录的对象、分片上传及无数据库引用对象仍需检查。数据库还存在运行归档、学生媒体擦除记录的键列，清理时必须核对归档与擦除队列的职责。

## 最终执行门禁

1. 完成使用样本的验收，冻结明确 ID 和完整对象键清单，记录生产版本与备份可恢复证据。
2. 先停用测试访问并使会话失效，保留用户真实账号与验证码用途的必要记录。
3. 审查外键和不可变/审计触发器。无 organization_id 的修订、页面尝试等子表需通过已冻结父 ID 定位，不能遗漏。不得为清理关闭生产保护规则。
4. 使用现有业务删除流程；如其不能处理独立测试组织，先提供具体清理方案及影响范围，必要的生产策略变更须获授权。
5. 按明确键处理 COS 对象，保留每项结果和失败重试清单；不得整桶或泛前缀删除。
6. 回读数据库关联及 COS 对象状态，并验证真实组织未变化、三个 Web 入口与服务健康。对按业务规则保留的审计/历史数据明确标为保留，不能计为已删除。

当前没有删除数据库行、COS 对象或消费 outbox。主组织的待验收账号继续保留。清理不是整体完成的替代条件；需先补齐业务验收。
