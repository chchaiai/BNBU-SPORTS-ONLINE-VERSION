# Portal 学期接口适配

2026-09-08。新增 semester-api.ts：分页读取学期列表，保存学期配置，读取切换预检查，以及固定确认版本和幂等键的切换意图。意图会合并同时发起的请求，失败后同一对象重试保留原请求；不会自动重新预览并提交新的版本。

docker-semester-portal-client-first-20260908.txt 退出 0：在正式结算到归档测试中加载实际 Portal API 模块，通过真实后端读列表及预检查，切换成功后故意丢失响应，再调用同一意图两次。两个调用复用一个 Promise；两次 HTTP 使用完全相同的请求体和键；列表回读新当前学期。报告、导出和成员历史检查继续通过。测试只在传输包装中补入测试管理员令牌，不代表真实浏览器登录或点击验收。

Portal 的 `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` 退出 0，semester-portal-client-typecheck-20260908.txt 原始输出为空。未运行完整前端 build 或全部测试。

现有 admin-semesters.tsx 在正式模式仍走只读分支，原切换函数是演示状态变更。本包先完成真实适配，尚未将页面连接到新模块，也未改变任何页面布局。后续应复用既有学期列表和确认框，展示真实阻塞，保留待确认意图，并完成实际页面验证。完整三端目标未完成。

新增意图目前保存在调用方对象中，未实现整页刷新后的持久恢复；页面接线不得宣称已支持跨刷新恢复。配置保存 API 已提供但本包未单独用 Portal 模块验证，其后端已有专项。源码和手写文档单独检查空白，原始 Docker 日志保留。
