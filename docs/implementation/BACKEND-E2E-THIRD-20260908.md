# 完整 Docker E2E 第三轮（2026-09-08）

执行：专用 bnbu-v81-full-integration Docker 项目，PostgreSQL 18.4 临时测试数据库，57 个迁移，integration-tests 镜像重新构建后 npm run test:e2e。未连接生产数据库。

结果：12 套件、65 项用例全部通过，0 失败。随后运行时全接口覆盖门禁失败 136 项，整个命令退出码 1。因此本轮不是整体 E2E 门禁通过，也不是三端功能全部验收通过。

原始证据：docker-backend-e2e-full-third-20260908.txt。保留此前两轮完整失败日志及各专项失败日志。

本轮通过套件：认证 11、客户端 10、进班 5、运动记录 8、审核 5、运动会话 4、导出审计 4、材料 4、名单 2、公共错误 1、内部最终成绩 3、教学结构 8。

门禁剩余：运行时清单仅 126 个操作，当前 OpenAPI 为 249 个操作；部分旧业务已按现行规则禁用但清单仍当作启用；许多 V81 操作没有纳入这次统一 E2E 的成功请求证据。136 项为检查失败条目，不等于 136 个产品缺陷。既有专项验证不能自动替代本门禁证据；应核对权限与业务边界、补齐统一场景和清单，不降低校验。

成绩变更见 SCORE-E2E-CURRENT-20260908.md。三端总体验收还包括 Draft 规范诊断、实际页面业务覆盖和剩余依赖；当前不宣称腾讯云生产联调完成。
