# 第九轮全量 Docker E2E（2026-09-08）

执行基线 4de11bb5。独立 bnbu-v81-full-integration Docker 环境、PostgreSQL 18.4、57 项迁移、真实 Nest HTTP 与严格响应协议 hook。

90/90 测试、24 套件通过，0 失败/跳过/取消，测试耗时约 114 秒（不含构建）。249 个操作全部有错误或访问控制响应证据；206 个启用操作同时有成功和错误证据，17 个既有停用操作有拒绝证据，合计 223 个运行时协议验证完成。1456 条有效响应、0 条非法响应。

覆盖门禁仍失败，进程退出 1：26 个启用分类操作缺少成功证据，加上 manifest 仍以 126 而非 249 计数的两项错误，共 28 项。日志中重复输出是测试层检查与 --write 检查各一次，不是两轮执行。前一轮 87/87、40 项缺口保留。

剩余：名单注册预览、运行日志归档创建/读取/取消/下载凭据/下载、邀请撤销、结算后成绩及体测更正；以及旧课程写接口、旧成绩规则/分数/调整流程和已暂缓的学生注销挑战入口。后者必须按现行业务文档与用户决定核对实现和清单，不能仅为通过门禁修改分类。管理员权限守卫未分配 createCourse/updateCourse；旧成绩服务仍含实现，不能直接据缺失测试认定全部停用。

证据：docker-backend-e2e-full-ninth-20260908.txt、runtime-conformance-ninth-20260908.json、runtime-conformance-ninth-20260908.md。内存对象存储、模拟 OCR 提供方、合成学生/会话/材料等边界详见专项记录。真实三端浏览器、对象存储/邮件链路及最终交接仍需继续核对；此结果不代表全部三端功能或腾讯云部署已验收。
