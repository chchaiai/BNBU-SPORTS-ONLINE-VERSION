# 原始体测网页与实际日期校验

2026-09-09。用户批准用时、实际日期最小调整。原成绩弹窗接入原始体测独立保存，按学生性别显示项目，分钟/秒和实际日期作为原始事实提交；已有免测时禁用录入。最终 INT 成绩继续由原按钮保存。读取体测历史、分页及课程结算状态；已结算时展示更正原因并调用 corrections。该更正 UI 本轮未另做完整浏览器验收，不以普通保存代替更正证据。

按用户与 enrollment 保存 sessionStorage 意图，未知结果重放同一路径、请求和键。刷新后读到新版本也重放原键，避免再追加一版。已知校验/状态/版本冲突要求刷新资料。密码或令牌不进入该存储。

浏览器发现实际缺口：2999-01-01 原先得到 201。后端 V81PhysicalResultsService 增加服务器 Clock + 学校时区当前业务日期校验，晚于今天返回 422；直接写入、结算更正、电子表格与 OCR 确认共用该写入方法。只补充现有“实际测试日期”含义，未增加学期日期范围或分数换算规则。docs/backend-contracts/openapi.yaml 补充说明，已运行 generate-openapi.mjs，251 operations 不变；冻结 contracts 目录不变。

Docker docker-physical-date-20260909.txt：体测结果、体测导入、结算三套件共 5/5 通过，包括男女项目未来日期拒绝，既有导入与结算链回归。此后重建本地 backend-browser 并确认 healthy。physical-result-browser-retest3-20260909.txt 真实浏览器通过未来日期拒绝、保存 275 秒/2026-09-07、丢弃成功响应、刷新原键重放同版本、再次刷新读取 v3。Portal 类型检查退出 0。新学生为专门合成男性账号，使用 UUIDv7 与现有合成课程，未改真实账号。

失败证据：初次成绩表测试用学号定位，但该表仅显示姓名；按实际姓名修正。retest 首次发现未来日期错误被接受；坏日期成为合成学生 v1 历史，保留不回写，随后合法日期作为 v2/v3 追加。retest2 完成保存及重放，但最后断言使用 ISO 日期，页面本地化为 2026/9/7，故修正为精确历史版本标识并核对用时及日期输入值，retest3 通过。未删除失败记录或伪造全量通过。

仍待：结算更正 UI、免测/未知性别实际网页拒绝、学校午夜时间边界及大历史分页的补充验证。完整 Docker 全量待最后集成执行；教师和管理员通知入口继续。
