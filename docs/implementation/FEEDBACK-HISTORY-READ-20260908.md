# 反馈历史读取与当前账号资料隔离

2026-09-08：反馈管理列表和详情通过 v81_user_subjects 的学生来源关联保留反馈，当前 users/student_profiles/admin_profiles 使用可选关联。当前资料删除后，提交人姓名、学号、邮箱及不存在的管理员姓名返回 null，使用原协议已允许的空值。历史正文、回复、内部操作人标识及版本保留。详情在 RepeatableRead 事务内读取反馈和处理历史。

列表继续每页 6 条、按创建时间和编号倒序，保留分类/状态/编号/当前资料/正文搜索。查询全部参数化；搜索中的百分号、下划线和反斜杠作为字面字符转义。统计基于当前组织的学生历史主体，不再要求当前账号存在。

docker-feedback-history-read-20260908.txt 退出 0：构建及 52 项迁移检查通过。专项数据库建立 7 条合成反馈，其中 1 条通过真实管理员 HTTP 处理；删除合成学生的当前会话、资料和用户后验证：

- 两页共 7 条无遗漏或重复，汇总仍为总计 7、待处理 6、处理完成 1。
- 编号、正文、状态分类搜索正常；百分号和下划线按字面匹配；删除前邮箱搜索得到 7 条，删除后得到 0 条。
- 当前提交人三个资料字段均为空，详情其他内容与删除前一致。
- 数据库全部反馈字段和处理事件与删除前相同，当前用户确实不存在。

这是合成数据直接删除后的读取验证；没有执行注销确认，不验证用户偏好清理。对删除后的反馈继续处理、管理员本人删除后的名称空值分支、真实浏览器及全量回归仍待验证。原有失败保持未修复。未修改前端、协议字段或数据库迁移。

复现主专项：

```powershell
docker compose --env-file .local/v81-sql.env -f tools/local-integration/compose.v81-test.yml run --build --rm -e V81_FEEDBACK_HISTORY_READ=1 backend-startup
```

本轮修改查询后另运行 V81_FEEDBACK_TENANT_ISOLATION=1，输出见 docker-feedback-history-tenant-regression-20260908.txt。

跨组织回归退出0；8个双向越权请求404、搜索汇总与通知隔离通过。测试服务已停止，卷保留。

存档检查：明确暂存9个文件；git diff --cached --check退出2，仅两个原始Docker日志尾随空格，保留输出并记录未通过。
