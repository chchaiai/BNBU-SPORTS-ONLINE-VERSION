# OCR 名单成员状态与综合导出 E2E（2026-09-08）

在统一OCR正式名单场景后，显式准备一名与名单匹配的合成成员及一名额外成员；学生使用测试JWT。后续状态、汇总、规则发布、综合预览和导出均走真实HTTP，Docker第三轮完整链路1/1通过。此场景验证已注册成员关系的投影变化，不是实际注册操作验收。

验证本人名单状态MATCHED、registrationComplete=true、名单版本正确，跨学生404；管理员体测摘要未注册数由1转0，未录入数1；先发布有效模板及完整课程规则，综合名单来源OCR、registrationComplete=true、isSettlementSnapshot=false，名单内1人、额外成员1人；解析导出XLSX的说明表，来源类型与来源名单标识匹配OCR批次。

首轮本人名单状态404未声明，补getV81OwnRosterStatus；第二轮发布规则因夹具缺每日开放时间422，按已有业务前置补全天测试窗口；第三轮通过。旧v81-ocr-roster-member-probe仍使用学生密码登录及缺模板，当前统一测试直接采用测试JWT和正式模板接口，没有将旧探针宣称可用。日志保留docker-ocr-member-e2e-first/retest/third-20260908.txt。

本轮延续模拟OCR提供方、内存对象存储、合成身份边界，导出只核对来源说明并未逐格验收所有业务字段。未改UI布局。名单基准切换、OCR体测及整体验收继续；全量门禁最新仍第七轮70项缺口。
