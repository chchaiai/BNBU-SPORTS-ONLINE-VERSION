# 第七轮完整 Docker E2E（2026-09-08）

基于提交 ad2f2caa 的实现重新构建专用 Docker 测试镜像，运行真实 PostgreSQL、内部 SMTP Mailpit 与严格 HTTP 协议验证。20套件、84/84用例通过，0失败、跳过或取消，测试约93秒（不含构建）。人工审核/补证/进度/更正、男女体测和CSV/XLSX导入加入完整运行并通过。

1,213条响应有效，0条无效。249接口均有错误/权限响应证据；按当前清单，164启用接口具备成功及错误证据，17停用入口拒绝验证通过。覆盖门禁仍70项失败，进程退出1：68接口缺少成功证据，两项旧清单126/249数量不符，比第六轮少18项。

逐接口证据为 runtime-conformance-seventh-20260908.json/.md，原始输出为 docker-backend-e2e-full-seventh-20260908.txt。保留此前所有轮次；重复门禁输出来自运行器和报告写出各一次检查，完整测试只执行一次。

下一批重点：教师批量建号预览/确认/首次改密，名单当前业务投影，OCR配置与核对确认，认证及结算后更正，运行日志归档；同时修订旧版运行清单的数量与停用分类。学生注销仍按用户决定暂缓。完整三端浏览器最新版本、真实云服务依赖和全业务交接尚待收尾。局部内存存储或合成时间夹具的边界见各专项，不把测试全部通过等同于业务和部署完成。
