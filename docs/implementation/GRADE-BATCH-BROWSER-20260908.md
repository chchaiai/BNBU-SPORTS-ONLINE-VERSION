# 批量成绩部分发布恢复（2026-09-08 22:45）

基线 61209f3a，本轮未修改产品代码。浏览器脚本 v81-grade-batch-browser-probe.mjs 通过真实页面为两名 synthetic 学生分别保存整数 130、131，然后发起班级发布。

第一条发布返回成功；第二条发布由 Playwright route.fetch 提交到本地 Docker 后端后中断浏览器响应。错误出现后点击确认发布重试。断言：总请求数 3，第一名学生没有重发；第二名重试 URL、输入、幂等键相同，后端返回与第一次相同的完整版本对象；两名学生发布版本均等于各自草稿版本加 1。刷新页面后仍能读取两名已录入成绩。grade-batch-partial-browser-20260908.txt：PASS。

测试未模拟成功业务响应；使用真实 Docker 后端、PostgreSQL 持久化和本地 synthetic 账号，保留测试历史。无 COS/CVM 操作。

继续项：页面刷新/重新登录后的未确认请求恢复仍需验证；错误上报 /audit-logs/client-errors 后端无对应路由，故障场景 404 待处理。ACCEPTANCE.md 包含大量早期快照，必须按业务文档和当前实现逐项重新核对，不能作为当前完成声明。
