# 三端业务入口待办优先级

## 2026-09-09 用户确认后的执行状态

09:33 更新：六项均已接线并完成各自主流程浏览器验证；独立证据与尚存的关闭/历史、更正、学期归档阻断见 SIX-ENTRY-HANDOFF-20260909.md。下表保留最初排查状态，不代表现行实现。

用户回复“确认”，批准下表全部六项最小必要调整；保持页面整体布局。下表“待答复”为历史状态，不再作为阻断。先完成首次改密，再推进结算/导出、补练窗口、原始体测、本人名单与通知。首次改密已完成，详见 TEACHER-FIRST-PASSWORD-20260909.md。

2026-09-09 03:16，依据当前源代码、docs/business/20-teacher-flow.md 第 12、18 节及已有真实浏览器证据重新核对。完整后端通过不解决以下页面缺口。

| 优先级 | 业务阻断 | 当前源代码事实 | 所需动作 |
|---|---|---|---|
| 1 | 新教师首次进入工作台 | api-client 有 changeOwnPassword；portal-app 未接 mustChangePassword 门禁；已实测新账号 /me 403 | 复用原改密弹窗的最小调整待答复；完成首登、重试和正常进入 |
| 2 | 课程正式收尾与学期归档 | 教师页面没有 settlement-preview/settlement-reports 接线，也没有综合名单导出入口；现有下载为二维码和证据原件 | 原课程详情内预检、确认、报告/导出入口调整待答复，按真实阻塞项完成闭环 |
| 3 | 补练授权 | 原“补录学生学时”表单为手填分钟，真实模式拒绝；没有 makeup-grants 接线 | 指定学生起止窗口表单调整待答复；不能用直接增加学时替代补练 |
| 4 | 原始体测确认 | teacher-data 只有读取历史；真实成绩弹窗写最终 INT 成绩，不写原始用时/测试日期 | 用时和日期最小表单调整待答复；不得代填日期 |
| 5 | 本人名单核对结果 | 学生 api.js 有本人 roster-status 方法，之前浏览器确认页面无展示/无主动请求 | 在原课程区域增加本人状态行待答复，保持其他学生隐私 |
| 6 | 教师/管理员通知 | 共用样式隐藏图标、无列表/已读/跳转；真实浏览器失败已存档 | 显示原图标并复用弹窗待答复，完成任务定位和结果回读 |

这些调整会改变当前可见内容或交互，均已询问以协调用户原 UI/UX 限制。现有最终 INT 成绩输入框已经单独获准并实现；该批准不自动扩展到上述入口。不是要求重复批准普通技术选择。

本轮修正补录弹窗误导文案：移除过期“协议 1.2.0”和“真实模式可立即计入”暗示，说明须先授权窗口、实际运动及材料；新增对应英文翻译。未改元素/样式/布局，没有宣称实现补练授权。`makeup-entry-copy-typecheck-final-20260909.txt` 是最终 Portal 类型检查；本轮没有新增后端运行或业务通过结论。

这不是全范围剩余事项的唯一清单。其他非入口问题继续以 ACCEPTANCE.md 为准，包括合同发布差异、其他审计写入、刷新/跨身份边界，以及腾讯 COS/CVM/真实邮件和未配置 OCR 的外部依赖。先解决阻断主要业务路径的入口，再完成剩余边界，不以新增局部绿灯替代主流程完成。
