# 维护模式管理菜单补修

已部署 maintenance-navigation-20260914，源代码提交 10d12753。

上次遗漏：portal-app.tsx 除维护刷新跳转外，还有侧栏独立过滤器，将维护状态下的管理菜单裁剪为 system。此次删除该过滤器，使用完整管理员菜单；后端权限仍负责授权，未改变角色权限。

36 项回归通过（含实际菜单表达式在正常/维护状态的执行检查）、类型检查及构建通过。线上 9 个静态资源哈希通过；首页实际引用的资源也与新构建一致。后端容器未改变，无数据库迁移。浏览器自动化初始化失败，未完成真实浏览器点击验收；不能将源码逻辑测试视为浏览器交互验证。

已登录页面需刷新以加载新构建。保留上版 admin-maintenance-20260914 可回退。部署失败自动回退脚本已归档 tools/production/deploy-maintenance-navigation.py。测试与部署原始证据在同名 evidence 目录。既有并行修改保留。
