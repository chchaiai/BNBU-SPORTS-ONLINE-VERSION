# 管理员课程目录看板浏览器验证

2026-09-09。本地合成管理员实际登录 Portal localhost:3300，读取 Docker API 3199 的 `/admin/course-directory`。

`tools/local-integration/v81-course-directory-browser-probe.mjs` 对当前返回的全部课程逐项验证：

- 顶部课程数、去重学生数、去重教师数与服务器 summary 一致，卡片数量与 rows 一致。
- 搜索课程后，逐卡片核对有效学生、已提交学生、记录总数、有效/无效记录和计入时长。
- 原展开按钮显示学期、责任教师、有效/移出人数、两个目标、人均计入时长和入班开放状态，与服务器字段一致。
- 不存在的搜索词返回零卡片；清空搜索后，模拟目录请求断网，现有错误提示可见；恢复网络并点击原刷新按钮，重新成功回读服务器汇总，错误消失。

`course-directory-browser-retest-20260909.txt` PASS，退出 0。首次 `course-directory-browser-20260909.txt` 因脚本误用“课程目录”而非实际“课程目录看板”导航名称，未取得目录响应；失败保留。修正测试后通过，产品代码未变。

本轮只读，无数据库业务写入；沿用本地 Docker 实际服务，不是 Mock 数据成功。未新增后端全量运行，最新全量仍第二十轮 97/97。当前完成率字段仍按协议不可用显示；没有据此宣称全部目标完成率、无当前学期、并发快照、其他身份权限、移动视口或云端已验收。
