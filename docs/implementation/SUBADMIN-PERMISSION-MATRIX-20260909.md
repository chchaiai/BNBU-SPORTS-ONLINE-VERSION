# 分管理员八项权限专项 2026-09-09

在专用本地 Docker PostgreSQL 合成账号上运行严格运行时协议 E2E，docker-subadmin-matrix-20260909.txt 退出 0，2/2 通过。原 SMTP OTP 身份治理和现有会话撤权测试同时回归。

扩展 v81-subadmin-permissions-probe.mjs，使用同一个完成首次改密后的分管理员会话，由总管理员每次只授予一项权限。课程、学期、教师账号、反馈、换算表、系统模式历史、帮助中心、审计八个实际 HTTP 入口形成 8×8 检查：有对应权限 200，其余 403。每种单项权限均不能访问总管理员分管理员列表。最后清空权限，八个入口均立即 403。

每种权限还尝试一次有效的维护模式写请求：仅 SYSTEM_MODE 返回 201，并以该分管理员会话真实恢复 NORMAL；其余七项均 403。测试结束系统为正常模式。共 64 个读取矩阵断言、8 个撤权后读取断言、8 个总管理员入口隔离断言及系统模式写入/恢复检查。本轮未发现新的实现缺陷，无产品、协议、数据库或 UI 变更。

边界：单项授权矩阵不等于全部 256 种权限组合；除系统模式外的每类业务写动作未由本专项完整覆盖。使用合成数据库与账号，不代表真实学校或腾讯云验收。最新已完成全量仍为第十六次 94/94；本轮仅专项 2/2，不宣称已跑新增测试后的全量。首次改密和结算网页最小调整仍待用户答复，三端完整目标继续。
