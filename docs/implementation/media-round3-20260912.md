# 暂停、录像预览与本机草稿删除修复

暂停/继续原先等待 GET 和写入两个往返才更新界面。现在立即更新本地显示，并直接携带已知版本发送命令；仅 409 时读取服务器并协调重试。同步中禁止重复操作，失败恢复状态并提示。最终计入时长仍由服务器决定，不以本地反馈改写后端计时。

录像转换期间不再将原始、可能不可解码的视频交给播放器，改为显示处理进度或重试入口。正常刷新时保留同一份同一 URL 的播放器及播放状态；每次渲染都重新关联错误显示，防止消息消失但播放器已被替换。原件继续保留。

申请表新增“删除草稿”，清除当前账号本机未提交的表单和材料；已提交申请及补材料记录不受该本机按钮影响。服务端草稿不是本机清除操作的删除对象。

隔离 Docker 业务流程六项通过；学生 smoke 86 项通过。浏览器新增验证：请求延迟 1.5 秒时暂停/继续界面反馈小于 1 秒；处理中的录像无 video 播放节点；录制并兼容处理后实际播放，在六次刷新中保持节点身份并持续播放；删除草稿后 IndexedDB 记录不存在。见 evidence/media-round3-20260912/checks.json。

范围限于三个学生端 JS 文件，无数据库/Contract 变动。未取得用户失败视频原件，因此不能断言已覆盖所有厂商编码；本轮修复可复现的播放生命周期问题。iOS 真机仍待设备验收。

部署结果将在同目录证据存档；此前工作区的 26 项无关变动保留。

已部署源码 c54acd0d48e4b4538428b02d9731b1caff08f807，当前发布 media-round3-20260912；三个静态文件 SHA-256 和 JavaScript MIME 验证通过。学生及教师公开入口加载通过，无页面脚本异常。前一发布 media-round2-pdf-20260912 完整保留，可切换静态目录回退；本轮未改动服务端或数据库。
