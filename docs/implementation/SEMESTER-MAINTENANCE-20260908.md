# 维护模式拒绝已就绪学期切换

2026-09-08。docker-semester-maintenance-first-20260908.txt 退出 0。隔离 Docker 完成单课程正式结算且预检查 ready=true 后，通过真实系统模式接口进入维护；管理员以原版本和键提交学期切换返回 503，数据库全部学期状态、版本及时间与之前相同。finally 通过真实接口恢复正常模式，再以该请求及键切换成功，归档报告与成员历史保留检查通过。

补齐维护状态对已具备结算条件的切换写操作约束，不把未到日期或未结算的其他拒绝误当作维护验证。本包只扩展测试，产品实现未改动；持续浏览器环境未切换模式，此流程只在 v81_runtime_test 执行。原始日志保留，测试源码及手写文档检查空白。

完整三端验收仍未完成，继续成功切换的浏览器恢复、管理员课程结算摘要、归档后其他事实纠错及剩余页面业务。此专项不替代全范围回归。
