# 补练授权到运动会话 E2E（2026-09-08）

在补练窗口套件新增截止后运动场景，复用会话专项并采用真实本地邮箱 OTP 登录。为已过常规截止的合成课表补充有效规则模板引用，符合 0042 迁移；过期课表由测试直接入库，授权、登录、开始、重放、撤销、读取及取消均使用 HTTP。

Docker 复测补练套件 2/2 通过，退出 0。无补练授权时开始运动 409；授权有效窗口后 201 并可幂等重放。数据库记录开始时间不早于授权时间，业务日期采用学校时区的服务器日期，不接受客户端提交的七天前日期；窗口来源关联仅一条。撤销后已有会话保持 IN_PROGRESS，合法取消后再次开始 409，原关联继续保留。

首轮新增场景失败于旧探针取消请求多传 clientObservedAt，后端正确 422，但协议也缺少 422。保留非法请求断言并验证会话未变，再以正确 VersionedReasonBody 取消；补 cancelExerciseSession 的 UnprocessableEntity。未放宽输入验证。

证据 docker-makeup-session-e2e-first-20260908.txt（1/2）与 docker-makeup-session-e2e-retest-20260908.txt（2/2）。协议生成与 249 操作/249 handler 对应检查通过。本次无 UI 或业务实现变更。

边界：本场景测试开始与取消，没有等待实际运动时长、上传运动材料或验证最终计入进度；这些仍需结合记录、审核和进度场景。完整 E2E 最新仍第五轮 75/75 用例通过、107 项覆盖缺口，本次没有重跑全量门禁。
