# 体测纠错边界验证（2026-09-08）

本轮仅扩展 Docker 业务测试；产品接口、数据库结构和前端布局未变。

- 无原始体测结果的学生提交纠错返回 409；结果、通知、PHYSICAL_RESULT 审计均为零。
- 归档后的同一体测版本并发提交两个不同结果，返回 201 和 409；最新版本只增加一次，值与成功响应一致。
- 通知仅增加一次；原始报告和上一纠错报告内容保持不变，新报告引用成功版本并通过响应 Schema 校验。
- 同轮继续通过正式 HTTP 结算、学期切换归档、报告故障事务回滚、同键恢复与回放、角色及责任教师范围检查。

证据：`docker-physical-correction-boundaries-first-20260908.txt`，Docker 命令退出 0。测试使用隔离的 `v81_runtime_test` 数据库，课程、历史规则与无原结果学生由测试准备；结算、归档和纠错通过真实 HTTP 执行。

剩余：无原报告、免测以及学生本人读回的体测纠错专项仍需补验；持续浏览器后端尚未包含最近纠错实现，三端页面及全量验收继续。既有 Draft 合约 391 项诊断不在本轮修复范围。云端 COS/CVM/PostgreSQL 未执行验收。
