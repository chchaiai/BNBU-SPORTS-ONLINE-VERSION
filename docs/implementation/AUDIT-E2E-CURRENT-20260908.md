# 审计查询统一 E2E（2026-09-08）

将现有审计专项接入系统管理 E2E，并启用严格运行时 OpenAPI 响应校验。Docker 系统管理套件最终 5/5 通过，退出 0。

审计专项使用六条明确标注的合成事件验证：Asia/Shanghai 单日首尾边界、分页游标、请求号/操作/操作者/目标/结果组合筛选、未知操作名称保留、V81 与 FOUNDATION 两类详情、跨组织 404、教师 403、权限撤销即时生效；非法日期、超限页大小、变更过滤条件后使用原游标均拒绝。响应 safeMetadata 仅包含 version，不含合成密码、姓名或成绩标记；查询结束后六条原事件仍在。

首轮 4/5：旧专项断言操作者角色为空，与 0050/0051 迁移后的写入时角色快照冲突。按当前数据库规则验证 TEACHER，同时核对六条原始数据库快照。移除旧日志中与此专项无关且过时的 runtimeZipNotImplemented 声明。

第二轮 4/5：跨组织审计详情实际 404，但 Draft 协议未声明。补充 getV81AuditEvent 的 NotFound。最终 5/5。三轮证据分别为 docker-audit-e2e-first-20260908.txt、docker-audit-e2e-retest-20260908.txt、docker-audit-e2e-final-20260908.txt。

协议生成及 249 操作/249 handler 对应检查通过。没有修改 UI、审计业务实现或数据库约束。本次仅补统一查询验收，不能代表全部业务操作都有正确审计，也不涵盖运行归档下载。完整门禁最近仍第四轮 69/69 用例通过、126 项覆盖缺口，等待下一轮汇总。
