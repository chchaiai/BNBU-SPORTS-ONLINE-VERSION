# 补练撤销后既有会话收尾

2026-09-09，现行业务要求补练按指定成员和时段授权，撤销阻止新运动，不截断此前合法会话。扩展常规 v81-makeup.e2e.test.ts 调用的 probeMakeupSession，使用真实 SMTP 学生登录、教师授权/撤销与运动 API。

docker-makeup-finish-retest-20260909.txt 退出 0，2/2 tests、1 suite。覆盖：

- 常规截止后未授权开始 409；授权后开始及同键回放；数据库保存真实业务日期与补练来源。
- 撤销后会话仍 IN_PROGRESS；错误取消参数 422 不改变状态。
- 合法结束 COMPLETED/version 2，伪造 2099 年客户端时间不产生巨大时长；同键结束回放一致。
- 结束后创建材料草稿，同键回放一致，数据库仅一条记录且没有未结束时间片；学生可读取，同校其他教师及外校教师均 404。
- 再次开始 409，补练来源关联仍只有一条。

课程截止安排为明确标注的合成数据库夹具，运动是短会话，计入为零。没有通过本轮证明材料上传、最终提交、审核、有效学时或教师网页授权。现有教师弹窗是“补录学时”，不符合“指定补练窗口”，最小输入调整已向用户提出，未在等待答复时改变界面。

首次 docker-makeup-finish-20260909.txt 1/2，失败为新增测试用错 Prisma 时间片字段 sessionId；改为 schema 中 exerciseSessionId 后复测通过。保留原始失败，不作为产品故障。

学生 SESSION_OUTSIDE_TIME_WINDOW 中英文消息移除无业务依据的固定 06:00–22:00，改为课程开放或本人授权补练时段。没有新增布局元素。首次文本检查遗漏通用“刷新最新状态后再试”操作提示而失败，makeup-window-message-20260909.txt 保留；改为检查错误模型 message 字段，makeup-window-message-retest-20260909.txt 退出 0。产品通用操作提示未被删除。

最新完整全量仍为十九轮 97/97；本轮扩展既有专项，没有新增后端操作或迁移。原始 Docker 日志的工具行尾空格保留，源码和 Markdown 单独检查。
