# demand.md 全量实施与验收记录

> 以下各日期段保留当时的阶段状态；当前验收状态以 `demand-20260916-acceptance.md` 为准。

## 2026-09-16 浏览器与完整测试阶段更新

### 完整回归复测结果

- 浏览器上传验收已通过：`upload-result.json` 记录 PDF、PNG 的真实字节百分比（0→100）、确认、处理、成功和提交；MinIO 两次 PUT 200，申请提交 200。
- 浏览器维护恢复已通过：`maintenance-upload-result.json` 记录文件上传后实际进入维护、页面拦截、实际退出维护、主动重新获取状态、保留表单和附件并成功提交。两个文件仍只发生两次 PUT，没有重复上传已有对象。本地验收服务器已补齐公开组织与 `appEnv:test` 配置，使轮询行为与部署环境一致。
- Windows 视频验收运行时补齐 FFmpeg/ffprobe 9.0.1，来自 FFmpeg 官方下载页列出的 gyan.dev Windows 包；ZIP SHA-256 `fec81ae03971d9dd4be3ebe02e263bd2ec1d789483f931bdba5f5715e65da2e9` 与发布者校验一致。存于 `.local/demand-media-runtime/verified/`，仅本次测试进程使用，不纳入源码存档。正在运行本机完整媒体、维护、结算链路。
- Contract 基础结构、权限登记、枚举约束、维护响应、既有勘误和 OpenAPI lint 已通过；handler parity 通过。完整 `contract:check` 在正式版本兼容步骤停止于现有 `3.0.0-v81-local-draft` / `2.0.12-contract` 基线差异。该边界已存在于 `demand-20260912-handoff.md`，本轮不重写已发布快照、不将草稿冒充正式版本；部署前仍需完成当前运行契约的一致性检查与证据归档。

- Docker E2E 144/144 通过，包含真实 MinIO/Mailpit 运动、维护跨凭证过期恢复、审核和结算。`docker-e2e-final.log` 首次结束时覆盖检查发现启用专业纠正后停用接口计数仍为 34；清单改为 33 后，对同一份真实响应记录重跑校验通过（没有豁免任何接口或响应）。
- `docker-runtime-conformance.log`：231/231 启用接口成功响应、264/264 错误/权限响应、33 个停用接口拒绝通过。原始记录 `docker-runtime-conformance.ndjson` 仅含测试响应统计。
- 本机学生端全部 69/69；Portal 生产构建及全部 140/140；Docker 安全 53/53；前后端 TypeScript 通过。已修正补证重试测试：业务请求和幂等键保持一致，每次 HTTP 请求拥有独立诊断编号。
- API 补齐邀请码创建的 422、无效入班邀请码请求验证码的 401、视频时长/音轨/位置错误枚举。旧合成邮箱、学院、年级及旧恒定拒绝断言改为当前业务规则下的有效测试数据；权限和数据不变断言保留。
- 真实管理端事件表增加横向滚动与筛选布局，避免长编号挤压状态文字。详情入口和类型筛选再次通过浏览器验证。
- 浏览器后端与代理可用 `DEMAND_BROWSER_REUSE=1` 保留专用测试库重启，已支持真实 MinIO 上传代理。仍待上传浏览器流程、客户端维护恢复、生产部署和线上复测；上述通过项不等同于完整任务验收。

- 浏览器证据位于 `evidence/demand-20260916/browser/`。真实本地后端、独立 PostgreSQL、Mailpit 支撑登录与写入，不使用前端假数据。手机和平板经隐私同意与引导进入登录；电脑在验证码前拦截。设备以浏览器模拟验证，实体手机仍待验收。
- 24/23/16：教师英文课程设置无系统中文残留，中英往返通过；改名真实 PATCH 200，刷新后名称保留；独立常规截止字段已移除。截图及 `result.json` 保存证据。
- 18/22/14：Mailpit 实际收件完成学生浏览器登录，未分流→JC 保存成功，确认后学生编辑入口消失。验收发现完整未分流资料缺少确认入口，已补齐并新增回归测试 3/3。
- 21：真实页面原先仅有按钮，详情组件误放在预览分支；已修正。真实管理员登录→详情→事件类型筛选通过，数量与接口返回一致，证据 `admin-outbox-result.json` 和截图。权限、组织隔离、微秒分页、敏感字段隔离另有 HTTP E2E。
- 后端完整单元 322/322；Contract 35/35；Docker 数据库集成 60/60。历史测试夹具同步现行资料年份边界，过期上传单测覆盖对象不存在、已上传和存储不可用三种情形。运行时接口清单同步为 264 项，管理员专业纠正不再列为恒定拒绝接口。
- 安全测试首次运行 51/53，原因是两个旧角色守卫测试未注入资料校验依赖；补齐合法合成资料依赖后本机 53/53。Docker 复跑待完成。
- 20：真实 MinIO 已上传对象在实际维护模式切换、应用时钟前移 301 秒超过 PUT 凭证有效期后，维护中拒绝确认且不破坏草稿，退出维护后原对象确认→关联→提交→教师审核通过。该项用本地 SMTP 实际收件；不是生产 COS 或实体移动设备验证。
- 16：Docker 完整结算 E2E 通过，含待办、学期归档、报表和更正；本机完整结算受 Mailpit 容器域名解析影响，尚不宣称本机全链路通过。历史补卡及全量 E2E 正在复核。
- 当前仍是阶段结果。全量 Docker E2E 已启动；上传百分比/维护恢复的浏览器操作、所有需求综合回归、生产部署与线上复测、最终本地 Git 存档未完成。

## 2026-09-16 错误诊断及真实对象存储回归

- 12：学生端及管理端 API 请求携带独立 X-Request-ID，网络失败仍保留该编号；客户端本地异常按同一错误对象保持稳定编号。页面安全错误面板及兼容文本展示错误码、原因、下一步动作和诊断编号。课程改名、历史补卡、关闭、结算、补练、导入、专业纠正等原本仅取 message 的路径已保留诊断信息。
- 管理端导入和客服处理不再直接显示未知 Error.message。已有后端 HTTP_REQUEST_REJECTED / UNHANDLED_HTTP_ERROR 脱敏日志经新增测试验证，错误响应与日志使用同一编号，不记录异常原文和私密 URL。
- 已通过：学生错误投影 4/4；Portal API、诊断与翻译测试 26/26；Portal TypeScript；后端脱敏日志测试 1/1、真实 HTTP 系统错误响应及请求头编号贯通 1/1。尚需全页面失败场景与切换语言后的浏览器验收。
- 隔离 Compose 新增临时 MinIO（本机 59000）和 Mailpit（本机 58025）；都属于合成测试环境。Docker 内 makeup E2E 2/2，通过教师/组织/学生权限、日期边界、分页、并发撤销、审计、测试邮箱 OTP 实际收件登录，以及真实运动一分钟→MinIO 上传→确认→提交→人工审核→重放。
- 这次真实对象存储测试不是生产 COS 验收，也没有覆盖所有维护中断场景。仍待结算/历史补卡回归、完整浏览器验收、完整本地及 Docker 回归、生产部署、线上复测及最终 Git 存档。

## 2026-09-16 时间规则实施进度

- 16：教师设置页删除独立常规提交截止日期；新增打卡以教学班日期为边界，补练授权也不得越过该日期。已开始运动及既有草稿不再被旧统一截止或学期结束时间截断。补证独立时钟、归档、权限和材料检查保留。
- 结算改为打卡日期结束后检查现有待办；课程容量和提醒改用实际打卡日期。API 三个旧日期字段标记 deprecated、可省略，历史数据列保留兼容；新规则保存生成兼容值而不据此拒绝操作。
- 0082 迁移调整补练数据库守卫，保留组织、责任教师、在班、学期、模式、版本等限制，以课程打卡日期代替七天收尾窗口。独立本地测试库已执行，82 条迁移检查通过。
- 已通过：时间领域测试 8/8，真实 HTTP 运动流程 4/4；新增北京时间截止跨零点完成、学期结束后旧草稿提交测试 2/2。Portal 类型检查及翻译检查 3/3 通过。
- 修复旧测试夹具缺失学生必填资料的问题；共享夹具使用合法合成资料，保留生产资料校验。游泳回滚测试改为当前有效的材料集合缺失场景，保留失败重放与零部分写入断言。
- 尚待：历史补卡、补练真实对象存储、结算、提醒、旧课程兼容完整回归，客户端其他截止文案检查，Contract 全检查及 Docker/浏览器/线上验收。当前不能将第 16 项标记为完整验收。

## 2026-09-16 上传恢复阶段记录

- 17：照片、视频、证明材料上传接入 XHR 实际字节百分比，区分读取、传输、后端确认、处理、成功及失败；证明材料完成后单独显示申请提交阶段。前端上传/恢复测试 6/6。浏览器真实进度和多文件界面仍待验收。
- 20：同文件上传凭证过期可续期，保留 mediaId、上传会话及对象路径；已存在对象不因 PUT 凭证到期而被清理，确认仍校验所有权、哈希、类型及 ETag。新增 0081 迁移严格限定 ACTIVE 上传会话只延长凭证期限；81 条迁移静态检查和独立测试库迁移成功。
- 本机 HTTP/PostgreSQL 过期与维护恢复定向测试 3/3。Docker 应用测试容器运行媒体 E2E 10/10，包括真实 FFmpeg 转码、失败释放配额、维护后确认和关联、并发恢复、类型伪造及跨运动绑定拒绝。对象存储使用内存替身，尚不能作为真实 COS/MinIO 或浏览器完整验收。
- 本机完整媒体组初次 7/10：测试替身异常构造与编译应用类型不一致已修正；本机缺少 FFmpeg 的两项已在安装 FFmpeg 的 Docker 测试容器重跑通过。本机完整视频环境仍未验收。
- 21：分页游标改用数据库完整时间精度，避免同毫秒事件漏页；新增微秒时间用例。
- Docker 测试环境：bnbu-demand-20260916-postgres-1（临时数据）、bnbu-demand-test-runtime（本次应用测试/FFmpeg）；数据库测试串行运行，禁止指向生产。
- 尚未完成第 12/16 项、真实对象存储及浏览器回归、完整应用验收、生产部署、线上复测和最终本地 Git 存档；目标保持进行中。

## 基线及范围

2026-09-16 开始，分支 codex/bnbu-sports-web-new-20260907，HEAD 9f6fe8a3d398f49dafef28269acde8cdbffa5230。工作区已有邮箱、媒体、学时及部署相关未提交内容；最终存档须核对本轮范围，保留已有工作。当前 demand.md 全文包含 12、14、16、17、18、19、20、21、22、23，共十项；追加教师课程设置英文显示修复。

## 用户确认

- 14：历史已验证邮箱继续登录，新绑定严格执行一个小写字母加九位数字。
- 16：学生打卡时间由教师设置的打卡结束日期决定，且不得晚于学期结束。到期停止新打卡，已开始运动、材料提交、审核及反馈各自继续处理，结算等待有效待办结束。已由用户确认。
- 18：首次选择正式专业即锁定；未分流可自主确认为正式专业一次；具有用户与账号权限的管理员可纠正，纠正不恢复学生次数。
- 19：允许手机和平板，禁止电脑；属于产品限制。
- 21：完成当前组织的真实排障入口，不自动补发历史事件；重试中及最终失败须有真实事实支持。

## 逐项影响及验收台账

| 项 | 当前发现与实施范围 | 验收要求 | 当前状态 |
|---|---|---|---|
|12 错误体系|已有全局异常过滤、错误码及 requestId，需审计前端投影、上传直连错误、已知状态异常及日志关联。API 保持安全错误信封，不输出原始异常。|登录、入班、资料、运动、上传、审核、课程各类失败提供原因、动作、代码和诊断编号；日志关联及脱敏|分析中|
|14 邮箱|保留原域名验证函数用于历史登录，新增严格新绑定校验；须覆盖入班验证码、换绑、最终验证及旧挑战绕过。无历史邮箱批量修改。|前后端边界值、新入班、换绑、历史登录、配额和发送前拒绝|实现中|
|16 日期|现行常规截止与七天收尾绑定，并影响历史补卡、结算和发布校验。按教师打卡结束日期控制新增打卡，提交、审核及反馈应分别检查。|截止边界、时区、进行中会话、补证、历史补卡、结算、归档及旧课程兼容|分析中|
|17 上传进度|现有媒体上传使用 fetch PUT；需真实字节进度及读取、处理、上传、确认阶段，覆盖照片、视频及证明 PDF。|正常、失败、重试、多文件、处理中、100% 后确认及维护恢复|待实现|
|18 未分流|学院专业双端目录及资料强制补录共用校验；现资料完成接口允许重复更改。需持久确认事实、服务端并发控制、管理员纠正及安全投影。|首次正式专业、未分流一次确认、重复/并发/绕过、管理员纠正、跨学期和历史账号|待实现|
|19 设备|登录前及已登录入口均须控制；兼容手机和平板，保持可理解提示。|电脑不能进入验证码流程、手机和平板可用、已有会话、窗口大小变化|待实现|
|20 维护恢复|确认上传阶段直接按 capabilityExpiresAt 拒绝，可能阻止已成功上传对象关联。需核对中断计时、凭证刷新、对象核验、草稿和重新取状态。|维护前开始→维护→长时间等待→恢复→原记录成功提交；权限、哈希及记录归属不绕过|分析中|
|21 Outbox|健康统计目前全库 PENDING/FAILED，无详情；无实际消费者，FAILED 尚非最终失败。需当前组织同口径分页查询、安全字段、筛选、真实状态及日志关联。|数量与列表同口径、跨组织/角色拒绝、分页筛选、空值不伪造、无敏感 payload|待实现|
|22 JC|前后端专业目录新增 SCC/JC，按既有规则 SAI 同步包含 JC；不改历史档案。|入班和资料校验可选 JC，其他固定目录不误开放|单元测试 8/8，真实环境待验收|
|23 课程改名|现教师创建同时写 Course 和 ClassSection；不能直接复用管理员改课接口扩大权限。须限定责任教师并同步名称、版本、审计和视图。|本人课程改名、跨教师拒绝、并发、刷新、学生及管理端显示、历史事实保留|分析中|
|追加 英文课程设置|翻译资源漏项，历史补卡、结算和删除子组件未纳入原测试。|英文无系统中文残留、动态提示、下拉/无障碍文本、中英切换及刷新|实现中|

## 环境和证据

- 本轮已执行 JC 前端目录测试 3/3、后端身份资料测试 5/5。
- 英文资源测试复现缺失；已补齐三个课程设置子模块及动态目标、发布规则、结算报告提示。翻译检查 3/3 通过，Portal TypeScript 检查通过。浏览器中英文往返及真实请求待测。
- 新绑定邮箱与历史域名校验分开，邮箱定向测试前端 3/3、后端 6/6。最终验证、旧挑战及完整 E2E 仍待核对。
- 学生入口新增手机/平板识别，桌面在启动及渲染入口显示访问说明；设备测试 2/2。浏览器实际回归待测。
- Docker Desktop 已启动；本轮独立临时 PostgreSQL 容器 bnbu-demand-20260916-postgres-1 健康，绑定本机 55432，80 条迁移成功。数据库只含可重置合成测试数据。
- 本地真实 HTTP、数据库、完整集成/E2E、Docker、部署、线上复测及最终存档均未完成。不得将上述单元测试作为完整验收。

## 2026-09-16 第二批进度

- 23：复用现有 PATCH /class-sections/{id}，新增课程设置改名入口；师生页面及管理端学生课程关联以教学班 displayName 展示。共享 Course 目录名称保留，避免影响其他教学班。现有责任教师、组织隔离、版本、幂等、关闭/归档权限及审计继续执行。真实 HTTP E2E 覆盖名称持久化、重放、版本冲突、权限与关联不变。
- 18：所有已知学院支持“未分流”；正式专业在服务端锁定，确认事实使用 STUDENT_MAJOR 事件持久保存。既有有效正式专业视为已确认；格式异常历史资料可沿用资料修正流程首次确认。管理员使用 USER_ACCOUNTS 权限、原因、版本及幂等纠正，已用确认机会不恢复。没有批量改写历史资料。
- API：StudentProfile 增加 majorConfirmationLocked；UpdateStudentRequest 增加 majorCorrectionReason，允许受控学院/专业纠正；资料提交 majorName 接受未分流。前端资料错误保留诊断信息，专业锁定及邮箱格式错误有专用操作指引。
- 14：继续补齐最终验证码确认、入班证明消费的严格新邮箱校验；历史已验证账号通过原登录和已登录入班路径复用邮箱。
- 已通过：真实 HTTP/PostgreSQL teaching-structure E2E 9/9，后端专业/资料单元 12/12，前端学院/专业/邮箱 8/8，管理端名称投影及翻译 4/4，管理端 TypeScript。E2E 使用本机应用连接 Docker PostgreSQL，不等于应用整体容器回归已通过。
- 尚待：上述功能浏览器真实操作、完整 Contract 检查、邮箱全链路回归、其余需求实现、全套本地/Docker 回归、部署及线上验收。测试曾捕获重复审计版本键，已将专业事件独立为 STUDENT_MAJOR 后通过。

## 业务事件排障进度

- 21：新增 GET /health/admin/outbox 及健康页“查看详情”；要求 AUDIT_QUERY 权限，限定当前组织，分页游标绑定用户、组织和筛选条件。健康积压统计同步为当前组织 PENDING/FAILED，与详情 backlog 一致。
- 列表提供类型、真实状态、创建/最近处理时间、尝试/重试次数、错误码、关联对象及已记录的 UUID 诊断编号；支持状态、类型和时间范围筛选。既有 FAILED 显示等待重试，不冒充最终失败；没有历史时间和诊断编号时显示未记录。API 不投影 payload、邮箱、令牌或内部 worker 标识。
- 本轮最新 teaching-structure 真实 HTTP/PostgreSQL E2E 为 10/10，覆盖 Outbox 分页、失败过滤、跨组织隔离、权限、脱敏及健康数字一致；健康/专业/邮箱定向单元 12/12。后端构建、管理端类型检查通过。
- 发现并补齐新操作在分管理员默认拒绝权限表中的登记，保留 AUDIT_QUERY 边界。应用整体 Docker 测试、真实浏览器、线上数据验证仍待完成。
