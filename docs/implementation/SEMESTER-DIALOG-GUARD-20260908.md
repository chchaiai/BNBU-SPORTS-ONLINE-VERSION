# 学期提交未确认时保留原请求

2026-09-08。管理员学期页面在请求进行中或结果未确认时禁用表单编辑和取消，关闭回调也保留原弹窗。网络异常、5xx、请求仍处理中或写入已成功但回读失败均保留未确认状态；继续保存/确认使用原意图。明确的业务拒绝允许修改后重新提交。

semester-dialog-guard-browser-first-20260908.txt 记录实际 Edge 与持续 Docker 后端验证通过：创建响应丢失后输入禁用、取消禁用，点击关闭仍留在原弹窗；同键重试成功，之后编辑和阻塞切换回归通过。新增合成学期使用 2034-2035；原 2033-2034 证据截图保留。semester-dialog-guard-typecheck-20260908.txt 对应 Portal tsc 退出 0，输出为空。

源码核对请求进行中错误使用后端实际 CONFLICT_REQUEST_IN_PROGRESS。该错误码分支本包未单独注入测试；浏览器覆盖的是真实响应丢失分支。没有改变布局、CSS 或后端逻辑。

整页刷新或离开页面仍会丢失内存意图，持久恢复尚待实现；浏览器成功切换及完整三端验收仍未完成。当前限制只保证留在页面并重试的流程，不宣称所有未知结果均已解决。
