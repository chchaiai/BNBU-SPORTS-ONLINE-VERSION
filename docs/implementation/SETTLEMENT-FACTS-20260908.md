# 结算前真实事实检查

日期：2026-09-08 13:27。本包仅更新后端读取与本地验证，网页布局无变更。

## 实现

所有已确认名单来源统一核对注册状态、身份歧义和体测记录。重复身份即使教师已确认异常，仍报告身份阻断。体测缺项使用真实记录或已批准免测判定；注册/身份不足以确定分母时返回未知数量。已发布规则及服务器结算时间纳入检查。

未注册/未入班或缺体测是否允许正式结算，业务答复仍待提供；相关缺项返回 UNAVAILABLE，未假定业务通过。正式综合名单确认命令尚未实现，ready 仍为 false。

## 验证

- docker-settlement-check-facts-first-20260908.txt：默认 Docker 构建与原业务回归通过。
- docker-settlement-facts-probe-first-20260908.txt：首次专项测试因旧准备脚本缺少规则模板失败。
- docker-settlement-facts-probe-retest-20260908.txt：第一次复测发现旧脚本关闭班级未关闭入班状态，数据库约束拒绝。
- docker-settlement-facts-probe-retest2-20260908.txt：修正以上测试准备后，57 项迁移就绪，名单注册、确认重放、来源切换、综合名单、管理员范围及结算事实专项通过。
- settlement-duplicate-http-20260908.txt：持续 Docker 后端真实 HTTP 回读确认重复身份仍 BLOCKED，count=2；体测数量未知。
- docker-settlement-facts-browser-rebuild-20260908.txt：持续本地后端重建并等待健康通过。

专项读取使用注入时钟验证计划结算时间前一毫秒和恰好到达时刻；测试新增体测/修改邮箱与姓名均处于回滚事务，并验证无新增体测持久化。不能把注入时钟测试称为真实业务结算。首次失败日志保留原文，日志可能包含工具生成的尾随空白。

## 后续依赖与交接

正式结算、报告读取/导出、纠错、学期切换与完整三端验收仍待完成。学校结算缺项政策待答复；现有教师页面没有正式结算入口，后续需落实入口方案。腾讯云 COS/CVM/PostgreSQL 正式环境尚未据本包验证。当前只证明上述本地 Docker 行为。
