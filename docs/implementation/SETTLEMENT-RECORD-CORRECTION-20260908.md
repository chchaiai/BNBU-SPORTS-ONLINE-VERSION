# 审核纠错与更正版报告联动

日期：2026-09-08 14:00。依据教师流程第 12.3 节，旧事实更正须保留原判断并追加报告版本。

已结算课程的既有审核纠错接口现在同事务完成判断历史追加、学时重算、报告更正版与审计；只有明确纠错入口放行，普通审核仍受结算守卫限制。责任教师、原记录、初版时间、纠错事件/版本/原因/请求关联均被核对。相同事件不能重复生成报告；名单来源变化会拒绝本次记录纠错。更正版关联上一版与原记录，保留数据库计算摘要，原报告不覆盖。

未结算课程继续按原有审核纠错流程，不生成结算报告。本次没有开放初次结算 HTTP，也没有新增协议操作或 UI 布局。

## 本地 Docker 验证

- docker-settlement-record-correction-first-20260908.txt：真实纠错 HTTP 后报告从 1 版变成 2 版；原版 JSON/摘要不变，更正版学时重算、原记录与纠错事件关联、报告审计通过，同键重放没有第 3 版，退出 0。
- docker-settlement-correction-rollback-first-20260908.txt：在专用 v81_runtime_test 库临时注入报告写入拒绝触发器，预期 HTTP 500 后审核版本/历史、学时、事件、通知均未变化；finally 删除触发器和函数后，同键重试成功并通过上述版本验证，退出 0。该 500 是有意故障注入，不是未修复产品失败。
- docker-settlement-correction-unsettled-regression-20260908.txt：未结算原业务 HTTP 回归通过，退出 0。

初版报告通过明确标注的合成存储夹具生成；这些测试证明真实纠错接口联动，不能证明初次正式结算已经可用。隔离测试库允许保留合成历史；持续浏览器库和云环境未改。数据库迁移仍为 57 项。三轮测试均结束，原日志保留工具空白，代码及手写文档单独检查。

## 剩余依赖与交接

初次结算协议/幂等端点、报告读取/导出、学生本人结果、管理员摘要、学期切换和三端完整验证仍待完成。体测、成绩及认可的已结算纠错还需专用受控实现；普通入口目前继续拒绝。缺注册/体测是否允许结算的政策仍待答复。完整目标保持进行中。
