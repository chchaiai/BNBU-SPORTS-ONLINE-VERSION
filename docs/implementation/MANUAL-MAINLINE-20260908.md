# 人工审核主流程 Docker 复验

默认 backend-startup 主流程现已退出 0，证据 docker-manual-mainline-final-20260908.txt。使用本地 PostgreSQL、MinIO、Mailpit 和真实 HTTP 接口。

本轮修复两项测试夹具问题：先经总管理员接口创建规则模板，并在教师发布课程时传入 templateId；末尾新增另一学生用于越权验证时，不再让通用学生夹具改写已发布课程。新增 configureCourse 参数默认 true，只有此处显式 false。未放宽已发布安排不可变的数据库规则。

原失败保留：docker-manual-mainline-current-20260908.txt 为缺模板导致 422；docker-manual-mainline-template-20260908.txt 为流程末尾夹具重写已发布安排导致 P0001。

通过范围：管理员/教师登录和首次改密；总管理员启用人工审核；学生 SMTP 验证码登录；拒绝文件选择来源、接受 IN_APP_CAMERA 合成图片；真实对象存储上传、确认、绑定、校验；学生提交、教师有效审核、学生回读 60 分钟；学生不能审核、教师幂等重放只生效一次、其他教师和组织拒绝；内部成绩历史与角色隔离；认证材料、提交、批准和撤销；学生名单状态与教师端一致；管理员汇总、纠正后认可时长撤回；XLSX 导出元数据、文本学号与精确秒数；学生进度认可与撤销。

限制：运动会话完成时间及图片是合成夹具，身份冲突分支用数据库夹具切换；不是学生网页真实相机捕获或完整三端浏览器验收。正式结算/学期切换以及其他专项问题继续处理。此通过结果不能替代尚未验证的专项 flags。
