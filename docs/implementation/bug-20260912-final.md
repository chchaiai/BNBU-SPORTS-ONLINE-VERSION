# 腾讯云问题修复与验收交接（2026-09-12）

状态：本轮代码修复、真实 Docker 回归、腾讯云部署和合成账号线上业务验收完成。真实手机系统相机操作仍需设备验收；浏览器返回文件及超长拒绝已验证。

## 根因、修复及验证

| 问题 | 根因及修复 | 本地验证 | 线上验证 |
| --- | --- | --- | --- |
| 视频处理卡住、预览不可用、带宽告警 | 强制下载约 32 MB FFmpeg 并完整转码，处理状态挡住原件预览，静态资源不缓存。优先浏览器 H.264/AAC 编码，立即预览原件，兼容路径保留版本化 FFmpeg 缓存；修正原生编码静音 | 有声视频 RMS 0.69、约 2.5 秒处理、零 WASM 请求；Firefox 兼容转码播放通过 | 新静态文件哈希验证；真实 COS MP4 上传、扫描及读取通过。未进行大规模并发带宽压测 |
| 照片和视频灰色封面 | media ID 未换取授权地址。每次打开换取私有 COS 短时地址，加载真实图片和视频首帧，签名仅驻内存 | 图片像素、视频尺寸和播放通过 | 真实私有 COS 图片及视频首帧浏览器通过 |
| 原生拍摄、10 秒限制 | 改为系统相机 capture 请求，返回后校验新视频不超过 10 秒，保留旧 15 秒材料读取 | 原生文件入口及 11 秒视频拒绝通过 | 网页超长拒绝通过；系统相机硬件操作见剩余依赖 |
| 多余文案、详情原因区 | 删除首页服务端提示、businessDate 实现说明、保留原文标签及整个固定原因区 | 学生 86 项 smoke、真实浏览器通过 | 部署哈希及详情浏览器通过；公开教师说明保留 |
| 低于最低时长仍产生记录 | 原逻辑允许提交，仅计入为零。创建和提交均校验最低时长，结束页隐藏提交入口 | 真实 HTTP 拒绝且记录/审核未新增；网页入口验证通过 | 短运动创建返回 422，通过 |
| 教师删除课程 500 | 邀请码撤销写入 COURSE_RETIRED 与旧数据库 CHECK 冲突，SQLSTATE 23514 / Prisma P2039。0071 扩充允许值，复用事务及幂等重试 | 有效邀请码、权限隔离、幂等重试和历史保留通过 | 独立合成课程真实网页删除、同请求重试、学生历史仍可读通过；未代删用户实际课程 |
| 教师单次时长及计入上限 | 旧固定 60 分钟计入逻辑和无到点结束。新增整数分钟参数，新会话保存上限、记录保存计入快照；网页及后台工作器到点结束 | 真实 60 秒网页结束；无请求后台完成及系统事件；修改教师规则后旧会话上限不变 | 真实时间等待后数据库先于任何 GET 已 COMPLETED/60 秒；系统事件通过；网页自动结束通过 |
| 管理员总目标及教师重新分配 | 新增组织全局目标，默认 1200 分钟；复用 SUPER 权限、版本、组织锁及幂等。总目标变更后未分配课程禁止新打卡，旧运动可结束；教师保存两类目标后恢复 | HTTP 权限/幂等/暂停/恢复；真实管理及教师表单通过 | 相同业务链及两端真实表单通过，仅修改独立合成组织 |
| 补证来源不清晰 | 原待办缺少记录上下文且展示英文码，入口未明确目标记录。服务端补充课程、时间、项目、时长；首页和打卡显示中文原因、教师说明及截止时间；明确选择记录，草稿按记录隔离 | 两条真实待办切换、独立草稿恢复、正确记录提交通过 | 待办定位指定记录、中文原因、真实 COS 补证上传及指定记录提交通过；截图已视觉检查 |

## 验证与证据

- 最终 Backend 单元测试 277/277，学生 smoke 86/86，73 个迁移安全检查、后端及 Portal 类型检查、Portal 构建、263 个 OpenAPI 操作生成校验通过。
- 真实本地环境：Docker PostgreSQL、MinIO、Mailpit、Backend HTTP 和 Edge 浏览器；兼容编码另用 Firefox。非静态 Mock 验收。
- 云端：香港 CVM 43.129.193.7、TencentDB PostgreSQL、香港私有 COS，以及真实学生/教师网页；使用独立 LIMIT12 合成组织和账号。
- 脱敏结果：`evidence/bug-20260912/acceptance.json`；同目录保存云端补证、管理员和自动结束截图。私有原始日志在 `.local/bug-20260912-*.log` 和 `.local/exercise-limits-*.log`，账号/令牌不纳入 Git。
- 保留首测失败：编码静音已修复；原生 WebKit canvas 录像能力不足标为 PENDING；课程删除浏览器首测填写上层课程名导致按钮未启用，改为要求的教学班名后通过；云端只读探针误用 o.code，改为 organization_code 后通过，没有修改业务数据绕过结果。

## 部署与回退交接

- 已部署源码：`9618cc35e4d6f64a15c8392560d6c4500a631326`；前序修复存档 `6e15ad4`、功能存档 `eadcb2e`。
- 当前 release：`/opt/bnbu-sports-production/releases/exercise-limits-20260912`；上一版 `bug-20260912`。
- Backend 镜像：`sha256:e75b9d426564189d1b5f62fd2a41964d3e773d7ad5659d347f183b81e777cb8e`。
- Portal 镜像：`sha256:a7aea2c04e5361321059f4eec01169df5ed1c0c3105c5ada99b7cb16a094a2d4`。
- Migrator 镜像：`sha256:a9072e50cd6c2782d82bd10b620af5f76908eaf99a8079a97ec4fa4bd9a363c7`。
- 部署前备份：`/opt/bnbu-sports-production/backups/before-bugfix-20260912T151458Z.dump`，3,691,309 字节；pg_restore 目录检查通过，未做覆盖式恢复演练。
- 短暂关闭 API 写入并停止旧应用后执行 0072/0073，启动新应用，确认健康、8 个变更静态文件哈希及 Nginx 配置，再恢复写入。0071 在前次发布已应用。
- 写入已恢复。新会话上限属于正式数据规则，故障优先前向修复；不能直接换回旧版无上限计时逻辑，也不能未经数据兼容评估恢复数据库。部署脚本仅在开放写入前自动回退。

## 剩余依赖与使用说明

- 真手机系统相机、具体手机浏览器编码性能仍需设备复核。用户已接受网页 capture 请求和返回后校验：浏览器可能提供相册，网页不能强制系统相机第 10 秒停录。
- 未执行全校并发压测；不能把单用户视频处理改善等同于证明云带宽永不告警。生产 SMTP、OCR 不是本轮验收结论。
- 教师在课程设置输入单次最多分钟数，该值同时限制单次有效学时；仅新开始的运动采用，进行中及历史上限保留。
- 总管理员在全局规则设置所有当前课程总目标；教师必须重新分配两类目标后恢复学生新打卡。已关闭、结算课程保留历史目标。
- 补证待办现在直接显示原记录时间、项目、时长、课程、中文理由和截止时间；点击后只给该条记录补证。
- 用户实际失败的课程删除可在页面重试原请求；线上验收删除的是合成课程。账号和运动历史按确认规则保留。
- 本地存档只包含本轮明确文件；原有 bug.md 修改、0061 行尾状态及 25 份旧报告删除保留原状态，未推送远端。
