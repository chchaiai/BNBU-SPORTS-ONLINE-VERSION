# 关闭课程后的 OCR 续办与受理结果重放

依据教师业务文档 §5.3：关闭前服务器已受理的名单/OCR 继续按原规则处理，关闭后拒绝新业务起点。

发现并修复：OCR 图片批次成功受理后，若返回结果丢失且课程随后关闭，原键原材料重试被入口的课程状态检查拒绝为 409。入口现在先验证责任教师和组织范围，再解析材料并进入现有幂等服务；课程状态仍在新命令的加锁事务中验证。已成功请求返回原受理结果，新批次仍拒绝。重放和事务失败产生的临时对象仍由原 finally 清理。无协议或迁移变化。

`v81-ocr-intake.e2e.test.ts` 保留开放课程场景，并增加关闭前已有成员、已发布规则、已受理名单和体测图片的独立场景。通过 HTTP 关闭课程后验证：

- 新名单/体测图片批次均拒绝；原名单上传键和材料重试返回相同完整结果。
- 数据库证明原批次受理时间不晚于关闭时间。
- OCR 队列执行、提供方超时留痕、重试成功、草稿映射、人工核对、并发版本和原键重放。
- 正式名单确认继续完成，保留原图关联与历史，不自动建立成员；未完成事项继续阻塞结算。
- 体测识别与核对、正式确认继续完成，学生 HTTP 只收到本人原始 800m/270 秒事实，通知和确认版本持久化。

失败记录逐次保留：first 为测试命令缺少 conformance 输出路径；second 为旧建成员夹具试图重开关闭课程；third/fourth 为新分支缺少关闭前课程规则/时间夹具；fifth 复现真实原上传重放 409，同时开放分支的规则夹具未完整；sixth 关闭链通过，开放分支因提前建立成员破坏既有 CSV 切换探针的空名册前提。最终按场景安排夹具时序，原开放分支继续覆盖 OCR/CSV 名册切换。

边界：真实 Docker PostgreSQL、Nest HTTP 与严格接口检查；图片字节为合成数据，对象存储为内存测试实现，腾讯 OCR 提供方为显式模拟。这不是腾讯云 OCR/COS 或浏览器交互验收。电子名单在关闭后的续办、教师 Web OCR 入口和通知定位仍需后续核对。

第 36 次全量 Docker：类型检查通过，98/98 测试、25 套件、0 失败/跳过，124897.46799 ms。严格运行协议覆盖 219/219 已启用成功路径、253/253 错误/权限响应，34 项明确关闭。证据：`docker-backend-e2e-thirtysixth-20260909.txt` 与 `runtime-conformance-thirtysixth-20260909.json/.md`。

本地联调后端已按当前源代码重建；12:47 容器状态 running/healthy。构建日志见 ocr-closed-browser-rebuild-20260909.txt。
