# 电子名单关闭后续办验收

2026-09-09 12:50，依据教师业务 §5.3 已受理名单继续处理；名单确认不创建账户或成员。

真实 Nest HTTP / Docker PostgreSQL 测试：开放时受理合法 CSV，读取原文件；HTTP 关闭课程；新导入返回 409，原键原文件返回原 201 数据；导入数量保持 1、对象存储内容完整相同，无临时文件泄漏；关闭后原名单确认返回 201，可读取、原键重放，新键重复确认返回 409；当前正式名单和唯一确认事件落库；账户与成员数量不变；其他教师 404、管理员 403。

首次 `electronic-roster-closed-docker-20260909.txt`：上述测试在其他教师权限断言处被严格协议钩子拦截，原因是 confirmV81Roster 未声明服务实际返回的 404。已在候选 OpenAPI 的名单确认 GET/POST 增加 NotFound，并同步 extend-v81-contract.mjs，避免生成时丢失。业务实现无改动。

复测 `electronic-roster-closed-docker-retest-20260909.txt`：类型检查通过，名单 HTTP E2E 3/3、0 跳过/失败，4306.721224 ms；覆盖关闭续办、原版本与权限投影、名单差异确认/解决/重开。前序第 36 次全量 98/98 是补充此测试前的基线，不能称为当前全量 99 项通过。

对象存储为内存测试实现，CSV 为合成数据；此处不是 COS 或教师浏览器验收。当前候选协议声明更新，正式 contracts/openapi.yaml 基线未发布。本地没有数据库迁移或 UI 变化。下一步继续教师网页 OCR、通知具体对象定位及剩余全范围验收。
