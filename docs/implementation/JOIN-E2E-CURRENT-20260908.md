# 进班与二维码邀请端到端测试（2026-09-08）

独立 Docker/PostgreSQL 构建并运行 enrollment-qr-join.e2e.test.ts，runtime-conformance-hook 开启，5/5、退出 0。日志 `docker-join-e2e-current-first-20260908.txt`；此前完整第二轮该套件 0/5 的失败证据继续保留。

仅在本套件 beforeEach 为教师 A/B 和管理员准备已改密账号安全记录；管理员使用 SUB 身份、明确登录账号、仅 COURSE_VIEW 权限。没有改变共享 fixture、运行时授权或前端布局。

原有断言保持：邀请摘要存储、撤销轮换和同键重放；发放入班凭据不提前创建身份；性别和年级非法输入拒绝；原子入班及加密结果重放；教师手工加入既有学生、移除/恢复/退出和各角色读取/写入边界；同学期第二次进班回滚，教师移除后拒绝自行重新加入。

本套件使用其既有二维码/入班凭据路径，不代表新注册邮件验证码闭环、邀请自然到期宽限或三端页面所有入口均在此处验证。邮件注册和邀请期限专项需与已有独立证据一并核对，不能仅由 5/5 推断全部入班功能完成。

下一步继续教学结构及运动记录等剩余 E2E，完整结果需重新运行后更新。当前总体目标未完成。
