# 帮助中心当前浏览器验收 2026-09-09

在当前持续 Docker 后端（251 操作、58 迁移）与现有开发页面上验证，全部使用合成账号及文章；产品源码、UI 和布局未修改。

管理员原页面：创建草稿的成功响应丢失后，输入锁定；刷新恢复原提交、同键重试成功且待确认记录清空。发布响应丢失后同样刷新恢复。发布、下线、重新上线、编辑回读通过。并发编辑产生 409，原输入保留、不自动覆盖；关闭重开读取其他编辑者版本，再明确保存成功。危险 script/img/svg、事件属性和 javascript/data 链接不进入可执行 DOM，允许的 HTTPS 链接使用 noreferrer noopener。证据 admin-help-lifecycle-current-20260909.txt，退出 0。

学生实际邮箱 OTP 登录：读取发布文章并展开正文；管理员通过真实 API 下线/重新上线后，学生重新进入帮助中心分别看不到/重新看到。断网时显示缓存提示与内容，网络恢复重新进入后取得服务器内容且提示消失。证据 student-help-lifecycle-current-20260909.txt，退出 0。

随后指定本轮管理员新建文章 Synthetic Help 240a59ac-f701-4e30-bc10-fc7bf178ad15 复测，实现同一文章的两端对应：学生读到最后保存的“重新核对后保存”，下线/重新上线及离线恢复均通过。help-same-article-closure-20260909.txt 退出 0。help-bilingual-current-20260909.txt 验证同一文章英文标题和正文来自 locale=en 请求，再切回中文恢复对应内容，退出 0。学生语言已恢复中文，文章已恢复发布。

管理员、学生中文、英文截图已查看；另保留离线缓存截图。测试脚本现在默认写入忽略目录，避免复跑覆盖历史截图；本轮截图单独命名。学生脚本新增可选 STUDENT_HELP_TITLE，用于明确选择同一文章。两脚本 node --check 通过。

剩余范围：实际学校内容、生产邮件及云服务、极端内容/大量文章、多账号同时编辑和移动视口不由本轮证明。两项待用户确认的首次改密/结算界面调整不受本轮影响。完整三端目标仍未完成。