# 当前学生资料删除后的申请读取

2026-09-08：认证列表、免测申请列表和详情改为只加载投影所需的材料关联，不再强制加载当前学生资料。组织和责任教师过滤保留；写入、审批、撤销及通知路径未改。无需新增协议字段或数据库迁移。

docker-application-history-read-20260908.txt 退出 0：Docker 构建、52 迁移检查及四组基础账号验证通过。专项在数据库建立一个合成学生及两条草稿申请（800 米免测、校队认证），先通过教师 HTTP 读取，再事务删除该学生的当前会话、资料和用户。

- 删除后用户和学生资料确实不存在；两条申请全部存储字段与删除前一致。
- 责任教师免测列表、两条详情、认证列表 HTTP 投影与删除前一致。
- 同组织非责任教师和其他组织教师读取两条详情均返回 404。

这是合成数据直接删除后的只读验证，不是注销业务验收；未通过注销确认接口，未验证已批准申请、带材料申请、历史主体下的新审批/通知、教师删除或其他历史读取。原有偏好删除 P0001 未修复，完整清理仍待完成。分管理员修改邮箱的验证生效方式已向用户询问，资料编辑接口等待业务决定。

复现：

```powershell
docker compose --env-file .local/v81-sql.env -f tools/local-integration/compose.v81-test.yml run --build --rm -e V81_APPLICATION_HISTORY_READ=1 backend-startup
```

本轮未修改前端、业务权威文档或旧失败测试。

存档检查：明确暂存9个文件；git diff --cached --check退出2，仅原始Docker日志尾随空格，保留输出并记录未通过。
