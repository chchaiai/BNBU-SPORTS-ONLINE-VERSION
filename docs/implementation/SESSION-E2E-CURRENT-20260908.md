# 运动会话端到端测试（2026-09-08）

独立 Docker/PostgreSQL 构建，开启 runtime-conformance-hook，执行 exercise-session.e2e.test.ts 最终 4/4、退出 0，证据 `docker-session-e2e-current-success-20260908.txt`。

套件前置条件改为现行已改密教师与有权发布模板的 SUPER 管理员；经真实 HTTP 发布模板和课程规则后开始运动。补完整日程，不绕过模板和发布后不可修改的数据库约束。共享 fixture 不改。

验证开始及同键重放、活动会话恢复、暂停/恢复、保守同步和结束、取消保留历史、旧版本/重复活动/他人/已退班/额外字段拒绝、未验证离线时间拒绝，以及已有 20 小时历史 VALID 记录后继续运动。最后一项依据 docs/business/00-overview.md 的 BD-20260903-08 和禁止仅以达标限制真实运动规则，替换旧禁止开始预期。历史 20 小时不等同于当前规则计算的两类达标进度；本测试不证明完整进度算法。

已发布日程变更明确断言数据库拒绝。不可开始场景改为完整 CLOSED 教学班测试状态，验证 SESSION_OUTSIDE_TIME_WINDOW，再恢复 ACTIVE 测试状态验证离线拒绝。此为数据库测试状态准备，不代表产品提供重新开放操作，也不替代每日时间边界专项。新增已退班学生时禁用 helper 对已发布日程的重复设置。

所有失败保留，文件前缀均为 docker-session-e2e-current-，后缀均为 -20260908.txt：

- first：0/4，直接创建规则缺显式模板，数据库拒绝。
- retest：0/4，HTTP 发布缺完整每日时段，422。
- third：2/4，旧 fixture 和旧测试修改已发布日程。
- final 与 complete：3/4，关闭教学班测试数据缺完整关闭信息。
- verified：3/4，关闭状态仍开放进班违反约束。
- success：4/4，完整合法测试状态验证通过。

本轮仅测试和证据改动，没有修改运行时业务代码或 UI。完整 E2E、日时段边界及三端全量验收仍需继续；专项通过不能推算全量通过率。
