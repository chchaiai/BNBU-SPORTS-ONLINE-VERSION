# 补练窗口统一 E2E（2026-09-08）

新增 v81-makeup.e2e.test.ts，复用窗口专项并接入专用 Docker PostgreSQL、真实 SMTP/Mailpit、严格运行时协议校验。补齐旧专项课程发布遗漏的有效规则模板前置；本地收信读取器沿用与业务 API 分开的夹具读取方式。

最终完整场景通过，退出 0。验证教师在本人课程为有效成员创建窗口；管理员、其他教师和其他组织不能创建、列出或撤销；被移出成员、错误规则版本、超出常规/最终截止时间、缺少时刻的输入均被拒绝。创建重放不重复，窗口列表游标分页正确；并发撤销 201/409，获胜请求重放一致，只保留 CREATED/REVOKED 两条事件。

学生通过真实本地邮箱 OTP 登录后仅可读取本人窗口，能看到撤销历史；他人/未知入班记录 404，管理员和教师不能代读学生窗口接口。学生收到两条授权和一条撤销通知，理由与学校时区可见，通知列表一致且标记已读可重放。

首轮 docker-makeup-e2e-first-20260908.txt 因缺少 404 响应声明失败。补充窗口列表、创建、撤销及学生读取四项 NotFound，并在复测中实际验证全部四项；最终证据 docker-makeup-e2e-retest-20260908.txt。

生成检查及 249 操作/249 handler 对应检查通过。无 UI 或数据库约束修改。此测试验证补练窗口管理与学生读取，不等于在窗口内实际开始运动和计入进度的验收；这些需结合运动会话专项继续核对。完整门禁最近仍为第五轮 75/75 用例通过、107 项覆盖缺口，本轮未执行全量汇总。
