# CSV 体测草稿到正式结果 E2E（2026-09-08）

新增 v81-physical-imports.e2e.test.ts，在专用 Docker PostgreSQL 和真实 Nest HTTP 中验证体测 CSV 草稿、核对、确认。严格 OpenAPI 校验启用；对象存储端口使用明确的内存测试适配器，此证据不代表 COS/MinIO 实际存储验收。

复测 1/1 通过：含有效行与 4.30 歧义用时的 CSV 导入仅生成草稿；创建重放一致；草稿详情对其他教师404、学生403。混合确认有效行和未解决行返回422，前一行结果与通知全部回滚；教师改正草稿用时追加版本，旧版本编辑409；仅确认有效行成功，重复确认原key返回相同结果、新key409；学生只读取正式270秒结果，通知只产生一条；仍未解决的另一行保留待处理，原CSV内容与摘要保持，修改历史版本为2、1，列表确认数为1。

首轮导入详情404未声明导致严格校验失败，已补 getV81PhysicalImport 的 NotFound 并生成协议产物。249操作/249handler静态对应检查通过。原始日志：docker-physical-imports-e2e-first-20260908.txt；复测：docker-physical-imports-e2e-retest-20260908.txt。没有修改 UI 布局。

成员与身份为合成测试夹具；该专项不是 XLSX、OCR、COS文件持久化或免测验收。完整全接口门禁最新仍第六轮88项缺口，其余业务和三端浏览器仍需继续。
