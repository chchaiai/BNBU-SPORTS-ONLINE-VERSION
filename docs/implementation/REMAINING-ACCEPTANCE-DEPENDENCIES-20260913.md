# 剩余验收依赖

2026-09-13，整体目标未完成。当前发布为 semester-header-20260913。

本轮重新核对三份业务文档的功能章节、cloud-operation-coverage.json 的无证据项，以及 source-download-browser-attempt.json 的实际下载缺口。接口索引是历史索引，不能据其中的 219 次成功覆盖计数判定当前版本全功能通过。

## 需要用户输入的项目

1. 真实收信配合：学生验证码登录请求与消费、账号恢复请求与消费、分管理员身份挑战请求与消费共六个接口。已发出的“现在可以接收 / 稍后再做”问题尚未回复，不能把旧验证码或合成邮箱状态当作新流程成功证据。
2. 学生浏览器文件选择：Edge 扩展文件访问权限的问题尚未回复，不启用权限、不绕过选择器限制。
3. 业务规则：名单内未注册学生、缺体测且无已批准免测时，是否允许教师说明原因后结算。既有问题未回复，保留现有 UNAVAILABLE 行为，不自行确定业务含义。

## 浏览器下载

原有 IAB 点击证据没有落地文件；Edge 当前有真实管理员会话，不能以按钮点击或 HTTP 源文件字节一致替代实际浏览器下载证明。

本轮只读尝试 `curl.exe -I --max-time 15 https://teacher.bnbusports.cn/`，退出码 1，返回 `schannel: failed to receive handshake, SSL/TLS connection failed`。备用域名暂不能提供独立浏览器来源；未改变任何域名、证书、用户会话或云配置。主 www 域名的可用性不由此失败推断。

## 交付前仍需完成

上述操作证据、最终业务流程回归、按依赖清单清理样本及验证、最终结果与交接报告。现有 COS HEAD 仅覆盖数据库引用的 45 个键，不能证明无孤立对象；清理前仍需完整审查。旧公开帮助成功详情与原生 app-release-policy 的历史索引缺口需在最终报告中按当前 Web 范围明确说明，不能伪造成功记录或无说明删除历史缺口。

本记录列出依赖，不宣告所有其他功能已验收，也不替代完整交付审计。没有执行数据库或 COS 删除，没有新增权限、费用或密钥。
