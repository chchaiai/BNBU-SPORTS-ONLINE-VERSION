# 腾讯云第二轮缺陷修复与验收

输入：根目录 `bug.md`（BUG-01—BUG-07）。开始基线：`2b3052cce5a187330c52776449204a2604ae0afb`，分支 `codex/bnbu-sports-web-new-20260907`，开始时仅 `bug.md` 未跟踪。现行业务依据 `docs/business/` 四份 V8.1 文档。

## 当前状态

本地真实 Docker、HTTP/数据库和网页回归通过，已部署腾讯云。当前发布目录为 `/opt/bnbu-sports-production/releases/f87dba74f39d-closure`：Backend/Portal 来自 `f87dba74f39dc2ad7198a79d037e47ab8831c1d7`，Migrator 保持 `27f9f25abe884ab438acbe433179f8ff8b38acea`，学生端包含 `481f163a` 的进度加载、`2d4a49ca` 的详情时间修复及 `0bc25975` 的按钮删除与每日时段展示，后续三项验收见 `STUDENT-UI-20260911.md`；免测申请加载时序修复及完整验收见 `EXEMPTION-ACCEPTANCE-20260911.md`。学生邀请流程刷新宽限按钮删除及验收见 `JOIN-GRACE-20260911.md`。基础功能及后台连续性修复验收见 `BASIC-CONTINUITY-20260911.md`。课程关闭自动移出、历史名单修复及存量修复见 `COURSE-CLOSURE-20260911.md`。七项缺陷及追加独立学生、教师的1814秒腾讯云打卡整链验收均已完成。新增显示问题已修复并通过本地及线上复测，临时测试账号已停用，测试记录保留。本地存档不推送。

| Bug | 已定位事实 / 根因 | 修复与验收 |
| --- | --- | --- |
| 01 | 两端直接插入 estimatedRecoveryAt 原始 ISO 字段 | 共用浏览器本地时间格式化；上海/纽约时区测试及两端真实维护网页显示通过 |
| 02 | auth refresh 在维护时保存明确 503 幂等结果；两端始终复用同一刷新键，恢复后重放维护拒绝 | 明确维护拒绝后释放刷新意图，网络未知仍复用原键；历史恢复行公告显示不适用；两端真实过期令牌及不刷新自动恢复通过 |
| 03 | Backend、协议及 UI 将完成数量/比例固定为 null / 不可用 | 按有效成员逐人复用分类进度计算，包含有效认证；数据库/HTTP边界通过，管理网页33门合成课程进度条与API一致 |
| 04 | CSV 行校验输出的8个错误码没有对应文案，全部回退为操作未完成 | 明确字段/批次重复/已存在原因；本地网页合法批量创建、刷新读回、首改门禁及HTTP重复重放通过 |
| 05 | 线上 requestV81SubadminIdentity 返回 CONFLICT_RESOURCE_ALREADY_EXISTS（9次），UI catch 无条件投影为结果未知；创建验证码及跨角色邮箱唯一范围与批准规则不符 | 总管理员确认身份创建；共享学生邮箱、角色隔离、同键重放及资料管理通过。用户提供的邮箱线上创建成功，临时分管理员已停用，原学生仍启用 |
| 06 | readVideoPreview 仅在 loadedmetadata 读取时长，未监听分片MP4后续 durationchange；错误中显示 live_video 文件名 | 监听时长更新、友好素材名称；模拟时长异常能预览；真实30分钟会话、照片和视频上传提交、审核及0.5小时读回通过 |
| 07 | 线上两条记录均 PENDING_AI，课程无人工审核模式；reviewV81Record 15次409；通过动作原来没有填写公开说明的步骤 | 随审核填写公开说明，默认人工队列；已通过总管理员页面为两门真实课程保存开启原因，原两条记录均转为 PENDING_TEACHER，原责任教师继续负责；合成记录公开说明审核及学生读回线上通过 |

## 本轮业务确认

- 用户确认 BUG-07 只修复随审核提交的公开说明。
- 用户确认“优先人工审核，AI审核功能敬请期待”。未配置课程默认人工审核；显式已有配置保留，线上待 AI 记录通过现有受控人工模式迁移并留审计。
- 用户提供测试邮箱后指出可能是学生邮箱。复核发现现行文档只要求不与其他分管理员重复，当前后端却检查所有用户邮箱并要求验证码。验证码只能确认邮箱控制权，不能代替人员身份核验。用户随后明确“允许”：创建由总管理员确认身份并留审计，不发创建验证码；允许与学生邮箱相同，其他分管理员仍唯一。创建不虚构邮箱已验证，修改邮箱及找回仍需验证码。

## 修复前线上只读证据

香港 `43.129.193.7`，hostname `VM-0-13-ubuntu`。current=`/opt/bnbu-sports-production/releases/b83b302c-bugfix2`，Backend/Portal均healthy。公网与服务器当前模式均NORMAL，policyVersion=5，updatedAt=`2026-09-10T14:17:43.772Z`。最近两次维护→恢复事件持久化完整；恢复事件 announcementPublished=false 是没有维护公告发布，不代表恢复失败。

使用香港专用SSH身份；默认 `.ssh` 密钥未获授权，随后核对香港密钥登录成功。只读容器不允许docker cp，改用stdin执行只读聚合检查，无需改变容器安全配置。日志仅提取操作、状态、错误码及计数；不存储账号、请求体、凭据或签名地址。

## 验收记录

- Backend typecheck通过；Portal tsc及既有API/课程目录23项通过。
- 本轮新增本地时间与学生维护刷新2项通过；Portal新增同类刷新回归随后纳入完整129项测试通过。
- 原教师浏览器脚本因旧“学校邮箱”标签超时，保留失败事实；适配现有login-account后真实创建2名教师成功。
- 本地浏览器摄影切换、12秒录像跨轮询、二维码解码通过；这不替代腾讯COS和最终提交验收。
- 最新 Backend 单元 260/260；真实 Docker 集成 59/59、端到端 102/102、安全 53/53。Portal 完整构建及测试 128/128；初次完整测试发现缺少翻译，补齐后复测通过。
- `.local/round2-real-media-progress-retest.log`：真实 MinIO 上传、扫描、教师原文件读取、匿名拒绝、人工审核、公开说明及学生读回、审核幂等重放均通过。认证只完成一类不算全部完成、两类均完成计入、移出成员不计分母、认证撤销回退、未发布目标为空等数据库/HTTP 边界通过。初次新合成账号后缀超长导致测试数据校验失败，修正测试数据后重跑，保留首次失败日志。
- `.local/round2-video-preview.log`：原生 MP4 与模拟浏览器 duration=Infinity 两分支，均通过录制、暂停、继续、结束、缩略图生成、实际播放。截图 `.local/round2-evidence/video-unbounded-metadata.png` 已人工查看，播放帧与“现场视频”标题正常。该项属于合成摄像头浏览器回归。
- 新隔离学生的 30 分钟真实计时回归（purpose=round2-positive-credit）已经完成；1813秒、教师公开说明、学生0.5小时读回通过。
- 补充定位：教师将已有效记录改判无效误用首次审核接口。已接现有 corrections 接口并要求纠正说明，真实网页验证通过；原判断历史保留。
- `.local/round2-submit-retry-final.log`：真实后端提交完成后主动丢弃响应，再次点击提交，通过查询恢复原记录，只有一次 submit，按 sessionId 查询只有一条记录。脚本最初等待状态过早，随后标题匹配歧义，均已修正且保留各次日志。
- `.local/round2-teacher-review-short.log`、`.local/round2-student-reviewed-short.log`：教师网页填写公开说明并通过，学生刷新读到有效、未计入、0小时，短会话没有误记学时。
- `.local/round2-correction-browser-final.log`：教师网页完成 VALID→INVALID→VALID，未填纠正说明被前端阻止，填写后通过 corrections 接口保存公开说明，版本逐次增加。此前测试同时揭示加载中半成品展示：记录先更新、名单后更新，暂时出现14条记录/0名学生；已改为组合数据一起更新并显示加载提示。Portal再次完整构建及128项测试通过。
- 已通过的日志与视频预览截图开始归档于 `docs/implementation/evidence/bugfix-round2-20260910/`；已检查不包含本地已知密钥及账户密码。日志中的账号/组织/记录标识来自隔离的合成数据。
- `round2-real-checkin.log`、`round2-teacher-review-positive.log`、`round2-student-reviewed-positive.log`：实际计时1813秒，教师网页公开说明审核通过，学生刷新后读到0.5小时并在详情看到原文公开说明。
- `round2-maintenance-expired-browser.log`、`round2-maintenance-auto-browser.log`：仅为本地隔离组织的已登录合成账号签发过期访问令牌，保留真实刷新凭据；两端均真实收到刷新503，恢复后用新键刷新200。手动刷新与不刷新自动轮询两种方式均通过，结束时本地恢复NORMAL（policyVersion49）。
- `round2-subadmin-retry-final.log`：真实账号创建已提交后主动丢响应，再次点击后相同幂等键返回同一账号；错误验证码可纠正后重试；刷新、停启用、资料修改、邮箱变更及合成账号删除均通过。验证码核验/创建阶段也改为输出已有安全错误投影，避免统一掩盖真实原因。
- 最后一次 Portal 全构建及129项测试通过（`round2-portal-final5.log`）。新增队列测试明确待AI/技术/待补证不计教师当前待处理，全部记录仍保留。

## 剩余依赖与继续执行记录

本地源码及回归证据已按明确的73个路径提交为 `576db9b4dce40434f99f52cfb653f07d20a5243a`，未推送。归档日志仅去除行尾空白，原始输出仍保留在忽略目录 `.local/`。

该源码的 Backend、Portal 固定版本镜像均构建通过。Portal镜像使用UID/GID10001、只读根文件系统和移除全部capabilities的本地容器启动，首页HTTP200；临时镜像检查容器随后停止。镜像ID及源提交见 `evidence/bugfix-round2-20260910/image-build-receipt.json`。此检查不代表腾讯云发布或线上业务验收；如邮箱规则导致代码变更，须重新构建对应源码镜像。

1. 用户已在Edge“缺陷验收”标签页登录线上总管理员，已目视确认系统概览及总管理员身份；无需提供密码。
2. 用户已允许分管理员规则调整，已完成新迁移0060、协议、认证隔离和网页实现；创建不再需要收码配合。
3. 新规则回归：Backend单元260、真实Docker集成59、端到端103、安全53全部通过；Portal构建及129测试通过。真实网页共享学生邮箱创建、无创建验证码、提交丢响应同键恢复、停启用、编辑邮箱验证码及删除通过。旧OTP创建兼容路径仍通过。
4. `tools/production/deploy-bugfix-round2-20260910.py` 已执行成功：全量源提交、七项本地PASS、30分钟PASS、发布包SHA256、原nginx SHA256及旧current校验通过，私有数据库备份通过，0060事务迁移及权限收紧成功，两个公网入口健康。随后仅更新Portal旧提示文案，固定镜像ID验证后发布成功。回滚保留兼容的新列和数据，不恢复会拒绝共享邮箱的旧唯一索引。
5. 线上待AI记录使用已授权受控人工模式迁移并留原因、事件与审计，仍由原责任教师判断；不为通过验收而替真实学生作教育结果判断。合成账号与合成课程用于可重复的写入/媒体/审核验证。
6. 首轮腾讯云 PostgreSQL/COS/API/网页验收已完成，追加独立账号30分钟网页整链亦已通过。截图复核发现的两端详情显示问题继续修复并单独验证，详见下方追加记录。

## 发布和本地存档门禁

逐项本地真实Docker HTTP/数据库/网页及媒体回归通过后，固定镜像、精确文件清单、当前release校验、备份及自动回滚保护下部署。现有业务数据不清空，不对既有账号做合成测试修改。需要业务配置裁定的事项保持PENDING。最终补充逐项线上验证、剩余依赖和回滚交接，再按明确路径本地提交。

## 2026-09-11 共享邮箱回归补记

新增0060事务迁移已在真实本地及腾讯云PostgreSQL执行；60项迁移静态检查及清单校验通过。首次尝试不存在的test:unit命令失败，改用仓库已有npm test后260项通过。旧576db9b4镜像未用于新规则发布。迁移后本地原1813秒打卡仍能登录、刷新并显示0.5小时。

## 首轮腾讯云线上验收

| 项目 | 真实环境结果 |
| --- | --- |
| BUG-01 | 学生端、Portal均显示北京时间“2026年9月11日0点35分”；Portal英文为Sep 11, 2026, 12:35 AM；截图人工核对通过 |
| BUG-02 | 实际维护版本6→正常版本7；两个匿名浏览器页面不刷新自动退出维护；总管理员历史恢复行显示“不适用（恢复正常）”。过期令牌维护503后的新键刷新200已在本地真实环境覆盖，线上本次不伪造真实用户令牌 |
| BUG-03 | 管理页面两门真实课程显示0/3和0/5；合成课程真实API/数据库验证一类达标不完成、两类达标完成、移出成员不计分母、撤销认证回退 |
| BUG-04 | 合成组织真实HTTP批量创建教师、重复批次重放、数据库写入及初始改密门禁通过；原始重复数据返回明确错误 |
| BUG-05 | 用户提供的学生邮箱成功建立独立分管理员，无创建验证码；该分管理员已停用、首次改密仍为true、emailVerifiedAt未被虚构；同邮箱学生保持ACTIVE/PRIMARY和原验证状态 |
| BUG-06 | 真实COS图片及715711字节MP4上传、扫描可用、教师原文件字节匹配、匿名访问拒绝通过；追加真实浏览器拍照、10秒录像、1814秒服务器计时和正学时整链通过 |
| BUG-07 | 合成记录人工审核公开说明、学生读回、幂等重放、短会话0学时通过；真实两条记录从PENDING_AI转PENDING_TEACHER，两门课程人工配置已留原因 |

首轮媒体合成组织所有账号已停用。用户邮箱对应的临时分管理员只分配审计查询权限，已停用；保留治理和审计记录。原真实学生记录的教育结论由原责任教师决定。

维护验收首次日期控件自动化输入不完整，未产生模式变更；改为逐段键盘输入并核对控件有效值后提交成功。断言先按冒号时间和单语标题匹配导致脚本失败，改按页面实际中文时间和恢复时间字段复测通过，首次输出保留在`.local/`。发布后发现分管理员列表顶部旧提示与新规则不一致，已补发Portal `fad4083c`修正。

## 回滚与运行交接

- 私有备份：`/opt/bnbu-sports-production/backups/before-bugfix-20260910T161437Z.dump`，2171637字节，备份目录校验通过，权限0600。
- 初次发布前目录：`/opt/bnbu-sports-production/releases/b83b302c-bugfix2`；主修复目录：`/opt/bnbu-sports-production/releases/27f9f25abe88-round2`；提示修正版目录：`/opt/bnbu-sports-production/releases/fad4083c023f-round2`。
- Portal可回退到主修复目录的固定镜像，使用同一数据库和静态学生端。0060是兼容扩展，服务回滚保留该迁移；不得直接恢复旧跨角色唯一索引或用旧备份覆盖新增业务数据。未在有真实用户数据的生产库演练破坏性恢复。
- 系统已恢复NORMAL，版本7；两门真实课程人工审核已开启。AI审核维持“敬请期待”；OCR未在本轮新增启用或扩权。
- 日志与证据不存储真实凭据、签名下载地址、测试会话令牌或服务器私钥。追加长流程测试的私有账号/会话资料仅存于忽略目录，结束后撤销访问。

## 用户追加：独立学生与教师的腾讯云完整打卡

在新建隔离组织 `01a08c2b-3452-75dd-aec7-0411e8de38a3` 中创建学生、责任教师、课程和30分钟门槛模板。真实浏览器调用 getUserMedia/MediaRecorder 拍照和录制10秒视频；输入是自动化摄像头测试画面，未声称使用真人手机摄像头。计时使用真实服务器时钟，实际 ACTIVE 时长1814秒，没有改数据库时间或加速时钟。学生登录会话通过既有 AuthService 为隔离账号建立，因此不把此步骤记为生产SMTP验收；教师使用密码网页登录。

| 流程 | 证据与结果 |
| --- | --- |
| 记录、暂停、继续 | 实际1814秒，跨越访问令牌有效期后自动恢复；数据库保留2代刷新令牌 |
| 拍照、录像 | JPEG 10468字节、MP4 696122字节；通过真实拍摄控件形成文件 |
| 提交、COS | 记录 `01a08c4e-79a4-715a-9c1f-8683361e6eeb`；提交后待教师、学时为0；照片和视频均AVAILABLE |
| 教师核验 | 原始图片成功解码，原始视频实际播放超过1秒；网页填写公开说明并审核为VALID |
| 公开说明 | “腾讯云完整打卡验收：已核对现场照片和视频，运动记录有效，计入通识运动学时。”学生详情和通知正文逐字匹配 |
| 学生反馈 | 通知可打开并标记已读；记录显示有效、已计入、0.5h |
| 学时进度 | 权威API为其他运动1800秒、课程相关0秒；网页显示30/1200分钟、剩余1170分钟 |

首次测试脚本曾因会话恢复字段、新手指引异步出现、等待条件及把分钟误当小时而失败；修正测试脚本后使用真实数据复测通过，首次日志保留于`.local/`。第一个未提交测试会话已通过取消接口取消，最终只有一条提交记录。刷新时一次401随后恢复成功，跨30分钟的真实刷新代数为2。

### 追加发现的显示缺陷

1. 学生进度页在异步数据返回前把空工作区的0当成真实进度。增加加载占位，数据到达后再显示权威值。3项单元回归覆盖加载、真实0和30分钟；本地Docker及腾讯云均通过延迟真实HTTP响应、原样返回数据的浏览器验收。源码提交 `481f163a959ebc1600c90022f927b3d4bde05cc8`，静态发布验证公网SHA256一致。
2. 教师端 `toCreditedDurationHours` 仍只接受旧0/60/120分钟阶梯，将真实30分钟显示为“异常”。现行业务依据 `docs/business/00-overview.md:316` 明确不采用该旧阶梯。改为展示服务器给出的非负整分钟，保留历史记录的原计入值；非整小时用分钟精确显示，不重新计算记分。
3. 教师记录映射把 `businessDate` 填入 `submittedAt`，格式化后错误显示当天12:00。改为保留服务器提交时间戳；业务日期仍由后端决定，不被提交日期替代。
4. 学生详情的 `startTime` 固定为空，`endTime` 错用提交时间。复用已在读取的 evidence-context，将实际开始、结束时间写入详情；没有新增接口请求或推算运动时间。

后三项源码提交 `2d4a49cabd79a9d7b40eef1984334152e07b70c8`。Portal构建及129测试、typecheck、13项针对性回归通过；本地真实Docker两端详情与API逐项比对通过。只读、非root镜像启动HTTP200通过。腾讯云固定镜像部署和学生模块公网SHA256校验通过；同一条记录的两端网页最终复测通过：教师显示30分钟和01:12提交，学生显示00:42开始、01:12结束以及原文公开说明。

首次整链媒体截图保留了教师端“异常”和错误12:00，属于发现缺陷的原始证据；最终应以 `*-record-teacher-corrected.png` 和 `*-record-student-corrected.png` 为显示修复证据。学生开始/结束时间取自同一证据上下文，未把提交时间推算为开始或结束。

### 本阶段运行与交接（后续发布以本文当前状态及专项报告为准）

- 本阶段 Portal 镜像 `bnbu-portal-production:2d4a49ca-round2`，ID `sha256:612b35637ce1bf2774b4d7a60b933b8410087041d33409b4bf783a495e218dcb`；Backend ID `sha256:c332feb284f338de37fd59f7771a03cabd79a0c2cdf7f2c6b6bf7abf67ccd953`。两者最终均healthy，系统NORMAL、版本7。
- 本次详情发布可回退到 `/opt/bnbu-sports-production/releases/481f163a959e-round2`：用该目录固定Portal镜像恢复服务并原子切回current；数据库不回退。该目录仍有教师旧显示问题，但包含进度加载修复。更早版本与数据库备份见上文。
- 长流程隔离组织及配对隔离组织最初5个账号全部停用；发现详情问题后仅重开原测试学生、教师用于只读复测，完成后再次停用两者并增加tokenVersion。记录数量始终1；旧测试学生会话访问 `/me` 返回401，访问撤销验证通过。未审核既有真实学生记录。
- 两条真实打卡仍为PENDING_TEACHER，对应课程已开启人工审核；由原责任教师查看材料并作结论。这是正常业务交接，不是本轮测试失败。AI审核按用户决定保持“敬请期待”。
- 用户提供邮箱对应的临时分管理员保持停用；同邮箱原学生保持启用。停用分管理员仍占用分管理员范围内的邮箱唯一性，后续如要正式使用该邮箱，应通过现有账号治理处理测试账号，不直接改数据库或修改学生身份。
- 本轮未覆盖真人手机摄像头硬件、全部手机浏览器或生产SMTP收码；这些不以自动化摄像头/隔离会话测试冒充通过。腾讯COS、真实浏览器录制、服务器计时、上传、教师审核、通知与学时入账已实际验证；OCR未新增启用或扩权。
- 证据目录 `docs/implementation/evidence/bugfix-round2-20260910/`，最终关键日志：`round2-cloud-long-browser-capture.log`、`round2-cloud-long-review-progress.log`、`round2-record-display-cloud-browser.log`、`round2-cloud-long-final-cleanup.log`、`round2-cloud-access-revoked.log`、`round2-complete-online-state.log`、`round2-complete-runtime.log`。凭据只在忽略的私有测试资料中，未纳入提交。
