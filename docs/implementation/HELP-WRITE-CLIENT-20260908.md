# 帮助管理网页写入模块（2026-09-08）

help-api.ts 增加详情读取与保存请求意图。创建意图时复制完整输入并冻结关键词及请求体，固定路径和幂等键；同一意图的并发调用共用一个请求，失败后可使用原内容和原键重试。保存接口支持创建、编辑及已定义状态变更。

`docker-help-write-client-first-20260908.txt`：Docker 退出 0。实际加载网页模块调用后端，验证创建响应丢失、修改外部输入不污染原请求、同键重试仍是第一版、并发重试合并、详情一致、发布/下线/重新发布的可见性以及旧版本提交拒绝后结果不变。已有帮助生命周期、学生 SMTP 登录、学生网页 API、权限和组织隔离回归通过。

`help-write-client-typecheck-20260908.txt`：Portal tsc 退出 0。本轮未修改界面、后端协议或数据库结构。浏览器按钮接线和刷新后的请求恢复尚未完成，当前真实帮助页面仍只读；不能把模块验证作为页面管理闭环验收。未执行云端操作。
