# 有课程的正式结算与学期切换

2026-09-08。docker-settled-semester-switch-first-20260908.txt 退出 0，完成单课程旧学期的真实 HTTP 正式结算、切换归档、同键回放、历史报告读取及导出验证。

隔离数据库先准备单课程当前学期：将没有成员的其他基础测试课程放入历史学期，随后才建立本次名单和体测业务事实。规则使用已明确标注的历史发布时间和截止时间，服务器使用真实时钟。名单导入原始行是测试数据库夹具，名单确认、补齐体测、正式报告和学期切换由现有真实接口处理；没有直接写入结算报告或把本次旧学期手动归档。

正式报告生成后，全学期预检查 ready=true；POST switch 成功使旧学期 ARCHIVED、新学期 CURRENT，同键回放相同结果，数据库只保留一个当前学期。归档后教师读取第一版报告与切换前完全一致，导出文件名一致，课程成员行逐项保持。原有缺体测拒绝、旧预览指纹拒绝、权限隔离、结算并发仅一份及幂等回放检查继续通过。

此证据补齐此前仅空旧学期切换的缺口，但不覆盖整个学期实时时序、多课程大规模性能、运动及补证全生命周期、归档后所有纠错类型、新学期完整业务链和页面操作。完整三端目标未完成。本包仅新增测试与证据，产品和 UI/UX 未改变；没有重新运行全量契约检查。原始日志保留，源码和手写文档单独检查空白。
