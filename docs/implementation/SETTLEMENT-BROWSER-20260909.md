# 课程结算与综合名单网页闭环

2026-09-09 08:52。用户“确认”批准六项最小必要调整。本项在原课程设置弹窗内增加结算区，沿用弹窗、表格和按钮样式；课程卡片增加不影响显示的教学班标识属性，便于同名课程定位。

接线覆盖 settlement-check、settlement-preview、settlement-reports 确认/历史分页、composite-roster/export 和指定版本报告导出。页面按服务端检查显示待办，核对名单并勾选确认后才提交。确认意图按当前用户与教学班保存在 sessionStorage，刷新后重试同一请求。已知校验/版本或状态冲突要求重新预检，网络未知保留原意图。导出验证 XLSX 类型与 ZIP 文件头，生成浏览器下载。实时预览与已结算快照明确标记。

真实浏览器连接当前 Docker backend-browser + PostgreSQL：settlement-browser-retest5-20260909.txt 为首次确认完整通过，包括缺少体测禁用确认、补齐后实时 XLSX 下载、确认成功后丢弃响应、刷新后原幂等键重放得到 201、同一 v1 报告下载和刷新回读。settlement-browser-export-retest-20260909.txt 重读既有报告，解析两份 XLSX，验证报告性质、课程标识、1 名名单学生、原始用时 270 秒、快照版本及结算检查工作表。该次 newSettlement=false，不计为第二次新结算。类型检查退出 0；已查看本地忽略截图，原弹窗内滚动可达报告及后续设置。

测试前提为明确合成数据：seed-v81-settlement-browser.mjs 创建历史日期规则、学生和电子名单来源，通过真实 API 确认名单与提交体测；浏览器实际完成结算，没有预植报告。没有声称从新学期开始等候真实截止日，也没有新增后端全量测试结论。

失败保留及处理：
- 初始脚本学号后缀小写违反 student_profiles_student_number_check，改大写。
- 独立模板版本 1 违反连续版本约束；改复用组织现有已发布模板。随后旧发布时间早于模板触发 Course template must be published in the same organization，调整合成课程发布时间为当前时间。
- 初始教学班 UUIDv4 违反名单查询 UUIDv7 协议，页面加载名单 422 后清空数据。未放宽产品校验。两条失败合成教学班 f28b3253-9077-47b9-a382-5d945d54dfbb、43d1ddf5-3bd5-487d-bacb-386133c51dae 按脚本中明确 ID、组织、创建者及合成名称限定关闭并保留；旧忽略 fixture 另存。修正后的教学班使用 UUIDv7。未删除账号或历史事实。
- 初次浏览器按教学班名称定位，但卡片显示课程名称；改用卡片教学班属性并点击可见按钮。
- retest3/4 对未录体测预期“待处理”不符后端 UNAVAILABLE；按实际“待确认”校验。原日志均保留。
- 日志中已有未发布课程的 progress-target 404 是页面约定的“尚未发布规则”状态，最终运行无名单查询 422。

仍待后续全范围验证：历史报告超过一页、跨身份并发、存储损坏/满额、结算后网页更正入口，以及管理员学期归档整体操作。补练、原始体测编辑、本人名单和通知四项批准入口继续实施；本项通过不代表三端全部功能完成。
