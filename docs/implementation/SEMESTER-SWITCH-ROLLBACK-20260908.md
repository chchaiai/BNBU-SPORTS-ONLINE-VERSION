# 学期切换故障回滚与权限验证

2026-09-08。扩展既有真实 HTTP Docker 探测，未修改产品实现或 UI/UX。

docker-semester-switch-rollback-20260908.txt 进程退出 0，验证：

- 分管理员无权限或仅 COURSE_VIEW 时切换返回 403；具备 SEMESTER_MANAGE 时通过权限层，由旧版本校验返回 409。
- 在隔离 v81_runtime_test 中临时安装只拦截 CURRENT_SWITCHED 事件插入的测试触发器。命令执行到审计写入时返回预期 500，两个学期的状态、版本和时间均与调用前一致，没有切换事件。
- finally 删除指定触发器和测试函数。同一幂等键重新提交成功，再次重放返回同一结果；并发同键回放与新键旧请求分别 201/409，最终仅一个当前学期和一条切换事件。
- 原有日期、未结算、字段、角色、组织、版本、历史保留和学期读写回归继续通过。

正向仍使用空旧学期夹具，未证明含课程的全学期结算后切换。维护模式写入专项、管理员页面接线和完整三端验收继续待完成。本次未运行全量契约检查或扩展性能测试。

源码核对另发现：class-sections 服务允许在 UPCOMING 学期预建 UPCOMING 课程，运动入口要求课程 ACTIVE；现有学期切换只更新学期。现行业务文档 20-teacher-flow.md 第 90 行要求教师在当前学期创建课程。后续需按现行业务约束收敛预建入口，并审查已有 UPCOMING 数据的衔接，不能据本测试宣称新学期课程操作均通过。

本地存档仅包含探测、日志与交接。原始日志保留；源码和手写文档单独检查空白。完整目标保持未完成。
