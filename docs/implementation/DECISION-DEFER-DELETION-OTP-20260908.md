# 用户最新范围决定

用户明确要求：学生注销、教师删除暂时不可用，提示“暂时功能无法实现，敬请期待”；分管理员使用邮箱 OTP 核验身份；其他技术选择自行合理决定，以三端功能良好可用为目标。

该决定取代此前将学生注销和教师删除列为本轮必须实现的完整业务。原历史主体及历史读取实现保留，账号删除、偏好清理及其历史失败作为后续能力，不再阻塞本轮三端验收。分管理员邮箱核验按 OTP 方案继续实现，不再等待核验来源答复；新邮箱也按本人验证后生效处理。

已落实暂缓行为：

- 学生原注销入口调用已有弹窗组件，展示指定中文及对应英文，只提供关闭按钮，不进入验证码流程。
- 后端注销挑战入口返回 503 SYSTEM_SERVICE_UNAVAILABLE，公开 currentState=FEATURE_DEFERRED；不再发放注销挑战。旧页面错误映射同样展示指定提示。确认删除接口仍未开放。
- 教师资料抽屉保留原布局、文案区域及按钮位置，删除按钮停用并显示指定提示。正式教师删除接口原本未开放，继续不提供删除。

验证：docker-deletion-deferred-final-20260908.txt 退出 0，52迁移/构建/基础账号通过；真实学生邮件登录后两次请求均被拒绝，无注销挑战、账号不变，/me 仍可访问。学生 profileActions 本地 Node 验证弹窗正文、唯一关闭动作且未导航进入注销流程，通过。教师抽屉本轮做源码差异检查，浏览器视觉与点击验收待完成。

过程中的两次失败保留：docker-deletion-deferred-20260908.txt 为 ErrorDetails 不允许 feature 字段；docker-deletion-deferred-retest-20260908.txt 为内部 reason 未向客户端暴露。已移除非协议字段、改用现有公开 currentState，最终复测通过。

分管理员 OTP 创建/资料编辑仍未实现，不把方案决定当作交付。结算及其余技术实现继续按仓库现行业务文档合理推进；不因本决定放开学生成绩、角色越权或改变既有布局。
存档检查：明确暂存16个文件；git diff --cached --check退出2，仅4个原始Docker日志尾随空格，保留输出并记录未通过。持续后端刷新后本机就绪200。
