# 规则模板统一 E2E（2026-09-08）

系统管理 E2E 复用现有 v81-rule-template-probe.mjs，将独立专项接入严格运行时 OpenAPI 校验及 npm run test:e2e。Docker 复测系统管理 4/4 通过，退出 0。

真实业务断言：总管理员发布固定规则模板、幂等重放；教师可查询和选择模板但不能发布；分管理员即使具有 GLOBAL_RULES 仍不能读写模板管理入口；跨组织模板详情不可读。模板规则保持 1200 分钟目标、30/45/60 单次门槛、72 小时特殊补证等既有定义，拒绝请求注入自定规则字段。

教师发布课程必须选择有效模板，缺失模板 422、未知模板 404；规则未配置前读取 404。课程发布及重放成功后，两个并发新模板请求仅一次成功，游标分页可读两版。原课程继续引用原模板，不允许替换已发布规则。数据库直接更新/删除模板被拒绝，事件仅两条。

首轮 docker-template-e2e-first-20260908.txt：系统管理 3/4，跨组织模板详情 404 未在协议声明。补充模板详情、课程规则读取及保存三项 NotFound。复测实际覆盖全部三种 404，见 docker-template-e2e-retest-20260908.txt。

生成产物检查和 249 操作/249 handler 静态对应检查通过。未修改 UI、模板业务常量或数据库约束。全量门禁本轮未执行，最近完整结果仍第四轮 69/69 用例通过但覆盖门禁 126 项失败；本专项不代表三端完整验收。
