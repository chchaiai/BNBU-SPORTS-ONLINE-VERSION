# 第二十一次完整 Docker 后端回归

2026-09-09，隔离 PostgreSQL、Mailpit、MinIO 与 NestJS 测试环境；容器 bnbu-v81-e2e-twentyfirst-20260909 保留。

`docker-backend-e2e-twentyfirst-20260909.txt` exit 0：97/97 tests、25 suites，0 fail/cancelled/skipped/todo，测试运行 124.22 秒（不含构建）。217/217 enabled successes、251/251 error/access、34/34 fail-closed，运行时协议覆盖通过。

`runtime-conformance-twentyfirst-20260909.json` 和 `.md` 从本轮容器独立复制。相对第二十轮，包含新成绩事件明确成功结果及草稿、发布、更正、并发拒绝、幂等和报告失败回滚的加强断言；没有新增操作、迁移或测试条目。

验收主表同时根据已有浏览器证据更新课程目录、换算表和审计归档三行，去除过时待办，保留未完成边界。完整后端通过不能替代尚缺的网页入口、所有三端操作及真实腾讯云配置验收；整体目标保持未完成。

原始工具日志含行尾空白，按原样保留，源码及 Markdown 单独进行差异空白检查。
