# 结算及归档后的内部成绩纠错

2026-09-08。新增 POST /enrollments/{enrollmentId}/final-grades/corrections，责任教师提交整数成绩、发布状态、已有版本和非空纠错原因；拒绝备注字段。普通成绩写入的结算/归档限制继续保留。

专项沿用 Serializable 幂等事务及组织锁，核验责任、组织和成绩版本，追加成绩修订及 FACT_CORRECTED 审计，再生成更正版报告。报告服务验证匹配的教师/请求/原因/版本事件，并确认该成员在第一版结算前已有成绩事实；没有原报告或既有成绩时拒绝并回滚。报告纠错结构增加成绩分支 enrollmentId/gradeVersion，原运动记录结构保持兼容。没有新增迁移或 UI/UX 改动。

## 验证

- docker-final-grade-correction-first-20260908.txt：退出 0。归档状态下实际 HTTP 追加成绩98及新版报告、版本增长、同键回放、旧版本拒绝、空原因/备注拒绝、学生和管理员拒绝；旧报告内容与哈希不变，普通成绩写入409，学生结果不暴露成绩。
- docker-final-grade-correction-rollback-20260908.txt：退出 0。隔离库临时触发器使更正版报告插入失败，预期500时成绩修订和成绩审计逐项保持原值。finally 删除测试触发器和函数，同键重试成功；新报告 Schema 校验通过。
- 两次均回归既有运动记录纠错、报告追加、管理员摘要及人工审核/计入链。初始结算报告和归档状态为标注的隔离夹具，不替代真实学期切换验收。
- final-grade-correction-parity-20260908.txt：248 操作/248 handler，生成一致。contract-first 与 delta 文件确认旧391诊断无新增；旧检查器整体仍未通过。

下一步需非责任教师、并发成绩纠错、没有既有成绩/报告的回滚专项，以及结算后尚未归档分支和页面接线；体测与认证等其他事实纠错继续待补齐。持续浏览器后端本包未重建，完整三端目标未完成。源码/手写文档单独检查空白，原始 Docker 日志保留。
