# 学生帮助下线与重发同步（2026-09-08）

通过本地 Docker 管理员 HTTP 接口下线既有合成文章；学生真实邮箱登录后从帮助返回设置并重新进入，断言新响应无该文章且页面标题消失。管理员重新发布后，学生再次进入可见并展开正文。测试 finally 在失败时也恢复合成文章为发布状态。

`student-help-lifecycle-browser-final-20260908.txt` 退出 0，登录、下线不可见、重发可见及正文断言通过。产品及布局未修改。

保留首次脚本错误 `student-help-lifecycle-browser-20260908.txt`（Node Fetch status 属性误当方法）；第二次 `student-help-lifecycle-browser-retest-20260908.txt`（返回设置后误点击底层个人页设置按钮，浮层拦截）。最终修正导航通过，相关诊断截图及最终截图均保留。

本次证明在线重入会刷新缓存和文章可见性；不承诺停留页面自动推送更新或离线立即撤回已缓存内容。英文切换、多账号缓存隔离、离线与移动视口仍需专项；三端全量验收继续。未执行云端操作。
