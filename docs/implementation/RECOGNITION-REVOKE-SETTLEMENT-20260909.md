# 结算后认可撤销与事务回滚

2026-09-09。依据教师业务 §10.3、§12.3，原责任教师可以对结算前已批准的认证追加撤销更正。复用原撤销入口和必填原因，保留版本、权限、NORMAL 模式及原批准时间检查。撤销会停止认可贡献，在同一事务内生成更正报告，原报告保留；归档无结算报告、无有效原认可等情况仍拒绝。

调整和撤销产生的更正事件均关联原申请，报告记录该事件、认可版本、原因及上一报告。仍为 253 操作、59 迁移，无新增数据库迁移和页面变更。

## 验证范围

扩展真实 HTTP/PostgreSQL 结算 E2E，先完成批准、结算和学期归档，再执行认可调整与撤销。分别使用仅存在于隔离测试库的临时触发器，故意拒绝更正报告插入；检查申请、认可、审核、通知、审计、业务事件、报告均与失败前一致。移除触发器后以原键重试，并验证再次重放结果一致、没有重复报告。调整报告两类认可秒数为 900/1200，撤销报告为 0/0，旧报告完整内容与校验值保留，学生回读最新报告版本。

第 32 次初测在故障注入处失败：调整接口 500 响应未写入协议，严格协议校验拒绝响应，未执行其后的回滚断言。因此不能把初测当作回滚验证通过。已保留原日志，并给调整、撤销补充 InternalError 500；撤销还补充实际资源范围检查对应的 404。

第 33 次复测通过：97/97、25 suites、0 fail/skip，126220.504636 ms；219/219 启用成功，253/253 错误权限响应，34 默认拒绝。上述新增断言全部执行通过。原始日志为 `docker-backend-e2e-thirtysecond-20260909.txt`、`docker-backend-e2e-thirtythird-20260909.txt`，运行协议证据为 `runtime-conformance-thirtythird-20260909.json/.md`。本地预览重建日志为 `recognition-revoke-settlement-browser-build-20260909.txt`。

## 后续交接

结算后认可调整/撤销的浏览器点击、刷新恢复和报告实际下载仍待补。第 31 次记录中的后端全量类型检查 78 条测试文件错误尚未处理，不能声称全量类型检查通过。申请补材料、名单/OCR及其他全范围项目沿用 ACCEPTANCE 和总交接；云服务实际连通及正式模型验证仍未由本地测试证明。
