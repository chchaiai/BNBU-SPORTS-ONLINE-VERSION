# 耐力规则管理 Docker E2E（2026-09-08）

专用 Docker PostgreSQL、真实 Nest HTTP、严格响应协议校验。复测 1/1 通过。

验证默认四张表、404 条规则且读取不创建数据库配置；修改首条说明、幂等重放、新增末尾区间、删除新增区间、非法中间删除及等级分数不一致拒绝、旧版本冲突。并发同版本修改得到 201/409，历史版本完整保存 1 至 5。分管理员无权限或仅 HELP_CENTER 时读写 403，取得 GLOBAL_RULES 后可读写，撤回后立即再次 403；教师和学生不能写规则。其他三张表保持原值。

首轮测试学生身份夹具缺失，收到 401 而预期 403；已创建明确测试学生并复测通过。原始日志 docker-endurance-e2e-first-20260908.txt，复测日志 docker-endurance-e2e-retest-20260908.txt。此轮未修改业务实现或 UI，未验证换算规则变更对已有结算快照的完整影响；整体目标继续进行，全量最新仍第八轮 87/87、40 项门禁缺口。
