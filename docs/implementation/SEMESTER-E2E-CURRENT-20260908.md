# 学期管理统一 E2E（2026-09-08）

新增 v81-semesters.e2e.test.ts，连接实际 Nest HTTP、Docker PostgreSQL 18.4，严格运行时协议校验开启。创建待启用学期、同键重放、编辑及重放、列表显示、旧版本拒绝、教师管理权限拒绝通过。

复用切换预检与切换专项：预检按页返回全部课程，未来开始日期/未正式结算/不可用待办阻止切换，课程检查与教师接口一致；错误日期和游标输入拒绝，跨组织/未知对象不可读，预检不修改学期或事件。切换使用双学期版本，未来目标、未结算旧学期、错误权限和错误版本均拒绝。

成功切换使用明确标注的空旧学期夹具，不把原课程伪装成已结算。临时测试触发器使审计写入失败，验证 500 时两学期均回滚且无切换事件；finally 删除触发器和函数。同一失败请求重试成功，后续并发重放与新旧版本请求保持唯一 CURRENT，只有一条切换事件，原课程和入班历史完全不变。切换后可在新当前学期建课并重放，旧学期建课被拒绝。

原切换专项的数据库保护扩展为明确允许 bnbu_sports_test，且该库必须提供 BNBU_SPORTS_EPHEMERAL_TEST_DATABASE_V1 确认；原 v81_runtime_test 入口保持兼容。目标版本改为参数，默认 4，当前 E2E 使用实际编辑后的 2。没有扩大到任意数据库。

首轮 docker-semester-e2e-first-20260908.txt 失败于跨组织预检 404 未声明。补学期编辑/预检/切换三个 NotFound，以及审计失败切换的 InternalError，复测实际覆盖全部。docker-semester-e2e-retest-20260908.txt 完整场景通过，退出 0。

生成及 249 操作/249 handler 静态检查通过。没有修改 UI 或学期业务实现。此证据不替代含已结算课程的正式学期切换、浏览器成功切换或三端完整验收。完整门禁最新仍第五轮 75/75 用例通过、107 项覆盖缺口。
