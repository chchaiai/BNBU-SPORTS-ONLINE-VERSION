# 管理员学期页面真实接线

2026-09-08。admin-semesters.tsx 的正式模式复用现有学期管理列表、编辑表单及切换确认框，移除旧的只读占位分支。页面读取真实列表、人数、状态和版本，新增/编辑调用后端并回读；切换先取得服务器预检查，以确认时的当前/目标版本创建固定幂等意图。未到日期、未结算课程或待办阻塞在原确认框内展示，确认按钮禁用。

不新增 CSS 或改造原管理界面布局。正式模式使用本组件局部真实数据，不调用演示数据写入。写请求进行中合并/阻止重复提交；保存请求相同输入重试复用键；切换重试保留确认时的键及请求。

## 验证

- semester-page-typecheck-first-20260908.txt：Portal tsc 退出 0，原始输出为空。
- docker-semester-page-backend-20260908.txt：持续本地 Docker 后端重建并等待就绪完成，退出 0。
- semester-page-browser-first-20260908.txt：实际 Edge 登录本地管理员页面，新增 2033-2034 合成学期时后端成功响应被测试路由丢弃；页面显示失败后再次保存，同键且只创建一条。随后编辑名称成功，打开切换框看到真实阻塞，确认按钮禁用。进程退出 0。
- semester-page-blocked-20260908.png：已人工查看工具截图，确认现有管理列表及弹窗展示；此为桌面视口证据，不证明所有屏幕尺寸。

本地数据库保留本次合成学期，没有切换或归档持续浏览器环境的当前学期。成功切换 API 及丢响应重试已有 SEMESTER-PORTAL-API-20260908.md 真实 Docker 证据；本包浏览器只验证阻塞路径，尚未证明浏览器成功切换。

## 剩余事项

页面意图目前保存在内存，整页刷新后的恢复未实现；提交不确定时改变表单内容或关闭重开仍需进一步约束。尚需成功切换浏览器路径、移动视口、新学期完整业务及其他三端功能验收。完整目标未完成。本包未重跑旧契约检查和全量前端构建。原始日志保留，源码及手写文档单独检查空白。
