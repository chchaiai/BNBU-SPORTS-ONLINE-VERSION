# 反馈附件注销与原生诊断修复部署记录

2026-09-23 18:08（Asia/Shanghai）部署成功。现网版本为 `feedback-erasure-20260923`，上一版本为 `outbox-accounts-20260922`。

## 修复内容

- `0091_feedback_erasure_cleanup` 将反馈附件纳入管理员删除及学生验证码注销的既有函数，登记原始上传对象和确认副本的清理任务。旧上传链接有效期结束后再次删除，避免对象被重新写入。
- 附件准备、确认落库和反馈提交通过用户行锁与注销串行；用户已经删除时拒绝继续写入。
- 复制 COS 对象前持久化清理任务。并发确认只采用一个副本，其余副本、校验失败副本和注销期间完成的副本可重试清理。
- 学生原生诊断接受 `IOS`、`ANDROID`；Web 角色匹配规则保留，管理员不能冒用学生原生标识。

## 验证结果

- TypeScript 类型检查、Nest 构建、OpenAPI 生成一致性、迁移清单一致性与 91 项迁移静态安全检查通过。
- 9 项现有单元测试通过。
- 候选镜像在隔离 PostgreSQL 中通过 8 项场景：并发确认、扫描失败及存储失败重试、带已绑定及未绑定附件注销、同组织其他学生隔离、旧身份拒绝、确认与注销交错、上传链接到期后再次清理、验证码授权注销、原生诊断权限（相关断言归并在 8 项检查中）。
- 候选数据库只含合成数据；对象存储使用内存替身，因此上述测试不构成真实手机、生产 COS 删除或真人注销验收。
- 生产仅执行 `0091`，数据库记录的 SHA-256 为 `a8ef189667050669f2e37bfd71da7303c559e86a55a687289675a9036623db7e`。
- 部署后 6 个运行文件哈希一致，后端及 Portal healthy，学生站、教师站和公网 readiness 返回 200。
- 生产附件数量部署前后均为 9（已绑定 8）；发布没有删除真实账号。发布后观察窗口内未出现反馈清理重试或学生媒体扫描重试日志。
- 隔离测试容器和网络已移除。

## 发布与回滚

从实际运行镜像创建范围受限的覆盖镜像，Portal、学生静态文件及其他运行文件沿用现网基线。

- 后端镜像：`sha256:fba0a6404bc80f3746fd63c11afb1f39c6fd2e8d8610266d239cdc2ec8aa63d9`
- 迁移镜像：`sha256:e51cdc5e24199bcd14034b536c3c394d8de4a811fbbf0754ac801ff132499cd9`
- 备份：`/opt/bnbu-sports-production/backups/before-bugfix-20260923T100801Z.dump`，79,965,672 字节，私有权限，已验证备份目录可读。
- 回滚命令：`sudo python3 /home/ubuntu/bnbu-feedback-erasure-20260923/deploy-feedback-erasure-20260923.py --rollback`。
- 应用回滚保留新增表和兼容的注销函数，不自动恢复数据库。新清理队列依赖新 worker；长期停留旧版本时须安排队列处理。回滚路径已编写，未在正常生产上实际触发。

机器可读结果见 `evidence/feedback-erasure-20260923/deployment.json`；隔离测试见同目录 `isolated-tests.json`。

## 未完成的 TestFlight 依赖

- 现网没有有效 iOS 版本策略；实际请求 `platform=IOS&currentBuildNumber=8` 返回 503 / `SYSTEM_MODE_UNSUPPORTED`。没有依据选择最低支持 build、最新 build 或正式下载地址，因此本次没有编造或发布版本策略。已向用户请求截图中的 TestFlight 准备记录路径。
- 现网迁移清单与本地差异中的 `0084_ai_review_decisions` 是既有未部署项，本次未将它混入发布；本次新增迁移与运行文件已单独对齐并记录哈希。完整本地源码不能视为现网完整源码快照。
- 正式 App Store 分发构建、Apple 账号与应用记录、生产 APNs、TestFlight 安装后的关键流程、教师真人审核与真人注销清理、首次外测审核材料和 Apple 审核均未在本次完成。
