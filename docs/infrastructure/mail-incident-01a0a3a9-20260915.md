# 学生验证码 503 排查

诊断编号：`01a0a3a9-4de4-7443-81e4-09603e38504d`。

## 确认证据

- 香港持久 HTTP 日志：2026-09-15 14:02:57.305 CST，`requestStudentSignInCode`，HTTP 503，`SYSTEM_SERVICE_UNAVAILABLE`，434.96ms。
- 只读数据库查询关联挑战 `01a0a3a9-4dee-733c-b63b-d5ce486c26c7`：`DELIVERY_FAILED`，deliveredAt=null、consumedAt=null、failedAttempts=0。此请求在邮件投递环节失败，未进入用户输入验证码的校验阶段。
- 14:00–14:05 同接口统计：43 次 503、92 次 429、7 次 202。这些是全接口请求数量，不代表同一个用户；429 不可归因到本诊断编号。
- 14:25–14:32:04 查询截止：同接口 12 次 202、7 次 429、无 503。当前容器 healthy，主模板 `E_154198061747`。实际 AoKSend 日志持续 ACCEPTED，最近一条 14:32:05 为 53ms。

## 原因与边界

直接原因已确认是验证码投递失败。结合当时生产使用的内置模板被用户编辑后重新审核，以及腾讯云在 14:27 实测返回 `FailedOperation.ExceedSendLimit`，最符合现有证据的解释是主模板不可调用且备用发送受限。

该具体挑战的提供商原始结果未在已检查的持久 HTTP 日志中留存；当前容器已于模板切换时重建，因此不能把稍后探测的提供商错误码声称为 14:02 该请求的逐条日志证据。

14:24 已切换到审核通过的正式 AoKSend 模板，并完成真实收件确认。此诊断编号早于修复，当前主通道真实业务已恢复接受发送。学生需重新请求新验证码；旧挑战已投递失败且过期。

腾讯云备用日发送总量受限仍未解决；主通道恢复不等于完整容灾恢复。待额度恢复后验证正式备用模板 60326。此轮未再次发邮件、未修改账号或验证码状态。

只读复核脚本：`tools/production/inspect-mail-incident-20260915.py`，输出仅限挑战状态、时间、HTTP 汇总与非敏感邮件配置。
