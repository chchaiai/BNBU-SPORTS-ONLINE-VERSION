import fs from 'node:fs';import assert from 'node:assert/strict';import {createRequire} from 'node:module';import {createTestPrisma} from '../../backend/test/helpers/database.ts';
const uuid=createRequire(new URL('../../backend/package.json',import.meta.url))('uuid').v7,dir='/workspace/.browser-state',state=JSON.parse(fs.readFileSync(dir+'/state.json')),feedback=JSON.parse(fs.readFileSync(dir+'/student-feedback-current.json'));
const url=new URL('postgresql://sql-postgres:5432/v81_browser_test');url.username=process.env.PGUSER;url.password=process.env.PGPASSWORD;const prisma=createTestPrisma(url.href);
try{assert.equal((await prisma.$queryRaw`SELECT current_database() AS name`)[0].name,'v81_browser_test');const fixture=[];
for(const [targetId,missing] of [[feedback.id,false],[uuid(),true]]){const id=uuid();await prisma.notification.create({data:{id,organizationId:state.fixture.organizationId,recipientUserId:state.fixture.adminUserId,notificationType:'COURSE_UPDATE',title:`Synthetic feedback target ${missing?'missing':'existing'}`,body:'Synthetic notification for feedback detail navigation.',targetType:'FEEDBACK',targetId,createdAt:new Date()}});fixture.push({id,targetId,missing});}
fs.writeFileSync(dir+'/admin-feedback-notification.json',JSON.stringify(fixture));console.log('SYNTHETIC_ADMIN_FEEDBACK_NOTIFICATIONS_READY');}finally{await prisma.$disconnect();}
