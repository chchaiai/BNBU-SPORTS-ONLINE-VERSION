import fs from 'node:fs';import assert from 'node:assert/strict';import { chromium } from '../../.local/browser-test/node_modules/playwright-core/index.mjs';
const state=JSON.parse(fs.readFileSync('.local/v81-browser-state/state.json'));const browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1100}});page.setDefaultTimeout(60000);
try{await page.goto('http://localhost:3300/');await page.locator('#login-account').fill(state.accounts.admin.email);await page.locator('#login-password').fill(state.accounts.admin.password);await page.getByRole('button',{name:'登录',exact:true}).click();await page.getByRole('button',{name:'系统模式',exact:true}).click();const manual=page.locator('[data-admin-form="manual"]'),ocr=page.locator('[data-admin-form="ocr"]');await manual.getByLabel('审核课程').selectOption(state.fixture.teacherAActiveSectionId);await manual.getByText(/当前状态: 开启/).waitFor();await ocr.getByLabel('OCR 服务商').waitFor();assert.equal(await ocr.getByLabel('OCR 服务商').inputValue(),'DISABLED');await manual.scrollIntoViewIfNeeded();await page.screenshot({path:'docs/implementation/admin-review-services-web.png',animations:'disabled'});await page.getByRole('button',{name:'全局规则',exact:true}).click();const templates=page.locator('[data-admin-form="templates"]');await templates.getByText(/Synthetic Web Template/).waitFor();await templates.scrollIntoViewIfNeeded();await page.screenshot({path:'docs/implementation/admin-template-web.png',animations:'disabled'});await page.getByRole('button',{name:'English',exact:true}).click();await templates.getByRole('heading',{name:'Unified exercise template',exact:true}).waitFor();console.log(JSON.stringify({result:'PASS',readback:true,manualEnabled:true,ocrDisabled:true,templateVisible:true,englishForm:true}));}finally{await browser.close();}
